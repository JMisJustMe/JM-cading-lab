name: Deploy Canonical Cloudflare Estate Package

on:
  push:
    branches:
      - main
    paths:
      - "cloudflare-estate-package/**"
      - "theory/**"
      - "lyrics/**"
      - "recovery/**"
      - ".github/workflows/deploy-cloudflare-estate-package.yml"
  workflow_dispatch:

concurrency:
  group: jmisjustme-estate-production
  cancel-in-progress: true

permissions:
  contents: read
  deployments: write

jobs:
  validate-and-deploy:
    name: Rebuild, validate and deploy jmisjustme-estate
    runs-on: ubuntu-latest
    timeout-minutes: 12

    steps:
      - name: Check out Estate source
        uses: actions/checkout@v6

      - name: Rebuild canonical Estate from shell and current rooms
        shell: bash
        run: |
          set -euo pipefail
          rm -rf cloudflare-estate
          mkdir -p cloudflare-estate
          cat cloudflare-estate-package/shell-part-* > "$RUNNER_TEMP/jm-estate-shell-v1.14.tar.xz.b64"
          base64 --decode "$RUNNER_TEMP/jm-estate-shell-v1.14.tar.xz.b64" > "$RUNNER_TEMP/jm-estate-shell-v1.14.tar.xz"
          tar -xJf "$RUNNER_TEMP/jm-estate-shell-v1.14.tar.xz" -C cloudflare-estate
          cp -a theory cloudflare-estate/theory
          cp -a lyrics cloudflare-estate/lyrics
          cp -a recovery cloudflare-estate/recovery

      - name: Verify required public doors
        shell: bash
        run: |
          set -euo pipefail
          test -f cloudflare-estate/index.html
          test -f cloudflare-estate/theory/index.html
          test -f cloudflare-estate/lyrics/index.html
          test -f cloudflare-estate/recovery/index.html
          test -f cloudflare-estate/DEPLOY_MANIFEST.json
          grep -q "JMISJUSTME" cloudflare-estate/index.html
          grep -q "v1.14.0" cloudflare-estate/DEPLOY_MANIFEST.json
          grep -q "JM3232 Navigator" cloudflare-estate/recovery/index.html
          echo "Canonical Estate shell plus current rooms passed."

      - name: Deploy existing Cloudflare Pages project
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy cloudflare-estate --project-name=jmisjustme-estate --branch=main
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
