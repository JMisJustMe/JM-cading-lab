# Zionfolder Manifest — JM Authority Hold Engine v0.2

Open `OPEN_FIRST.html` or `index.html`.

## Source bodies

- `core/` — governance kernel, scopes, models, delegation, handoff, receipts, replay and RouteOS bridge.
- `tests/` — 19 hard-body tests.
- `app.mjs`, `index.html`, `styles.css` — five-room responsive interface.
- `manifest.webmanifest`, `sw.js`, `icon.svg` — installable offline PWA shell.
- `BUILD_RECEIPT.md`, `SHA256SUMS.txt` — proof and content identity.
