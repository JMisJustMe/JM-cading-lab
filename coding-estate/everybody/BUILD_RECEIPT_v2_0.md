# JM EveryBody v2.0 — Current Canonical-Native Completion Receipt

**Date:** 17 July 2026  
**Release:** v2.0  
**State:** CURRENT CANONICAL-NATIVE COMPLETE  
**Registered federation:** 100 bodies  
**Current native completion:** 100 / 100  
**Current native degree:** 10 / 10 for every body  
**Historical syntax fabricated:** No  
**Supreme body crowned:** No

## Proof target

Close every remaining present-day native specification gap while preserving historical truth.

The release must:

1. give every registered body an official current native specification;
2. preserve every body’s identity, kind, law and capabilities;
3. prevent one generic grammar from silently replacing different body meanings;
4. provide parser, AST, semantics, source map, lowering, runtime, trace and Ding;
5. reject cross-body commands and malformed completion claims;
6. preserve the four exact recovered historical-native subsets;
7. keep unrecovered historical syntax labelled as unrecovered rather than manufacturing it;
8. retain all existing portable and multi-target proof.

## Completion body

Every one of the 100 bodies now receives:

- canonical-native schema `jm.everybody.canonical-native-spec/2.0`;
- current source version `2.0`;
- one of thirteen semantic grammar families;
- family-specific commands and effects;
- body-specific `CAP_*` commands derived from its registered capabilities;
- exact registered-law binding;
- body-id and version validation;
- token stream with line and column contact;
- body-identified canonical AST;
- semantic/effect validation;
- source-map records;
- identity-preserving lowering into the shared portable IR;
- runtime route and trace execution;
- assertions and final Ding;
- deterministic formatter;
- parse → format → parse round-trip proof;
- individual specification, source, AST, lowering and receipt files.

## Grammar families

The current native system preserves distinct meanings through thirteen grammar families:

1. route;
2. logic;
3. formula;
4. embodied;
5. compiler;
6. runtime;
7. game;
8. governance;
9. delivery;
10. visual;
11. authoring;
12. composition;
13. service.

A body must execute at least one operator proving its family meaning and at least one command derived from its own capabilities. Commands outside the body’s specification fail visibly.

## Conformance

Each body receives five mandatory checks:

1. positive native parse, semantic validation, lowering and execution;
2. parse/format/parse round-trip;
3. wrong-body rejection;
4. unknown-command rejection;
5. missing-Ding rejection.

**Body-level canonical-native conformance:** 500 checks  
**Bodies passing current native execution:** 100 / 100  
**Bodies passing round-trip:** 100 / 100  
**Bodies passing identity/law/capability validation:** 100 / 100

## Strongest and formerly weakest proof

- **Quadze** retains the strongest recovered full-source and historical-native evidence.
- **Root Method**, previously one of the thinnest historical profiles, now has the same current engineering floor: runtime-family grammar, opcode and VM capability commands, parser, AST, source map, semantics, lowering, runtime, round-trip and Ding.

This equalises present-day engineering without flattening historical evidence.

## Historical-native lane

Exact recovered historical-native subsets remain executable for:

1. Quadze;
2. Cading;
3. MMZG / MorseMinus ZeroGrip;
4. Speakuals.

The other bodies are not falsely described as recovered historical grammars. Their current canonical-native v2 specifications are authoritative for present builds and remain explicitly separate from historical source evidence.

## Portable and target preservation

The v2 layer lowers into the already completed portable fabric. The full 100-body estate retains execution across:

- JavaScript;
- TypeScript;
- WebAssembly;
- C++98;
- C++03;
- C++11;
- C++14;
- C++17;
- C++20;
- C++23;
- C++26 draft through Clang `c++2c`;
- Rust 2021;
- Rust 2024.

Targets remain delivery bodies. They do not govern the JM-native source.

## Generated release body

`npm run build` produces:

```text
generated/canonical-native-v2/
├── specs/       100 specification JSON files
├── source/      100 current native .jmn2 programs
├── ast/         100 canonical AST and source-map files
├── portable/    100 identity-preserving portable lowerings
├── receipts/    100 current-native receipts
├── CURRENT_NATIVE_COMPLETION_MATRIX_v2_0.json
├── CURRENT_NATIVE_COMPLETION_MATRIX_v2_0.md
└── INDEX.json
```

The complete generated portable and current-native estate is preserved by the GitHub Actions artifact:

```text
jm-everybody-v2-100-body-current-native-packs
```

The final release-head artifact ID, digest and retention date are recorded after the final green release run.

## Open-first

- `00_OPEN_FIRST.html` — immediate route;
- `canonical-native-v2.html` — mobile-first 100-body workbench;
- `OPEN_FIRST.md` — command and proof route;
- `cli.mjs spec <body-id>` — inspect one body;
- `cli.mjs native <body-id> --mode canonical` — compile one current native body.

## Ding

**JM EveryBody v2.0 is complete as the current canonical-native specification, parser, semantic, lowering, runtime, source-map, round-trip and receipt fabric for every body in the presently registered 100-body federation.**

## Claim boundary

This receipt does not claim:

- that 100 is the final historical estate count;
- that every coding body is the same category of language;
- that every lost historical file has been found;
- that the current v2 syntax existed unchanged in an earlier version;
- that historical evidence differences have disappeared;
- that Quadze, Cading, MMZG, JM EveryBody or another body is supreme;
- that future recovered history cannot improve an evidence record.

## Keeper

> Every current body now has a native way to speak. History remains labelled by what history can actually prove.
