# Next Gates

## Gate 1 — CI
Run the executable proof against the actual donor modules. No pass, no advance.

## Gate 2 — Creator room
Build a small creator/gamer-facing room around this exact runtime:

`WRITE / SAY → MARK → RUN → SEE CHANGE → ALTER → REUSE`

The creator room must not expose compiler internals by default.

## Gate 3 — Owner word contact
Exercise several ordinary words and separate:
- marking-only standing;
- recovered/discoverable relation;
- owner-authorised local role;
- rejected role.

## Gate 4 — Composition
Go beyond one `and` split while preserving grouping, order and two-sided custody.

## Gate 5 — Real sovereign routing
Replace federation envelopes with permission-checked contact through the mounted estate router. Preserve body identity and donor boundaries.

## Gate 6 — Target adapters
Use existing polyglot/compiler machinery only when the selected body/route actually needs it.
