import {CrownRegister,CurrentBestRegister,SourceLedger,Zionfolder} from './governance-delivery-native.mjs';
import {BuilderLads,Goisible,REAPROACH,RouteForm,TBS,ToolKnit} from './route-making-native.mjs';
import * as C from './native-corpus.mjs';
import {check,expect,rejects} from './selftest-support.mjs';
export const zionFiles={'Door.cad':'open door = door.open.✓','OPEN_FIRST.md':'open','manifest.json':'{}','receipt.json':'{\"ok\":true}'};
export const proofs={source:{ok:true},parse:{ok:true},trace:{ok:true},ding:{ok:true}};
export const tbsContacts={sensor:true,'plain-language':true,PatTurn:true,trace:true,REAPROACH:true,DoorMemory:true};
check('24 Zionfolder seals recoverable carrier',()=>{const r=Zionfolder.execute(C.ZION_SOURCE,zionFiles);expect(r.state.recoverable&&r.state.sha,'zion');return r;});
check('25 Zionfolder rejects missing OPEN_FIRST',()=>rejects(()=>Zionfolder.execute(C.ZION_SOURCE,{'Door.cad':'x','manifest.json':'{}','receipt.json':'{}'}),'ZION_FILE_MISSING'));
check('26 Current Best Register selects highest proven version',()=>{const r=CurrentBestRegister.execute(C.CURRENT_BEST_SOURCE,{'door-v1':{id:'door-v1',version:1,score:8,proof:'door-proof'},'door-v2':{id:'door-v2',version:2,score:9,proof:'door-proof'}});expect(r.state.chosen==='door-v2','current best');return r;});
check('27 Current Best Register rejects wrong proof',()=>rejects(()=>CurrentBestRegister.execute(C.CURRENT_BEST_SOURCE,{'door-v1':{id:'door-v1',version:1,proof:'wrong'}}),'CBR_PROOF_MISMATCH'));
check('28 Crown Register grants working crown through gates',()=>{const r=CrownRegister.execute(C.CROWN_SOURCE,{source:true,trace:true,ding:true});expect(r.state.passed&&r.state.status==='working-crown','crown');return r;});
check('29 Crown Register holds missing Ding',()=>{const r=CrownRegister.execute(C.CROWN_SOURCE,{source:true,trace:true});expect(!r.state.passed&&r.state.status==='hold','crown hold');return r;});
check('30 Source Ledger records authority and body link',()=>{const l=new SourceLedger();const r=l.record(C.LEDGER_SOURCE);expect(r.state.authority==='JM'&&r.state.body==='Door','ledger');return r;});
check('31 Source Ledger rejects authority conflict',()=>{const l=new SourceLedger();l.record(C.LEDGER_SOURCE);return rejects(()=>l.record(C.LEDGER_SOURCE.replace('authority = JM','authority = Other')),'LEDGER_AUTHORITY_CONFLICT');});
check('32 Builder Lads builds tests and exports',()=>{const r=BuilderLads.execute(C.BUILDER_SOURCE,{assembleDoor:(body,purpose)=>({body,purpose,opens:true,identity:true}),opens:b=>b.opens,identity:b=>b.identity});expect(r.state.tests.every(t=>t.passed)&&r.state.export==='DoorPack','builder');return r;});
check('33 Builder Lads rejects failed build test',()=>rejects(()=>BuilderLads.execute(C.BUILDER_SOURCE,{assembleDoor:()=>({opens:false,identity:true}),opens:b=>b.opens,identity:b=>b.identity}),'BUILDER_TEST_FAILED'));
check('34 REAPROACH protects source at re-entry',()=>{const r=REAPROACH.execute(C.REAPP_SOURCE,{source:'Door',contact:{source_contact:true}});expect(r.state.identityPreserved&&r.state.reentry==='DoorLab','reapproach');return r;});
check('35 REAPROACH rejects absent source contact',()=>rejects(()=>REAPROACH.execute(C.REAPP_SOURCE,{source:'Door',contact:{}}),'REAPP_GATE_FAILED'));
check('36 RouteForm opens doubling branches',()=>{const r=RouteForm.execute(C.ROUTEFORM_SOURCE);expect(r.state.opened===3&&r.state.values.at(-1)===8,'routeform');return r;});
check('37 RouteForm rejects broken pattern',()=>rejects(()=>RouteForm.execute(C.ROUTEFORM_SOURCE.replace('[2,4,8]','[2,5,8]')),'ROUTEFORM_PATTERN'));
check('38 ToolKnit carries proof thread forward',()=>{const r=ToolKnit.execute(C.TOOLKNIT_SOURCE,proofs);expect(r.state.carried&&r.state.output==='DoorThread','toolknit');return r;});
check('39 ToolKnit rejects missing proof thread',()=>rejects(()=>ToolKnit.execute(C.TOOLKNIT_SOURCE,{source:{},parse:{},trace:{}}),'KNIT_MISSING_THREAD'));
check('40 Goisible proves visible understandable movable fit',()=>{const r=Goisible.execute(C.GOISIBLE_SOURCE);expect(r.state.usable&&r.state.fit==='gold','goisible');return r;});
check('41 Goisible rejects invisible route body',()=>rejects(()=>Goisible.parse(C.GOISIBLE_SOURCE.replace('visible = true','visible = false')),'GOISIBLE_CORE'));
check('42 TBS turns contacted theory into body',()=>{const r=TBS.execute(C.TBS_SOURCE,tbsContacts);expect(r.state.formed&&r.state.stack.length===8,'tbs');return r;});
check('43 TBS rejects missing recovery',()=>rejects(()=>TBS.execute(C.TBS_SOURCE,{...tbsContacts,REAPROACH:false}),'TBS_STAGE_MISSING'));
