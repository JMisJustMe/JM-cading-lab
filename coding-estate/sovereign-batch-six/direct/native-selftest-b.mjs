import {FingerOne,FingerTwo,GameForge,GlyphForge,GlyphPlay,JMVisualRuntime,KadingGameEstateEngine,PatternTapping,PLAYFORM,Seedform} from './game-input-native.mjs';
import * as C from './native-corpus.mjs';
import {check,expect,rejects} from './selftest-support.mjs';
export const doorGame={name:'DoorGame',identity:'DoorGame.v1',capabilities:['tap','drag','save'],state:'prototype'};
export const forgeServices={mobileAdapter:g=>({...g,mobile:true}),traceAdapter:g=>({...g,trace:true}),buildGame:g=>({...g,built:true})};
export const kServices={codeWithKading:g=>({...g,coded:'Kading'}),runKadingGame:(g,organs)=>({...g,running:true,organs}),serveKadingGame:g=>({...g,served:true})};
export const taps=[{tap:'left',time:0},{tap:'right',time:100},{tap:'left',time:220}];
check('23 JM Visual Runtime turns touch into state',()=>{const r=JMVisualRuntime.execute(C.VISUAL_RUNTIME_SOURCE,{type:'tap',value:{x:2}},{door:{}});expect(r.state.door.input.x===2&&r.result.consequenceReadable,'visual runtime');return r;});
check('24 JM Visual Runtime rejects unknown input',()=>rejects(()=>JMVisualRuntime.execute(C.VISUAL_RUNTIME_SOURCE,{type:'swipe',value:1},{}),'VRT_INPUT_UNKNOWN'));
check('25 GameForge builds distinct multi-target game',()=>{const r=GameForge.execute(C.GAMEFORGE_SOURCE,doorGame,forgeServices);expect(r.state.distinct&&r.state.outputs.android,'gameforge');return r;});
check('26 GameForge rejects wrong game identity',()=>rejects(()=>GameForge.execute(C.GAMEFORGE_SOURCE,{...doorGame,identity:'Other'},forgeServices),'GAMEFORGE_IDENTITY'));
check('27 GlyphPlay executes creator glyph contact',()=>{const r=GlyphPlay.execute(C.GLYPHPLAY_SOURCE,'OPEN',{type:'tap',position:4});expect(r.state.sourceRoutePreserved&&r.state.creator==='JM','glyphplay');return r;});
check('28 GlyphPlay rejects wrong interaction',()=>rejects(()=>GlyphPlay.execute(C.GLYPHPLAY_SOURCE,'OPEN',{type:'drag'}),'GLYPHPLAY_INPUT_MISMATCH'));
check('29 GlyphForge preserves control grammar',()=>{const r=GlyphForge.execute(C.GLYPHFORGE_SOURCE,doorGame);expect(r.state.controlIdentityPreserved&&r.state.controlGrammar==='DoorControls','glyphforge');return r;});
check('30 GlyphForge rejects missing capability',()=>rejects(()=>GlyphForge.execute(C.GLYPHFORGE_SOURCE,{name:'DoorGame',capabilities:['tap']}),'GLYPHFORGE_CAPABILITY_MISSING'));
check('31 PLAYFORM executes form-region interaction',()=>{const r=PLAYFORM.execute(C.PLAYFORM_SOURCE,{region:'handle',type:'drag',value:7},{play:{}});expect(r.result.mutuallyExecutable&&r.state.play.lastInput.value===7,'playform');return r;});
check('32 PLAYFORM rejects unknown region',()=>rejects(()=>PLAYFORM.execute(C.PLAYFORM_SOURCE,{region:'window',type:'tap',value:1},{}),'PLAYFORM_REGION_UNKNOWN'));
check('33 Kading Game Estate Engine inherits organs through Kading',()=>{const r=KadingGameEstateEngine.execute(C.KADING_GAME_SOURCE,doorGame,kServices);expect(!r.state.hostFirst&&r.state.hosted.served,'kading game');return r;});
check('34 Kading Game Estate Engine rejects missing service',()=>rejects(()=>KadingGameEstateEngine.execute(C.KADING_GAME_SOURCE,doorGame,{codeWithKading:kServices.codeWithKading}),'KGE_SERVICE_MISSING'));
check('35 Pattern-Tapping parses temporal contact',()=>{const r=PatternTapping.execute(C.PATTERN_TAP_SOURCE,taps);expect(r.state.temporal&&r.state.route==='door.open','tap');return r;});
check('36 Pattern-Tapping rejects excessive gap',()=>rejects(()=>PatternTapping.execute(C.PATTERN_TAP_SOURCE,[{tap:'left',time:0},{tap:'right',time:400},{tap:'left',time:500}]),'PTAP_GAP'));
check('37 Seedform governs choice combo permission',()=>{const r=Seedform.execute(C.SEEDFORM_SOURCE,{permission:'player',choice:'open',combo:'tap-hold'},{seedform:{}});expect(r.result.governed&&r.state.seedform.selection.choice==='open','seedform');return r;});
check('38 Seedform rejects unpermitted input',()=>rejects(()=>Seedform.execute(C.SEEDFORM_SOURCE,{permission:'guest',choice:'open',combo:'tap-hold'},{}),'SEED_PERMISSION'));
check('39 Finger One selects sovereign source body',()=>{const r=FingerOne.execute(C.FINGER_ONE_SOURCE,{body:'Cading'},{Cading:{identity:'Cading'},FlowTalk:{},JMLogic:{}});expect(!r.state.flattened&&r.state.selected==='Cading','finger one');return r;});
check('40 Finger One rejects unlisted body',()=>rejects(()=>FingerOne.execute(C.FINGER_ONE_SOURCE,{body:'Quadze'},{Quadze:{}}),'F1_BODY_UNKNOWN'));
check('41 Finger Two executes embodied temporal route',()=>{const r=FingerTwo.execute(C.FINGER_TWO_SOURCE,{route:'mudra',embodied:true,duration:350});expect(r.state.executable&&r.state.temporal===true,'finger two');return r;});
check('42 Finger Two rejects non-contact route',()=>rejects(()=>FingerTwo.execute(C.FINGER_TWO_SOURCE,{route:'mudra',embodied:false,duration:350}),'F2_CONTACT_REQUIRED'));
