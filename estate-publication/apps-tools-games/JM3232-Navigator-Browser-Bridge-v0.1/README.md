# JM3232 Navigator Browser Bridge v0.1 — Wave 2 public-source carriage

This publication seats the remaining **search / Radius Lexicon / Stringdoor widget corpus** that Wave 1 had only partially represented.

Authority is the governed `JM3232_NAVIGATOR_BROWSER_BRIDGE_v0_1_ZIONFOLDER.zip` (1,715,467 bytes; SHA-256 `3f3710376307d34b45bb1d0de0c69c1089f3855d277ce1afaeeb47f4594375f7`). The public carriage is a source/proof subset of that exact package. It includes the bridge core, FastAPI/MCP server, authority extractor, 881-document search index, 640-entry Radius Lexicon, source authorities, Stringdoor widget, tests and bounded proof receipts.

The full Zionfolder ZIP remains Estate provenance and is not duplicated here. Disposable pytest cache, exported test packs and screenshot proof images are excluded because they are not source/runtime dependencies.

## Exact carriage

The 35-file public subset is reproduced deterministically as **PAX tar → XZ preset 9 → Base64**, then split into 33 Git-safe text parts (32 × 18,000 bytes plus a final 1,920-byte part). The reconstructed `tar.xz` is **433,440 bytes** with SHA-256 `c821e3fad085291d35113c9d0543aee1b1fc58435b574a1a134398824c42b283`. Every part has both a SHA-256 and an expected Git blob identity in the publication manifest.

The prior 144,480-byte four-part carrier remains in Git ancestry as a failure receipt: connected text transport silently truncated it to 19,999 bytes. It is not live publication evidence and is not blessed by the repaired manifest.

## Claim boundary

This seats **buildable/provable source**, not public-host contact. HTTPS deployment, ChatGPT Developer Mode connection, signed-in host approval, owner physical desktop proof and Final Ding remain OPEN. Registration is not proof; Mesh does not mean merge.

GitHub validation reconstructs the 35-member public source package losslessly, verifies all 33 part identities, all source authorities and corpus counts, reruns extraction and 9 tests, checks the ten-tool/Stringdoor surface, and scans the public body for private signing/credential material.
