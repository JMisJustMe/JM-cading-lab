# Apps / Tools / Games Publication Validation Receipt v1.1

**Repository:** `JMisJustMe/JM-cading-lab`  
**Branch:** `agent/estate-apps-games-publication-wave-1`  
**Pull request:** #85  
**Date:** 2 August 2026

## Source-complete public bodies

### TraceBox / RouteBox v5.7R

- Multi-file PWA source published.
- JavaScript and service-worker syntax passed locally.
- JSON data model, version and manifest parsed successfully.
- Required PWA source assets present.

### JM Verballage v0.1

- Browser source, PWA organs, seed village and C carrier published.
- Browser JavaScript syntax passed locally.
- C carrier compiled under strict warnings and emitted valid JSON.

### LiveForge Engine v0.1

- Product bench shell, branch model, runtime, styling, PWA organs, sample state, route-map assets and receipts published.
- Original keeper application script passed JavaScript syntax.
- Exact local model/runtime top-level split passed syntax and recombined to the original script in source order.

### JMISJUSTME GitHub Pages v1.15.0

- Public-safe Living Estate source published.
- Six readable JavaScript limbs and four readable CSS limbs published.
- Exact local split validation re-created the keeper JavaScript and CSS bodies in source order.
- Public estate registry, hosting map, PWA shell and SVG identity assets published.
- Full-custody owner package remains private.

### ROUTEBREAK: Crown Relay v1.0.0

- Complete playable source published.
- Three JavaScript limbs passed syntax locally.
- Ordered local concatenation matched the original keeper `game.js` byte-for-byte.
- PWA shell, responsive controls and source receipt published.

### FTR First-Person v0.5.1

- Playable shell, styles, six readable source limbs, manifest, service worker and frozen capability receipts published.
- Exact local six-limb extraction passed syntax and re-wrapped/recombined to the original keeper script byte-for-byte.
- The public files preserve the same source order and route structure; they are not represented as the original inline single-file bytes.

## Source-partial public body

### JM3232 Navigator Browser Bridge v0.1

Published:

- architecture;
- README and claim boundary;
- governed bridge core;
- MCP server;
- dependencies.

Deferred:

- restored multi-megabyte search index;
- Radius Lexicon corpus;
- source-authority HTML bodies;
- complete widget/data package.

The public directory is not falsely described as a standalone complete runtime until that corpus crosses an appropriate source route.

## Exact keeper register

- Public-safe Library keepers hashed: **22**
- Combined registered ZIP size: **22,002,895 bytes**
- Exact package SHA-256 values recorded in `ESTATE_PUBLICATION_REGISTER_v1_0.json`.

## Project/chat account

`PROJECT_CHAT_CROSSWALK_v1_0.md` records completed, frozen, active and unrecovered lanes that ZIP-only inventory would miss, including GameCore, BodyMesh/OneGame, FTR Energy-to-Engine, PRESSUREPOINT/BREAKLINE, FOURFOLD, Box-Out, Sumo, 128 Hand, Western Sniper, COMBOUND, Bonewake, cardBORED/LG, Route Catch and Rim Route.

A crosswalk entry prevents omission; it does not falsely claim source publication.

## Public-safety boundary

Excluded from the public repository:

- `.keystore`-bearing archives;
- private signing-maintenance packages;
- explicit private-owner packages;
- full-custody website packages.

The ~139.5 MB PlayZone lossless archive remains on an oversize route requiring Releases, LFS or split assets.

## GitHub-side validation

`.github/workflows/estate-publication-validate.yml` validates the actual committed branch bytes by:

- running `node --check` across published JavaScript;
- parsing JSON and web manifests;
- compiling Python source;
- compiling and executing the Verballage C carrier;
- checking required bodies;
- checking service-worker asset references.

Workflow outcome is reported by GitHub Actions and must pass before this PR is represented as merge-ready.

## Claim state

**DING:** authenticated GitHub source commits, six complete public source bodies, one partial bridge, exact keeper register, project/chat crosswalk and external validation gate exist.

**NO DING:** every Library ZIP byte copied, every active project converted into completed source, or every current-head Wave 2 body published.
