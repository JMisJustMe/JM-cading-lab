function renderConnections(main){main.innerHTML=`<section class="view"><header class="page-title"><div><h1>One Master Body</h1><p>The website exists as portable source first. Hosts, mirrors, addresses and APKs are outputs and organs—not landlords.</p></div></header><div class="connection-stage"><div class="connection-core"><div><img src="./assets/jm-mark.svg" alt=""><b>Standalone Master Body</b><span>HTML · CSS · JavaScript · PWA · OneBody</span></div></div>${registry.connections.filter(x=>x.id!=='master').map((n,i)=>`<article class="connection-node ${i%2?'right':'left'} node-${i+1}"><b>${n.name}</b><p>${n.role}</p><span>${n.state}</span></article>`).join('')}</div><section class="section"><header class="section-head"><div><h2>Connection law</h2><p>Cloudflare works beautifully and remains permanent as a live rail. It does not erase the freestanding body.</p></div></header><table class="proof-table"><thead><tr><th>Layer</th><th>Role</th><th>State</th></tr></thead><tbody>${registry.connections.map(n=>`<tr><td>${n.name}</td><td>${n.role}</td><td>${n.state}</td></tr>`).join('')}</tbody></table></section></section>`;postRender()}
function renderCommand(main){const gates=[['Plan','Set the body, claim boundary and source authority.'],['Build','Create the host-neutral runtime and all required packages.'],['Deploy','Emit Cloudflare, InfinityFree, GitHub and generic host carriers.'],['Protect','Separate public data from owner-side source, notes and secrets.'],['Monitor','Keep live routes, versions, receipts and broken doors visible.'],['Evolve','Mount proven outputs without erasing prior lineage.']];main.innerHTML=`<section class="view"><header class="page-title"><div><h1>Command Centre</h1><p>Six gates. One body. Every change must leave a recoverable route and an honest receipt.</p></div></header><div class="command-grid">${gates.map((g,i)=>`<article class="command-card"><span class="num">0${i+1}</span><h3>${g[0]}</h3><p>${g[1]}</p><button data-command="${g[0]}">Run ${g[0]} check →</button></article>`).join('')}</div><section class="section"><header class="section-head"><div><h2>Package output matrix</h2><p>All outputs are derived from the same sovereign runtime.</p></div></header><table class="proof-table"><thead><tr><th>Output</th><th>Purpose</th><th>Status</th></tr></thead><tbody><tr><td>Public PWA</td><td>Installable multi-file web app</td><td>BUILT</td></tr><tr><td>One-file proof</td><td>Direct local opening and transport</td><td>BUILT</td></tr><tr><td>InfinityFree htdocs</td><td>Freestanding host package</td><td>BUILT</td></tr><tr><td>Cloudflare Pages</td><td>Live-rail deployment carrier</td><td>BUILT</td></tr><tr><td>GitHub Pages</td><td>Mirror and source corridor</td><td>BUILT</td></tr><tr><td>Android wrapper source</td><td>APK/AAB carrier from same body</td><td>SOURCE READY</td></tr><tr><td>Production-signed APK</td><td>Owner install/distribution identity</td><td>EXTERNAL GATE</td></tr></tbody></table></section></section>`;postRender()}
