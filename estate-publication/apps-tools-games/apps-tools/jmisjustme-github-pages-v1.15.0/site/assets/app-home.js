function renderHome(main){const h=registry.houses;main.innerHTML=`<section class="view">
<div class="hero"><div class="hero-art"></div><div class="hero-copy"><h1>JM LIVING <span>ESTATE</span></h1><p>The portable, owner-controlled ecosystem of <strong>Theodore Benjamin Scott</strong>. Theories. Lyrics. Games. Apps. Code. Books. Visuals. Recovery.</p><div class="hero-actions"><button class="btn primary" data-route="estates">Enter the Estate →</button><button class="btn gold" data-route="connections">See the living line</button></div><div class="hero-proof"><div><b>1</b><span>Master body</span></div><div><b>${h.length}</b><span>Primary houses</span></div><div><b>${registry.publicDoors.length}</b><span>Live public doors</span></div><div><b>∞</b><span>Portable routes</span></div></div></div><div class="hero-host"><small>CURRENT LIVE RAIL</small><strong>Cloudflare Estate</strong><span>Useful, proven and replaceable—not the definition of the body.</span></div></div>
<section class="section"><header class="section-head"><div><h2>Enter the Estate</h2><p>Choose a house. Each route keeps its own identity and connection.</p></div><button data-route="estates">View registry →</button></header><div class="house-grid">${h.slice(0,8).map(houseCard).join('')}</div></section>
<div class="lower-grid"><section class="mini-panel"><h3>Command Centre</h3><div class="gate-row">${['Plan','Build','Deploy','Protect','Monitor','Evolve'].map((x,i)=>`<div class="gate"><i>${i+1}</i><span>${x}</span></div>`).join('')}</div></section><section class="mini-panel"><h3>Owner Room</h3><div class="owner-quick"><button data-route="owner">Notes<b>${state.notes?1:0}</b></button><button data-route="library">Favourites<b>${state.favourites.length}</b></button><button data-route="library">Recent<b>${state.recent.length}</b></button><button id="exportPackHome">SavePack<b>Export</b></button></div></section><section class="mini-panel"><h3>What changed</h3><div class="activity">${(state.activity.length?state.activity.slice(0,4):[{event:'MASTER BODY',detail:'v1.15.0 formed'},{event:'PWA',detail:'offline shell ready'},{event:'HOSTING',detail:'adapters packaged'}]).map(x=>`<span><i></i><b>${x.event}</b><em>${x.detail}</em></span>`).join('')}</div></section></div>
<div class="quote-strip"><span>♛</span> “I don’t follow a path. I build my own. I don’t wait for a seat. I build the table.” <b>JM</b></div>
</section>`;postRender()}
function houseCard(h){return `<button class="house-card" data-house="${h.id}" style="--accent:${accent[h.accent]||accent.violet}"><span class="icon-orb">${icons[h.icon]||'◇'}</span><h3>${h.name}</h3><p>${h.description}</p><footer><span>${h.count}</span><b>→</b></footer></button>`}
