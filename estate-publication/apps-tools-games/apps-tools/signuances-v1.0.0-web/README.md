# Signuances v1.0.0 — Wave 2 web-source publication

This directory publishes the final crowned portable Signuances web body without conflating it with the separate Android/signing rail.

The canonical identity is the reconstructed `Signuances_SINGLE.html`: **220,954 bytes**, SHA-256 `fad2ddc3122ca47a7e1d130a0c27980822a1c542ba7ac52ceb2498fec497d631`.

The 16 source-carriage files are not newly encoded copies. They reuse the exact Git blobs from the closed, unmerged Android build-doorway PR #25, whose build workflow already reconstructed this same frozen HTML identity. The current publication workflow re-proves the XZ and HTML identities, Git-object continuity, JavaScript syntax, source markers and public/private boundary.

`1.0.0-rc.1` remains lineage only. Android source, APK/AAB, keystore/signing maintenance and physical-device claims are excluded from this web-source publication.
