# TraceBox / RouteBox v5.7R — Estate-Recovered Source Sync Candidate

Open first: `OPEN_FIRST.html` or `index.html`.

## What this is
This is a rebuilt recovery app body based on the Estate-confirmed TraceBox / RouteBox v5.7 spec and the locked source model.

## Honest boundary
The original v5.7 package bytes were **not** recovered in this chat. This body is therefore named **v5.7R**, not v5.7 byte-for-byte.

## Mounted locks
- v0.2: 2⁴ gate table and 16 state model.
- v2: usable simulator — choose scenario, set gates, run input, read state, recover route.
- v3: calibration — expected vs actual, Match / Drift / Break.
- v4: observation check.
- v5: route across domains.
- v5.7: mobile workflow + result review pattern.

## Android note
The single-file HTML works best for direct `content://` quick-open. The multi-file PWA works best from a local server or deployed URL so service workers can install.
