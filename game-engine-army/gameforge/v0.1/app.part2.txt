ate.consequence,codingBody:c.name,codingLaw:c.law,codingStages:c.stages,target:state.target,seed:state.seed},buildGraph:graph.nodes,scene};core.cartridgeHash=hash(JSON.stringify({project:core.project,gameBody:core.gameBody,composition:core.composition,buildGraph:core.buildGraph,scene:core.scene}));compiled=core;state.scene=clone(scene);state.runtime={player:{x:1,y:7},progress:0,state:'ACTIVE'};if(record){state.builds.unshift({at:core.builtAt,body:b.name,coding:c.name,hash:core.cartridgeHash,identity:graph.identity});state.builds=state.builds.slice(0,30);trace('Compiled '+b.name+' through '+c.name+'.','PASS')}renderAll()}
function renderBodies(){$('#bodyList').innerHTML=GAME_BODIES.map(b=>`<button class="bodyCard ${b.id===state.bodyId?'active':''}" data-body="${b.id}"><b>${b.name}</b><small>${b.tag}</small></button>`).join('');$$('[data-body]').forEach(x=>x.onclick=()=>setBody(x.dataset.body))}
function renderGraph(){const g=buildGraph();$('#graph').innerHTML=g.nodes.map(n=>`<div class="node"><span>${n.kind}</span><b>${n.value}</b><code>${n.hash}</code></div>`).join('');$('#topBody').textContent=body().name.toUpperCase();$('#topCoding').textContent=coding().name.toUpperCase();$('#seedHud').textContent=state.seed}
function renderIdentity(){const b=body();$('#identityText').innerHTML=`<b style="color:var(--gold)">${b.name}</b><br>${b.law}<br><br><b>Body identity:</b> <code>${buildGraph().identity}</code><br><b>Difference law:</b> changing the coding body may change the build route, but the selected game-body identity must remain stable.`}
function renderBuilds(){$('#buildList').innerHTML=state.builds.slice(0,10).map(b=>`<div class="buildItem"><b>${b.body}</b> through ${b.coding}<br><code>${b.hash}</code></div>`).join('')||'<div style="color:var(--mut);font-size:.75rem">No compiled cartridge yet.</div>'}
function renderTrace(){$('#trace').innerHTML=state.trace.slice(0,18).map(t=>`<div class="${t.type}"><b>${t.type}</b> ${t.message}<br><small>${t.at.slice(11,19)}</small></div>`).join('')}
function renderReceipt(){$('#receipt').textContent=compiled?JSON.stringify({body:compiled.gameBody.name,identity:compiled.gameBody.identityHash,coding:compiled.composition.codingBody,cartridge:compiled.cartridgeHash,target:compiled.composition.target},null,2):'Compile a cartridge to produce a receipt.';$('#topBuild').textContent=compiled?compiled.cartridgeHash:'OPEN';$('#cartridgeHud').textContent=compiled?compiled.cartridgeHash:'OPEN'}
const canvas=$('#preview'),ctx=canvas.getContext('2d');function resize(){const r=$('#previewWrap').getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);canvas.width=Math.max(520,Math.floor(r.width*d));canvas.height=Math.max(420,Math.floor(r.height*d));ctx.setTransform(d,0,0,d,0,0);draw()}
function met(){const w=canvas.clientWidth,h=canvas.clientHeight,tw=58,th=29,eh=21;return{w,h,tw,th,eh,ox:w/2,oy:Math.max(70,h*.17)}}function pt(x,y,z=0){const m=met();return{x:m.ox+(x-y)*m.tw/2,y:m.oy+(x+y)*m.th/2-z*m.eh}}function diamond(x,y,z,color){const m=met(),p=pt(x,y,z);ctx.beginPath();ctx.moveTo(p.x,p.y-m.th/2);ctx.lineTo(p.x+m.tw/2,p.y);ctx.lineTo(p.x,p.y+m.th/2);ctx.lineTo(p.x-m.tw/2,p.y);ctx.closePath();ctx.fillStyle=color;ctx.fill();ctx.strokeStyle='#34536d';ctx.stroke()}function block(e,color){const m=met(),base=pt(e.x,e.y,e.z||0),top=pt(e.x,e.y,(e.z||0)+1);ctx.fillStyle=color+'cc';for(const poly of [[[top.x,top.y-m.th/2],[top.x-m.tw/2,top.y],[base.x-m.tw/2,base.y],[base.x,base.y-m.th/2]],[[top.x,top.y-m.th/2],[top.x+m.tw/2,top.y],[base.x+m.tw/2,base.y],[base.x,base.y-m.th/2]]]){ctx.beginPath();ctx.moveTo(...poly[0]);poly.slice(1).forEach(p=>ctx.lineTo(...p));ctx.closePath();ctx.fill();ctx.strokeStyle='#ffffff22';ctx.stroke()}diamond(e.x,e.y,(e.z||0)+1,color)}
function draw(){const m=met();ctx.clearRect(0,0,m.w,m.h);const grad=ctx.createLinearGradient(0,0,0,m.h);grad.addColorStop(0,'#173a52');grad.addColorStop(1,'#040a10');ctx.fillStyle=grad;ctx.fillRect(0,0,m.w,m.h);if(!state.scene)return;for(const t of [...state.scene.tiles].sort((a,b)=>(a.x+a.y)-(b.x+b.y)))diamond(t.x,t.y,t.z,(t.x+t.y)%2?'#17374b':'#1c4055');for(const source of [...state.scene.entities].sort((a,b)=>(a.x+a.y+a.z)-(b.x+b.y+b.z))){const e=source.kind==='player'?{...source,x:state.runtime.player.x,y:state.runtime.player.y}:source;const color=e.kind==='player'?body().palette[0]:e.kind==='goal'?COLORS.goal:e.kind==='node'?body().palette[1]:e.kind==='hazard'?COLORS.hazard:COLORS.wall;block(e,color);const p=pt(e.x,e.y,(e.z||0)+1);ctx.fillStyle='#041018';ctx.font='900 12px system-ui';ctx.textAlign='center';ctx.fillText(e.kind==='player'?'P':e.kind==='goal'?'G':e.kind==='node'?String(e.order+1):e.kind==='hazard'?'!':'■',p.x,p.y)}}
function move(dx,dy){if(mode!=='TEST'||!state.scene||state.runtime.state!=='ACTIVE')return;const p=state.runtime.player,nx=p.x+dx,ny=p.y+dy;if(nx<0||ny<0||nx>=state.scene.w||ny>=state.scene.h||state.scene.entities.some(e=>e.kind==='wall'&&e.x===nx&&e.y===ny)){trace('PermissionGate rejected blocked movement.','FAULT');return}p.x=nx;p.y=ny;resolve();draw()}
function resolve(){const p=state.runtime.player;for(const e of state.scene.entities){if(e.x!==p.x||e.y!==p.y)continue;if(e.kind==='hazard'){trace('Consequence pressure held; Recovery remains available.','FAULT')}if(e.kind==='node'&&!e.hit){e.hit=true;state.runtime.progress++;trace('Coding stage contacted: '+e.name,'BUILD');if(state.runtime.progress>=4)state.scene.objective.open=true}if(e.kind==='goal'){if(state.scene.objective.open){state.runtime.state='WIN';trace('Cartridge play loop completed without identity collapse.','PASS');show('GAMEFORGE DING',body().name+' through '+coding().name)}else trace('Goal held until all coding stages complete.','FAULT')}}$('#progressHud').textContent=state.runtime.progress+'/4'}
function tap(ev){if(mode!=='TEST')return;const r=canvas.getBoundingClientRect(),m=met(),sx=ev.clientX-r.left,sy=ev.clientY-r.top,dx=sx-m.ox,dy=sy-m.oy,x=Math.round((dy/(m.th/2)+dx/(m.tw/2))/2),y=Math.round((dy/(m.th/2)-dx/(m.tw/2))/2),p=state.runtime.player;move(Math.abs(x-p.x)>Math.abs(y-p.y)?Math.sign(x-p.x):0,Math.abs(x-p.x)>Math.abs(y-p.y)?0:Math.sign(y-p.y))}
function setMode(next){if(next===mode)return;if(next==='TEST'){if(!compiled)compile();testBackup=clone(state.scene);mode='TEST';state.runtime={player:{x:1,y:7},progress:0,state:'ACTIVE'};trace('Cartridge entered playtest from a recoverable build snapshot.','BUILD')}else{mode='EDIT';if(testBackup){state.scene=clone(testBackup);testBackup=null;trace('Forge restored compiled design after playtest.','PASS')}}$('#modeHud').textContent=mode;$('#testBtn').classList.toggle('active',mode==='TEST');$('#editBtn').classList.toggle('active',mode==='EDIT');hide();renderAll()}
function retry(){if(testBackup)state.scene=clone(testBackup);state.runtime={player:{x:1,y:7},progress:0,state:'ACTIVE'};hide();trace('RecoveryBody restored the cartridge baseline.','PASS');renderAll()}
function show(t,s){$('#overlayText').innerHTML=`${t}<small>${s}</small>`;$('#overlay').classList.add('show')}function hide(){$('#overlay').classList.remove('show')}
function proveDifference(){syncForm();const idBefore=buildGraph().identity,routeBefore=buildGraph().route,keys=Object.keys(CODING),i=keys.indexOf(state.coding),other=keys[(i+1)%keys.length],old=state.coding;state.coding=other;const idAfter=buildGraph().identity,routeAfter=buildGraph().route;state.coding=old;const pass=idBefore===idAfter&&routeBefore!==routeAfter;trace(pass?'Difference proof PASS: game identity held while coding route changed.':'Difference proof FAULT. ',pass?'PASS':'FAULT');renderReceipt()}
function snapshot()