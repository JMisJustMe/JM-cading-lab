{syncForm();return{project:state,compiled,receipt:{at:now(),bodyIdentity:buildGraph().identity,routeHash:buildGraph().route,cartridgeHash:compiled?.cartridgeHash||null}}}function save(){localStorage.setItem('gameforge-sovereign-v01',JSON.stringify(snapshot()));trace('Forge state saved.','PASS')}function load(){const raw=localStorage.getItem('gameforge-sovereign-v01');if(!raw)return trace('No saved forge state found.','FAULT');const s=JSON.parse(raw);state=s.project;compiled=s.compiled;fillForm();trace('Forge state restored.','PASS');renderAll()}
function download(name,text,type='application/json'){const a=document.createElement('a'),u=URL.createObjectURL(new Blob([text],{type}));a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}function exportCartridge(){if(!compiled)compile();download((state.name.replace(/\W+/g,'_')||'GameForge')+'.jmcartridge.json',JSON.stringify(compiled,null,2));trace('Identity-bound cartridge exported.','PASS')}function playableDocument(c){const data=JSON.stringify(c).replace(/</g,'\\u003c');return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>${c.project}</title><style>*{box-sizing:border-box}html,body{margin:0;height:100%;overflow:hidden;background:#06101a;color:#fff;font-family:system-ui}main{height:100%;display:grid;grid-template-rows:auto 1fr auto;padding:8px;gap:7px}header,footer{display:flex;gap:6px;align-items:center;justify-content:center;flex-wrap:wrap}canvas{width:100%;height:100%;border:1px solid #34536d;border-radius:18px;background:#081724;touch-action:none}button{min-width:48px;min-height:42px;border:1px solid #34536d;border-radius:11px;background:#13263a;color:#fff}b{color:#ffd166}.hud{justify-content:space-between}</style></head><body><main><header class="hud"><span><b>${c.gameBody.name}</b> through ${c.composition.codingBody}</span><span id="p">0/4</span><span>${c.cartridgeHash}</span></header><canvas id="c"></canvas><footer><button data-m="0,-1">↑</button><button data-m="-1,0">←</button><button data-m="0,1">↓</button><button data-m="1,0">→</button><button id="r">Retry</button></footer></main><script>const C=${data},cv=document.querySelector('#c'),x=cv.getContext('2d');let scene,player,progress,state;function reset(){scene=JSON.parse(JSON.stringify(C.scene));player={x:1,y:7};progress=0;state='ACTIVE';draw()}function met(){return{w:cv.clientWidth,h:cv.clientHeight,tw:58,th:29,eh:21,ox:cv.clientWidth/2,oy:Math.max(70,cv.clientHeight*.17)}}function pt(a,b,z=0){const m=met();return{x:m.ox+(a-b)*m.tw/2,y:m.oy+(a+b)*m.th/2-z*m.eh}}function d(a,b,z,col){const m=met(),q=pt(a,b,z);x.beginPath();x.moveTo(q.x,q.y-m.th/2);x.lineTo(q.x+m.tw/2,q.y);x.lineTo(q.x,q.y+m.th/2);x.lineTo(q.x-m.tw/2,q.y);x.closePath();x.fillStyle=col;x.fill();x.strokeStyle='#34536d';x.stroke()}function draw(){const r=cv.getBoundingClientRect(),D=Math.min(2,devicePixelRatio||1);cv.width=Math.max(520,Math.floor(r.width*D));cv.height=Math.max(420,Math.floor(r.height*D));x.setTransform(D,0,0,D,0,0);x.fillStyle='#081724';x.fillRect(0,0,cv.clientWidth,cv.clientHeight);scene.tiles.forEach(t=>d(t.x,t.y,t.z,(t.x+t.y)%2?'#17374b':'#1c4055'));scene.entities.forEach(e=>{const q=e.kind==='player'?{...e,x:player.x,y:player.y}:e,col=e.kind==='player'?'${body().palette[0]}':e.kind==='goal'?'#ffd166':e.kind==='node'?'${body().palette[1]}':e.kind==='hazard'?'#ff6b78':'#587086';d(q.x,q.y,(q.z||0)+1,col);const p=pt(q.x,q.y,(q.z||0)+1);x.fillStyle='#041018';x.font='900 12px system-ui';x.textAlign='center';x.fillText(q.kind==='player'?'P':q.kind==='goal'?'G':q.kind==='node'?String(q.order+1):q.kind==='hazard'?'!':'■',p.x,p.y)});document.querySelector('#p').textContent=progress+'/4'}function move(dx,dy){if(state!=='ACTIVE')return;const nx=player.x+dx,ny=player.y+dy;if(nx<0||ny<0||nx>=scene.w||ny>=scene.h||scene.entities.some(e=>e.kind==='wall'&&e.x===nx&&e.y===ny))return;player={x:nx,y:ny};scene.entities.forEach(e=>{if(e.x!==nx||e.y!==ny)return;if(e.kind==='node'&&!e.hit){e.hit=true;progress++}if(e.kind==='goal'&&progress>=4){state='WIN';setTimeout(()=>alert('GAMEFORGE DING — '+C.gameBody.name+' through '+C.composition.codingBody),20)}});draw()}document.querySelectorAll('[data-m]').forEach(b=>b.onclick=()=>move(...b.dataset.m.split(',').map(Number)));document.querySelector('#r').onclick=reset;addEventListener('keydown',e=>{const m={ArrowUp:[0,-1],w:[0,-1],ArrowDown:[0,1],s:[0,1],ArrowLeft:[-1,0],a:[-1,0],ArrowRight:[1,0],d:[1,0]};if(m[e.key])move(...m[e.key])});addEventListener('resize',draw);reset();<\/script></body></html>`}function exportPlayable(){if(!compiled)compile();download((state.name.replace(/\W+/g,'_')||'GameForge')+'_PLAYABLE.html',playableDocument(compiled),'text/html');trace('Standalone playable exported.','PASS')}function importFile(f){const r=new FileReader();r.onload=()=>{try{const x=JSON.parse(r.result);if(x.schema==='jm.gameforge-cartridge/0.1')loadCartridge(x);else if(x.project){state=x.project;compiled=x.compiled;fillForm();renderAll()}else throw Error('Unsupported GameForge file');trace('Forge import restored.','PASS')}catch(e){trace('Import fault: '+e.message,'FAULT')}};r.readAsText(f)}function loadCartridge(c){compiled=clone(c);state.bodyId=c.gameBody.id;state.name=c.project;state.input=c.composition.input;state.play=c.composition.play;state.consequence=c.composition.consequence;state.coding=Object.entries(CODING).find(([,v])=>v.name===c.composition.codingBody)?.[0]||'glyphplay';state.target=c.composition.target;state.seed=c.composition.seed;state.scene=clone(c.scene);fillForm();renderAll()}
function renderAll(){renderBodies();renderGraph();renderIdentity();renderBuilds();renderTrace();renderReceipt();draw();$('#progressHud').textContent=(state.runtime?.progress||0)+'/4'}
function reset(){state=fresh();compiled=null;mode='EDIT';fillForm();compile(false);trace('GameForge first floor opened.','PASS');renderAll()}
['projectName','inputLoop','playLoop','consequence','codingBody','target','seed','notes'].forEach(id=>$('#'+id).addEventListener('change',()=>{syncForm();compiled=null;trace('Build graph changed; compile required.','BUILD');renderAll()}));$('#compileBtn').onclick=()=>compile();$('#testBtn').onclick=()=>setMode('TEST');$('#editBtn').onclick=()=>setMode('EDIT');$('#saveBtn').onclick=save;$('#loadBtn').onclick=load;$('#importBtn').onclick=()=>$('#importFile').click();$('#importFile').onchange=e=>e.target.files[0]&&importFile(e.target.files[0]);$('#exportBtn').onclick=exportCartridge;$('#playableBtn').onclick=exportPlayable;$('#receiptBtn').onclick=()=>show('BUILD RECEIPT',compiled?compiled.cartridgeHash:'Compile first');$('#resetBtn').onclick=()=>confirm('Reset GameForge?')&&reset();$('#randomBtn').onclick=()=>{$('#seed').value=Math.floor(Math.random()*999999)+1;syncForm();compile()};$('#proveBtn').onclick=proveDifference;$$('[data-step]').forEach(b=>b.onclick=()=>move(...b.dataset.step.split(',').map(Number)));$('#actBtn').onclick=resolve;$('#mobileAct').onclick=resolve;$('#retryBtn').onclick=retry;canvas.addEventListener('pointerdown',tap);window.addEventListener('keydown',e=>{if(['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName))return;const m={ArrowUp:[0,-1],w:[0,-1],ArrowDown:[0,1],s:[0,1],ArrowLeft:[-1,0],a:[-1,0],ArrowRight:[1,0],d:[1,0]};if(m[e.key]){e.preventDefault();move(...m[e.key])}});window.addEventListener('resize',resize);window.GameForgeApp={loadCartridge,compile,state:()=>state,cartridge:()=>compiled,proveDifference};reset();resize();$('#app').dataset.ready='true';
})();
