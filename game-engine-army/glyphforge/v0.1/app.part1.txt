(()=>{'use strict';
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const SIZE=12,PALETTE=['#00000000','#5be9ff','#9e63ff','#ffd05e','#ff557b','#68ffad','#ff9a4d','#f8fbff','#27356f','#070a18'];
const KINDS=['player','enemy','wall','goal','projectile','effect','ui','marker'];
const ACTIONS=['move-up','move-down','move-left','move-right','aim','fire','fling','confirm','cancel'];
const INPUTS=['keyboard:ArrowUp','keyboard:ArrowDown','keyboard:ArrowLeft','keyboard:ArrowRight','keyboard:W','keyboard:S','keyboard:A','keyboard:D','pointer:drag','pointer:release','gesture:swipe','button:fire','button:confirm','button:cancel','touch:dpad-up','touch:dpad-down','touch:dpad-left','touch:dpad-right','none'];
const TARGETS={
 glyphplay:{name:'GlyphPlay',law:'Playable glyph assets must preserve stage contact, movement and source route.',required:['asset-atlas','move-actions','confirm-action','pointer-contact','save-roundtrip']},
 gameforge:{name:'GameForge',law:'Adapters must preserve game-body identity while exposing build and preview capabilities.',required:['asset-atlas','action-map','identity-hash','cartridge-export','save-roundtrip']},
 jmgamecore:{name:'JM GameCore',law:'Runtime assets and actions must enter a shared core without collapsing the donor body.',required:['asset-atlas','action-map','event-stream','serial-state','recovery-route']},
 kading:{name:'Kading Game Estate Engine',law:'Kading-fronted game assets must expose deterministic input and consequence routes.',required:['asset-atlas','action-map','deterministic-update','trace-receipt','recovery-route']}
};
const CAPABILITIES=['asset-atlas','move-actions','confirm-action','pointer-contact','save-roundtrip','action-map','identity-hash','cartridge-export','event-stream','serial-state','recovery-route','deterministic-update','trace-receipt'];
const MODE={Pull:{power:1,arc:0,spin:0},Pitch:{power:1.12,arc:1,spin:0},Fire:{power:1.32,arc:0,spin:0},Fling:{power:1.48,arc:.35,spin:1}};
const donor={file:'03_GAME_ENGINE__GLYPHFORGE__Forge_Routing_Repair.html',bytes:44208,sha256:'12447b3eca56488824878be5eb6b71ea3298a9728de3946843a8b44c486d649d'};
let state,view='ASSET',surface='PIXEL',selectedColor=1,drawMode='paint',undo=[],redo=[],compiled=null,aim=null,raf=0;
const uid=()=>crypto?.randomUUID?.()||'g'+Math.random().toString(36).slice(2)+Date.now();const clone=v=>JSON.parse(JSON.stringify(v));const now=()=>new Date().toISOString();
function hash(text){let h=2166136261;for(const c of String(text)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return(h>>>0).toString(16).padStart(8,'0')}
function blankPixels(){return Array.from({length:SIZE},()=>Array(SIZE).fill(0))}
function glyphPattern(kind){const p=blankPixels(),set=(x,y,c)=>{if(x>=0&&y>=0&&x<SIZE&&y<SIZE)p[y][x]=c};if(kind==='player'){for(let y=2;y<10;y++)for(let x=3;x<9;x++)if(y<5||x>4&&x<7)set(x,y,1);set(4,3,7);set(7,3,7)}else if(kind==='enemy'){for(let y=2;y<10;y++)for(let x=2;x<10;x++)if((x+y)%2)set(x,y,4);set(4,4,7);set(7,4,7)}else if(kind==='wall'){for(let y=2;y<10;y++)for(let x=1;x<11;x++)set(x,y,(x+y)%2?8:2)}else if(kind==='goal'){for(let y=1;y<11;y++)for(let x=1;x<11;x++)if(x===1||y===1||x===10||y===10||x===y||x+y===11)set(x,y,3)}else if(kind==='projectile'){for(let y=4;y<8;y++)for(let x=4;x<8;x++)set(x,y,6)}else{for(let i=2;i<10;i++){set(i,6,5);set(6,i,5)}}return p}
function asset(name,kind,glyph,color){return{id:uid(),name,kind,glyph,pixels:glyphPattern(kind),primary:color,version:1}}
function recommendedRoutes(){return{'move-up':'keyboard:ArrowUp','move-down':'keyboard:ArrowDown','move-left':'keyboard:ArrowLeft','move-right':'keyboard:ArrowRight','aim':'pointer:drag','fire':'pointer:release','fling':'gesture:swipe','confirm':'button:confirm','cancel':'button:cancel'}}
function fresh(){return{schema:'jm.glyphforge-project/0.1',name:'GlyphForge First Adapter',target:'glyphplay',mode:'Pull',tuning:{power:72,deadzone:12,bounce:35},assets:[asset('Player Glyph','player','P','#5be9ff'),asset('Enemy Glyph','enemy','E','#ff557b'),asset('Boundary Glyph','wall','■','#9e63ff'),asset('Goal Glyph','goal','G','#ffd05e'),asset('Shot Glyph','projectile','●','#ff9a4d')],selected:null,routes:recommendedRoutes(),capabilities:Object.fromEntries(CAPABILITIES.map(c=>[c,true])),forcedMissing:null,events:[],trace:[],receipts:[],preview:{player:{x:2,y:7,z:0},target:{x:9,y:2,z:1},shot:{x:2,y:7,z:1,vx:0,vy:0,vz:0,active:false},action:'READY',source:'NONE',eventCount:0}}}
function selected(){return state.assets.find(a=>a.id===state.selected)||state.assets[0]}
function pushUndo(label='change'){undo.push(JSON.stringify(state));if(undo.length>60)undo.shift();redo=[];trace('Undo point: '+label,'BUILD')}
function trace(message,type='ROUTE'){state.trace.unshift({at:now(),type,message});state.trace=state.trace.slice(0,100);renderTrace()}
function pulse(text){const p=$('#pulse');p.textContent=text;p.classList.remove('show');void p.offsetWidth;p.classList.add('show')}
function routeStatus(action,source){state.preview.action=action.toUpperCase();state.preview.source=source;state.preview.eventCount++;$('#actionHud').textContent=state.preview.action;$('#sourceHud').textContent=source;$('#eventHud').textContent=state.preview.eventCount;$('#topRoute').textContent=action.toUpperCase();pulse(action.toUpperCase());trace(`${source} → ${action} → visible consequence`,'ROUTE')}
function routeConflicts(){const seen={},conf=[];for(const [action,input] of Object.entries(state.routes)){if(input==='none')continue;(seen[input]??=[]).push(action)}for(const [input,actions] of Object.entries(seen))if(actions.length>1)conf.push({input,actions});return conf}
function supportedCapabilities(){const support={...state.capabilities};if(routeConflicts().length)support['action-map']=false;if(state.forcedMissing)support[state.forcedMissing]=false;return support}
function negotiation(){const t=TARGETS[state.target],support=supportedCapabilities(),missing=t.required.filter(c=>!support[c]);return{target:t.name,required:t.required,support,missing,pass:missing.length===0&&routeConflicts().length===0}}
function adapterCore(){const a=selected(),neg=negotiation(),assetDigest=hash(JSON.stringify(state.assets.map(x=>({name:x.name,kind:x.kind,glyph:x.glyph,pixels:x.pixels,version:x.version}))));const core={schema:'jm.glyphforge-adapter/0.1',project:state.name,target:{id:state.target,name:TARGETS[state.target].name,law:TARGETS[state.target].law},mode:state.mode,tuning:clone(state.tuning),assets:clone(state.assets),selectedAsset:a.id,routes:clone(state.routes),capabilities:supportedCapabilities(),negotiation:{required:neg.required,missing:neg.missing,pass:neg.pass},assetDigest,donor};core.identityHash=hash([state.target,assetDigest,Object.keys(state.routes).sort().map(k=>k+':'+state.routes[k]).join('|')].join('|'));core.adapterHash=hash(JSON.stringify(core));return core}
function compile(){const conflicts=routeConflicts(),neg=negotiation();if(conflicts.length){trace('Compile held: conflicting physical input routes.','FAULT');pulse('ROUTE FAULT');return null}if(!neg.pass){trace('Compile held: required adapter capability missing.','FAULT');pulse('CAPABILITY FAULT');return null}compiled=adapterCore();const receipt={schema:'jm.glyphforge-receipt/0.1',at:now(),action:'COMPILE_ADAPTER',target:compiled.target.name,identityHash:compiled.identityHash,adapterHash:compiled.adapterHash,assetDigest:compiled.assetDigest,routeCount:Object.keys(compiled.routes).length,negotiation:'PASS'};receipt.receiptHash=hash(JSON.stringify(receipt));state.receipts.unshift(receipt);$('#topAdapter').textContent=compiled.adapterHash;trace('Adapter compiled with visible routes and zero held conflicts.','PASS');pulse('ADAPTER PASS');renderReceipt();return compiled}
