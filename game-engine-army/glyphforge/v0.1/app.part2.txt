function setView(next){view=next;$$('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===next));if(next==='ASSET')setSurface('PIXEL');if(next==='ROUTE')setSurface('NEGOTIATE');if(next==='TEST')setSurface('PREVIEW')}
function setSurface(next){surface=next;$$('[data-surface]').forEach(b=>b.classList.toggle('active',b.dataset.surface===next));$$('.surface').forEach(x=>x.classList.toggle('active',x.id.toLowerCase().startsWith(next.toLowerCase())));if(next==='PREVIEW')requestAnimationFrame(resizePreview)}
function renderAssets(){$('#assetList').innerHTML=state.assets.map(a=>`<button class="assetCard ${a.id===selected().id?'active':''}" data-asset="${a.id}"><span class="assetIcon" style="color:${a.primary}">${a.glyph}</span><span><b>${a.name}</b><small>${a.kind} · v${a.version}</small></span><span>›</span></button>`).join('');$$('[data-asset]').forEach(b=>b.onclick=()=>{state.selected=b.dataset.asset;renderAll()})}
function renderTarget(){const t=TARGETS[state.target];$('#targetSelect').innerHTML=Object.entries(TARGETS).map(([id,x])=>`<option value="${id}" ${id===state.target?'selected':''}>${x.name}</option>`).join('');$('#targetLaw').innerHTML=`<b style="color:var(--gold)">${t.name}</b><br>${t.law}`;$('#topTarget').textContent=t.name.toUpperCase()}
function renderModes(){$('#modeGrid').innerHTML=Object.keys(MODE).map(m=>`<button data-mode="${m}" class="${m===state.mode?'active':''}">${m}</button>`).join('');$$('[data-mode]').forEach(b=>b.onclick=()=>{state.mode=b.dataset.mode;trace('Mode family changed to '+state.mode,'BUILD');renderAll()})}
const pix=$('#pixelCanvas'),pctx=pix.getContext('2d');function pixelMetrics(){const r=pix.getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);pix.width=Math.max(360,Math.floor(r.width*d));pix.height=Math.max(360,Math.floor(r.height*d));pctx.setTransform(d,0,0,d,0,0);return{w:r.width,h:r.height,cell:Math.min(r.width,r.height)/SIZE}}
function renderPixels(){const a=selected(),m=pixelMetrics();pctx.clearRect(0,0,m.w,m.h);pctx.fillStyle='#070a18';pctx.fillRect(0,0,m.w,m.h);for(let y=0;y<SIZE;y++)for(let x=0;x<SIZE;x++){const c=a.pixels[y][x];pctx.fillStyle=c?PALETTE[c]:'#0d1430';pctx.fillRect(x*m.cell,y*m.cell,m.cell,m.cell);pctx.strokeStyle='#27356f';pctx.strokeRect(x*m.cell,y*m.cell,m.cell,m.cell)}$('#assetTitle').textContent=a.name;$('#assetMeta').textContent=`${a.kind} · ${SIZE}×${SIZE} · v${a.version}`;$('#assetName').value=a.name;$('#assetKind').innerHTML=KINDS.map(k=>`<option ${k===a.kind?'selected':''}>${k}</option>`).join('');$('#assetGlyph').value=a.glyph}
function pixelPoint(ev){const r=pix.getBoundingClientRect(),m={cell:Math.min(r.width,r.height)/SIZE};return{x:Math.max(0,Math.min(SIZE-1,Math.floor((ev.clientX-r.left)/m.cell))),y:Math.max(0,Math.min(SIZE-1,Math.floor((ev.clientY-r.top)/m.cell)))}}
let drawing=false;function drawPixel(ev){if(!drawing)return;const p=pixelPoint(ev),a=selected(),value=drawMode==='erase'?0:selectedColor;if(a.pixels[p.y][p.x]!==value){a.pixels[p.y][p.x]=value;a.version++;compiled=null;renderPixels()}}
function renderPalette(){$('#palette').innerHTML=PALETTE.map((c,i)=>`<button class="swatch ${i===selectedColor?'active':''}" data-color="${i}" title="${i===0?'Erase':c}" style="background:${i===0?'repeating-conic-gradient(#fff 0 25%,#888 0 50%) 50%/12px 12px':c}"></button>`).join('');$$('[data-color]').forEach(b=>b.onclick=()=>{selectedColor=+b.dataset.color;drawMode=selectedColor===0?'erase':'paint';renderPalette()})}
function renderRoutes(){$('#routeMap').innerHTML=ACTIONS.map(action=>`<div class="routeRow"><label>${action}</label><select data-route="${action}">${INPUTS.map(input=>`<option ${input===state.routes[action]?'selected':''}>${input}</option>`).join('')}</select><button data-test-route="${action}">TEST</button></div>`).join('');$$('[data-route]').forEach(s=>s.onchange=()=>{pushUndo('route '+s.dataset.route);state.routes[s.dataset.route]=s.value;compiled=null;renderAll()});$$('[data-test-route]').forEach(b=>b.onclick=()=>dispatchAction(b.dataset.testRoute,{power:.6},'route-test'))}
function renderConflicts(){const c=routeConflicts();$('#conflicts').innerHTML=c.length?`<b style="color:var(--red)">FAULT HOLD</b><br>${c.map(x=>`${x.input} → ${x.actions.join(', ')}`).join('<br>')}`:'<b style="color:var(--green)">PASS</b><br>Every physical input has one readable action route.'}
function renderCapabilities(){const n=negotiation(),required=new Set(n.required);$('#capabilityMatrix').innerHTML=CAPABILITIES.map(cap=>{const ok=n.support[cap],cls=!ok&&required.has(cap)?'missing':ok?'supported':'';return`<div class="capCard ${cls} ${required.has(cap)?'required':''}"><b>${cap}</b><small>${required.has(cap)?'required':'optional'} · ${ok?'supported':'missing/held'}</small></div>`}).join('');$('#negotiationStatus').innerHTML=n.pass?`<b style="color:var(--green)">NEGOTIATION PASS</b> — ${n.target} requirements satisfied.`:`<b style="color:var(--red)">NEGOTIATION HELD</b> — missing: ${n.missing.join(', ')||'route conflict'}.`}
function renderReceipt(){$('#receipt').textContent=compiled?JSON.stringify({target:compiled.target.name,identity:compiled.identityHash,adapter:compiled.adapterHash,assets:compiled.assets.length,routes:Object.keys(compiled.routes).length,negotiation:compiled.negotiation.pass?'PASS':'HELD'},null,2):'Compile to produce receipt.'}
function renderTrace(){$('#trace').innerHTML=state.trace.slice(0,22).map(t=>`<div class="${t.type}"><b>${t.type}</b> ${t.message}<br><small>${t.at.slice(11,19)}</small></div>`).join('')}
function renderAll(){renderAssets();renderTarget();renderModes();renderPalette();renderPixels();renderRoutes();renderConflicts();renderCapabilities();renderReceipt();$('#power').value=state.tuning.power;$('#deadzone').value=state.tuning.deadzone;$('#bounce').value=state.tuning.bounce;$('#powerVal').textContent=state.tuning.power;$('#deadVal').textContent=state.tuning.deadzone;$('#bounceVal').textContent=state.tuning.bounce;resizePreview()}
function mutateAsset(){const a=selected();pushUndo('asset inspector');a.name=$('#assetName').value||a.name;a.kind=$('#assetKind').value;a.glyph=$('#assetGlyph').value||a.glyph;a.version++;compiled=null;renderAll()}
function newAsset(){pushUndo('new asset');const a=asset('New Glyph','marker','✦','#68ffad');state.assets.push(a);state.selected=a.id;trace('New asset body created.','BUILD');renderAll()}
function duplicateAsset(){const a=selected();pushUndo('duplicate asset');const b=clone(a);b.id=uid();b.name+=' Copy';b.version=1;state.assets.push(b);state.selected=b.id;trace('Asset duplicated without overwriting source.','BUILD');renderAll()}
function deleteAsset(){if(state.assets.length<=1)return trace('Delete held: at least one asset body must remain.','FAULT');pushUndo('delete asset');state.assets=state.assets.filter(a=>a.id!==selected().id);state.selected=state.assets[0].id;compiled=null;trace('Asset removed through explicit route.','BUILD');renderAll()}
function clearGlyph(){pushUndo('clear glyph');selected().pixels=blankPixels();selected().version++;compiled=null;renderAll()}
function mirrorGlyph(){pushUndo('mirror glyph');selected().pixels=selected().pixels.map(row=>[...row].reverse());selected().version++;compiled=null;renderAll()}
function rotateGlyph(){pushUndo('rotate glyph');const old=selected().pixels;selected().pixels=Array.from({length:SIZE},(_,y)=>Array.from({length:SIZE},(_,x)=>old[SIZE-1-x][y]));selected().version++;compiled=null;renderAll()}
