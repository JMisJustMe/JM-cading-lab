const prev=$('#preview'),ctx=prev.getContext('2d');function pm(){const r=$('#previewWrap').getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);prev.width=Math.max(520,Math.floor(r.width*d));prev.height=Math.max(420,Math.floor(r.height*d));ctx.setTransform(d,0,0,d,0,0);return{w:r.width,h:r.height,tw:62,th:31,eh:22,ox:r.width/2,oy:Math.max(75,r.height*.18)}}function ip(x,y,z=0){const m=pmCache;return{x:m.ox+(x-y)*m.tw/2,y:m.oy+(x+y)*m.th/2-z*m.eh}}let pmCache={w:600,h:500,tw:62,th:31,eh:22,ox:300,oy:90};function diamond(x,y,z,c){const m=pmCache,p=ip(x,y,z);ctx.beginPath();ctx.moveTo(p.x,p.y-m.th/2);ctx.lineTo(p.x+m.tw/2,p.y);ctx.lineTo(p.x,p.y+m.th/2);ctx.lineTo(p.x-m.tw/2,p.y);ctx.closePath();ctx.fillStyle=c;ctx.fill();ctx.strokeStyle='#344987';ctx.stroke()}
function sprite(asset,x,y,z=0,scale=2.2){const p=ip(x,y,z),cell=scale;for(let py=0;py<SIZE;py++)for(let px=0;px<SIZE;px++){const ci=asset.pixels[py][px];if(!ci)continue;ctx.fillStyle=PALETTE[ci];ctx.fillRect(p.x+(px-SIZE/2)*cell,p.y-(SIZE-py)*cell-15,cell+.4,cell+.4)}ctx.fillStyle='#03050c';ctx.font='800 10px system-ui';ctx.textAlign='center';ctx.fillText(asset.name,p.x,p.y+10)}
function resizePreview(){pmCache=pm();drawPreview()}
function drawPreview(){const m=pmCache;ctx.clearRect(0,0,m.w,m.h);const g=ctx.createLinearGradient(0,0,0,m.h);g.addColorStop(0,'#273779');g.addColorStop(1,'#040711');ctx.fillStyle=g;ctx.fillRect(0,0,m.w,m.h);for(let y=0;y<9;y++)for(let x=0;x<12;x++)diamond(x,y,0,(x+y)%2?'#151d49':'#1b255a');const player=state.assets.find(a=>a.kind==='player')||selected(),goal=state.assets.find(a=>a.kind==='goal')||selected(),enemy=state.assets.find(a=>a.kind==='enemy'),wall=state.assets.find(a=>a.kind==='wall');sprite(player,state.preview.player.x,state.preview.player.y,1);sprite(goal,state.preview.target.x,state.preview.target.y,1);if(enemy)sprite(enemy,7,5,1);if(wall){sprite(wall,5,3,1);sprite(wall,5,4,1)}const s=state.preview.shot;if(s.active){const shot=state.assets.find(a=>a.kind==='projectile')||selected();sprite(shot,s.x,s.y,s.z,1.5)}if(aim){const p=ip(state.preview.player.x,state.preview.player.y,1),r=prev.getBoundingClientRect();ctx.beginPath();ctx.moveTo(p.x,p.y-25);ctx.lineTo(aim.x-r.left,aim.y-r.top);ctx.strokeStyle='#ffd05e';ctx.lineWidth=3;ctx.stroke();ctx.setLineDash([6,6]);ctx.strokeStyle='#5be9ff';ctx.stroke();ctx.setLineDash([])}}
function dispatchPhysical(input,payload={}){const matches=Object.entries(state.routes).filter(([,physical])=>physical===input);if(matches.length!==1){trace(`PermissionGate rejected ${input}: ${matches.length?'conflicting routes':'unmapped input'}.`,'FAULT');pulse('ROUTE REJECT');return false}return dispatchAction(matches[0][0],payload,input)}
function dispatchAction(action,payload={},source='test-control'){if(!ACTIONS.includes(action)){trace('PermissionGate rejected unsupported action: '+action,'FAULT');pulse('ACTION REJECT');return false}routeStatus(action,source);const p=state.preview.player;if(action==='move-up')p.y=Math.max(0,p.y-1);if(action==='move-down')p.y=Math.min(8,p.y+1);if(action==='move-left')p.x=Math.max(0,p.x-1);if(action==='move-right')p.x=Math.min(11,p.x+1);if(action==='confirm'){pulse('CONFIRM');trace('Confirm reached target adapter action.','PASS')}if(action==='cancel'){state.preview.shot.active=false;aim=null;trace('Cancel explicitly cleared active aim/shot.','PASS')}if(['fire','fling'].includes(action))launchShot(payload,action);drawPreview()}
function launchShot(payload={},action='fire'){const mod=MODE[state.mode],p=state.preview.player,s=state.preview.shot,force=(payload.power??state.tuning.power/100)*mod.power;s.x=p.x;s.y=p.y;s.z=1;s.vx=(payload.dx??1)*force*.16;s.vy=(payload.dy??-1)*force*.16;s.vz=mod.arc*.09+.02;s.spin=mod.spin;s.active=true;$('#shotHud').textContent=Math.round(force*100)+'%';cancelAnimationFrame(raf);tickShot()}
function tickShot(){const s=state.preview.shot;if(!s.active)return;s.x+=s.vx;s.y+=s.vy;s.z+=s.vz;s.vz-=.006;if(s.z<0){s.z=0;s.vz=Math.abs(s.vz)*state.tuning.bounce/100;s.vx*=.82;s.vy*=.82}if(s.x<0||s.y<0||s.x>11||s.y>8||Math.hypot(s.x-state.preview.target.x,s.y-state.preview.target.y)<.7){const hit=Math.hypot(s.x-state.preview.target.x,s.y-state.preview.target.y)<.7;s.active=false;trace(hit?'Shot route contacted target.':'Shot route exited field.',hit?'PASS':'FAULT');pulse(hit?'HIT':'MISS');drawPreview();return}drawPreview();raf=requestAnimationFrame(tickShot)}
function previewPos(ev){const r=prev.getBoundingClientRect();return{x:ev.clientX,y:ev.clientY,localX:ev.clientX-r.left,localY:ev.clientY-r.top}}
function beginAim(ev){aim=previewPos(ev);dispatchPhysical('pointer:drag');drawPreview();prev.setPointerCapture?.(ev.pointerId)}function moveAim(ev){if(!aim)return;aim=previewPos(ev);drawPreview()}function endAim(ev){if(!aim)return;const end=previewPos(ev),r=prev.getBoundingClientRect(),start=ip(state.preview.player.x,state.preview.player.y,1),dx=(start.x-end.localX),dy=(start.y-end.localY),len=Math.hypot(dx,dy);if(len<state.tuning.deadzone){trace('Deadzone rejected accidental weak release.','FAULT');pulse('TOO SOFT');aim=null;drawPreview();return}dispatchPhysical('pointer:release',{dx:dx/len,dy:dy/len,power:Math.min(1,len/220)});aim=null}
function resetPreview(){state.preview={player:{x:2,y:7,z:0},target:{x:9,y:2,z:1},shot:{x:2,y:7,z:1,vx:0,vy:0,vz:0,active:false},action:'READY',source:'NONE',eventCount:0};cancelAnimationFrame(raf);aim=null;trace('RecoveryBody restored preview baseline.','PASS');renderAll()}
function autoNegotiate(){pushUndo('auto negotiate');state.forcedMissing=null;for(const cap of TARGETS[state.target].required)state.capabilities[cap]=true;repairRoutes(false);trace('Required capabilities and recommended routes negotiated.','PASS');compiled=null;renderAll()}
function simulateFault(){const req=TARGETS[state.target].required;state.forcedMissing=req[0];compiled=null;trace('FaultHold simulated missing capability: '+state.forcedMissing,'FAULT');renderAll()}
function recoverFault(){state.forcedMissing=null;trace('RecoveryBody restored capability contract.','PASS');renderAll()}
function repairRoutes(log=true){pushUndo('repair routes');state.routes=recommendedRoutes();compiled=null;if(log)trace('Recommended routes restored; no silent duplicate inputs.','PASS');renderAll()}
function save(){localStorage.setItem('glyphforge-sovereign-v01',JSON.stringify(state));trace('GlyphForge project saved locally.','PASS')}function load(){const raw=localStorage.getItem('glyphforge-sovereign-v01');if(!raw)return trace('No local GlyphForge save found.','FAULT');state=JSON.parse(raw);compiled=null;trace('GlyphForge project restored.','PASS');renderAll()}
function download(name,text,type='application/json'){const a=document.createElement('a'),u=URL.createObjectURL(new Blob([text],{type}));a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1200)}
function exportAdapter(){const c=compiled||compile();if(!c)return;download((state.name.replace(/\W+/g,'_')||'GlyphForge')+'.jmglyphadapter.json',JSON.stringify(c,null,2));trace('Adapter exported with assets, routes and capability receipt.','PASS')}
