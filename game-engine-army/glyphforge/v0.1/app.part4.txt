$$('[data-view]').forEach(b=>b.onclick=()=>setView(b.dataset.view));$$('[data-surface]').forEach(b=>b.onclick=()=>setSurface(b.dataset.surface));$('#compileBtn').onclick=compile;$('#undoBtn').onclick=undoNow;$('#redoBtn').onclick=redoNow;$('#saveBtn').onclick=save;$('#loadBtn').onclick=load;$('#importBtn').onclick=()=>$('#importFile').click();$('#importFile').onchange=e=>e.target.files[0]&&importProject(e.target.files[0]);$('#exportBtn').onclick=exportAdapter;$('#demoBtn').onclick=exportDemo;$('#resetBtn').onclick=()=>confirm('Reset GlyphForge?')&&reset();$('#newAssetBtn').onclick=newAsset;$('#duplicateAsset').onclick=duplicateAsset;$('#deleteAsset').onclick=deleteAsset;$('#clearGlyph').onclick=clearGlyph;$('#mirrorGlyph').onclick=mirrorGlyph;$('#rotateGlyph').onclick=rotateGlyph;['assetName','assetKind','assetGlyph'].forEach(id=>$('#'+id).onchange=mutateAsset);$('#targetSelect').onchange=e=>{pushUndo('target');state.target=e.target.value;state.forcedMissing=null;compiled=null;trace('Adapter target changed to '+TARGETS[state.target].name,'BUILD');renderAll()};['power','deadzone','bounce'].forEach(id=>$('#'+id).oninput=e=>{state.tuning[id]=+e.target.value;compiled=null;renderAll()});$('#repairRoutes').onclick=()=>repairRoutes(true);$('#autoNegotiate').onclick=autoNegotiate;$('#simulateFault').onclick=simulateFault;$('#recoverFault').onclick=recoverFault;$('#resetPreview').onclick=resetPreview;$$('[data-action]').forEach(b=>b.onclick=()=>dispatchAction(b.dataset.action,{},'button:'+b.dataset.action));
pix.addEventListener('pointerdown',e=>{pushUndo('pixel stroke');drawing=true;pix.setPointerCapture?.(e.pointerId);drawPixel(e)});pix.addEventListener('pointermove',drawPixel);pix.addEventListener('pointerup',()=>{drawing=false;compiled=null;renderAll()});pix.addEventListener('pointercancel',()=>drawing=false);prev.addEventListener('pointerdown',beginAim);prev.addEventListener('pointermove',moveAim);prev.addEventListener('pointerup',endAim);prev.addEventListener('pointercancel',()=>{aim=null;drawPreview()});window.addEventListener('keydown',e=>{if(['INPUT','SELECT'].includes(document.activeElement?.tagName))return;const map={ArrowUp:'move-up',ArrowDown:'move-down',ArrowLeft:'move-left',ArrowRight:'move-right',w:'move-up',s:'move-down',a:'move-left',d:'move-right',' ':'confirm',Escape:'cancel'};if(map[e.key]){e.preventDefault();dispatchPhysical('keyboard:'+e.key)}});window.addEventListener('resize',()=>{renderPixels();resizePreview()});window.GlyphForgeApp={compile,adapter:()=>compiled,state:()=>state,dispatchAction,dispatchPhysical,negotiation,routeConflicts};reset();
})();
