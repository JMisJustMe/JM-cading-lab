# GlyphPlay Sovereign Rebuild v0.1 — First Living Floor

This is the first independent rebuild in **JM Game Engine Army — Sovereign Rebuild, Polish & Coding-Body Trials**.

## Run

Serve this directory through HTTP and open `index.html`. The repository form loads the ordered runtime chunks and keeps the source reviewable.

The CI artifact also produces `OPEN_FIRST_GLYPHPLAY_SOVEREIGN_REBUILD_v0_1.html`, a dependency-free single-file edition that opens directly on phone or laptop.

## Working surface

- dependency-free 2.5D isometric editor and playtest field;
- responsive phone and laptop layouts;
- scene hierarchy and entity inspector;
- tile-height editing, rotation, zoom, undo and redo;
- player movement, collision, damage, enemies, walls, coins, hazards and goals;
- recoverable Edit → Test → Edit route;
- local save/load and JSON import/export;
- self-contained playable HTML export;
- FaultHold, RecoveryBody, TraceBox route and receipts.

## Coding-body trials

The selected coding body changes the actual stage law and win route rather than merely recolouring the interface:

- **GlyphPlay:** creator → stage → movement → playtest;
- **Cading:** Mark → Signal → RouteGate → StateCore;
- **Quadze:** four distinct quarters → relation → centre crown;
- **RECORP:** scatter → recover → regroup → lock;
- **FlowTalk:** contextual word sequence → changed route;
- **FormeULA:** formula terms become visible height and movement;
- **ContactCode:** contact sequence becomes explicit code input.

## Restored donor lineage

The source manifest records exact hashes for:

- GlyphPlay v27.8 current mounted candidate;
- GlyphPlay v27.6 stable fallback;
- GlyphPlay v9.4 Android creator lineage;
- GlyphPlay v3.5 RC polish donor;
- GameForge v3.13 current anchor;
- GlyphForge v67 current anchor;
- PLAYFORM exact-source implementation.

No donor is silently renamed as this rebuild. Shared organs raise GlyphPlay while the earlier bodies retain their own provenance.

## Claim boundary

Status: **FIRST_LIVING_FLOOR_BUILD_NOT_FINAL_ENGINE_CROWN**.

The repository can prove source assembly, JavaScript syntax, required feature contracts, body-trial distinction and deterministic packaging. Final crown still requires direct phone/laptop play contact, sustained editing use, deeper native GlyphPlay grammar/IR and user-confirmed product polish.
