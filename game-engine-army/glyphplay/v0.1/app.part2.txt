ding'){P.splice(1);addEntity('Mark',2,6);addEntity('Signal',4,5);addEntity('RouteGate',7,3);addEntity('StateCore',10,1);project.scene.rules=['touch Mark','carry signal','pass RouteGate','enter StateCore'];project.scene.objective={kind:'chain',needed:4,current:0,open:true};}
 if(id==='quadze'){P.splice(1);[[1,1],[10,1],[1,7],[10,7]].forEach((p,i)=>addEntity('QuadCore',p[0],p[1],0,{quarter:i+1}));addEntity('Goal',6,4,0,{locked:true});project.scene.rules=['contact all four quarters','preserve four identities','unlock centre crown'];project.scene.objective={kind:'quadze',needed:4,current:0,open:false};}
 if(id==='recorp'){P.splice(1);[[2,2],[9,2],[2,6],[9,6]].forEach((p,i)=>addEntity('Fragment',p[0],p[1],0,{fragment:i+1}));addEntity('RecoveryCore',6,4);project.scene.rules=['recover scattered fragments','return them to the core','regroup and lock'];project.scene.objective={kind:'recorp',needed:4,current:0,open:false};}
 if(id==='flowtalk'){P.splice(1);[['I',2,7],['CONTACT',4,5],['YOU',7,3],['WE',9,2]].forEach((p,i)=>addEntity('Word',p[1],p[2],0,{word:p[0],order:i}));addEntity('Goal',10,1,0,{locked:true});project.scene.rules=['touch words in context order','context changes response','open shared route'];project.scene.objective={kind:'sequence',needed:4,current:0,open:false};}
 if(id==='formeula'){P.splice(1);[[2,7,1],[4,6,2],[6,4,3],[8,3,2]].forEach((p,i)=>{addEntity('Formula',p[0],p[1],p[2],{term:i+1});const t=tileAt(p[0],p[1]);if(t)t.height=p[2]});addEntity('Goal',10,1);project.scene.rules=['formula terms form visible height','climb the equation','reach output'];project.scene.objective={kind:'formula',needed:4,current:0,open:false};}
 if(id==='contactcode'){P.splice(1);[[2,6],[4,4],[7,5],[9,2]].forEach((p,i)=>addEntity('Contact',p[0],p[1],0,{pressure:i+1}));addEntity('Goal',10,1,0,{locked:true});project.scene.rules=['contact every node','pointer and body contact both count','unlock goal'];project.scene.objective={kind:'contact',needed:4,current:0,open:false};}
 project.runtime={hp:5,score:0,state:'ACTIVE',inventory:[],flags:{sequence:0}};selectedId=null;trace('Body lens applied: '+BODY_PROFILES[id].name,'ROUTE');receipt('APPLY_BODY',{body:id,generated:fromGenerate});renderAll()}
function generate(){const text=$('#prompt').value.trim().toLowerCase();let lens=project.bodyLens;if(text.includes('quad'))lens='quadze';else if(text.includes('recorp')||text.includes('recover')||text.includes('scatter'))lens='recorp';else if(text.includes('cading')||text.includes('mark')||text.includes('signal'))lens='cading';else if(text.includes('flowtalk')||text.includes('dialogue')||text.includes('word'))lens='flowtalk';else if(text.includes('formula')||text.includes('formeula'))lens='formeula';else if(text.includes('contact')||text.includes('gesture')||text.includes('touch'))lens='contactcode';applyBodyLens(lens,true);if(text.includes('hard')){addEntity('Enemy',9,7);addEntity('Hazard',7,6)}if(text.includes('coin')){addEntity('Coin',3,3);addEntity('Coin',8,5)}project.name='Generated '+BODY_PROFILES[lens].name+' Playform';trace('Intent generated into a playable body.','ROUTE');receipt('GENERATE',{prompt:text,body:lens});renderAll()}
function remix(){pushUndo();for(const e of project.scene.entities){if(e.type!=='Player'){e.x=Math.max(0,Math.min(project.scene.w-1,e.x+(Math.random()<.5?-1:1)));e.y=Math.max(0,Math.min(project.scene.h-1,e.y+(Math.random()<.5?-1:1)))}}trace('World remixed without changing body identity.','ROUTE');receipt('REMIX');renderAll()}
function setMode(next){if(next===mode)return;if(next==='TEST'){testBackup={scene:clone(project.scene),runtime:clone(project.runtime)};mode='TEST';project.runtime={hp:5,score:0,state:'ACTIVE',inventory:[],flags:{sequence:0}};hideBanner();trace('Playtest started from a recoverable design snapshot.','ROUTE')}else{mode='EDIT';if(testBackup){project.scene=clone(testBackup.scene);project.runtime=clone(testBackup.runtime);testBackup=null;trace('Editor restored the design after playtest.','RECOVERY')}else trace('Returned to editor.','RECOVERY')}$('#statusMode').textContent=mode;$$('[data-mode]').forEach(b=>b.classList.toggle('active',b.dataset.mode===mode));renderAll()}
function setTool(t){tool=t;$('#toolHud').textContent=t;$$('#toolGrid button').forEach(b=>b.classList.toggle('active',b.dataset.tool===t))}
function resize(){const box=$('#canvasWrap').getBoundingClientRect(),dpr=Math.min(2,devicePixelRatio||1);canvas.width=Math.max(520,Math.floor(box.width*dpr));canvas.height=Math.max(420,Math.floor(box.height*dpr));ctx.setTransform(dpr,0,0,dpr,0,0);renderWorld()}
function rotated(x,y){const w=project.scene.w-1,h=project.scene.h-1;if(rotation===1)return{x:h-y,y:x};if(rotation===2)return{x:w-x,y:h-y};if(rotation===3)return{x:y,y:w-x};return{x,y}}
function unrotated(x,y){const w=project.scene.w-1,h=project.scene.h-1;if(rotation===1)return{x:y,y:h-x};if(rotation===2)return{x:w-x,y:h-y};if(rotation===3)return{x:w-y,y:x};return{x,y}}
function metrics(){const cw=canvas.clientWidth,ch=canvas.clientHeight,tw=58*zoom,th=29*zoom,eh=22*zoom;return{cw,ch,tw,th,eh,ox:cw/2,oy:Math.max(70,ch*.16)}}
function projectPoint(x,y,z=0){const r=rotated(x,y),m=metrics();return{x:m.ox+(r.x-r.y)*m.tw/2,y:m.oy+(r.x+r.y)*m.th/2-z*m.eh}}
function screenToGrid(sx,sy){const m=metrics(),dx=sx-m.ox,dy=sy-m.oy;const rx=(dy/(m.th/2)+dx/(m.tw/2))/2,ry=(dy/(m.th/2)-dx/(m.tw/2))/2;const p=unrotated(Math.round(rx),Math.round(ry));return{x:Math.max(0,Math.min(project.scene.w-1,p.x)),y:Math.max(0,Math.min(project.scene.h-1,p.y))}}
function diamond(x,y,z,color,stroke='#31536e'){const m=metrics(),p=projectPoint(x,y,z);ctx.beginPath();ctx.moveTo(p.x,p.y-m.th/2);ctx.lineTo(p.x+m.tw/2,p.y);ctx.lineTo(p.x,p.y+m.th/2);ctx.lineTo(p.x-m.tw/2,p.y);ctx.closePath();ctx.fillStyle=color;ctx.fill();ctx.strokeStyle=stroke;ctx.lineWidth=1;ctx.stroke()}
function block(x,y,z,h,color){const m=metrics(),base=projectPoint(x,y,z),top=projectPoint(x,y,z+h);ctx.fillStyle=color;ctx.strokeStyle='rgba(255,255,255,.16)';ctx.lineWidth=1;const left=[[top.x,top.y-m.th/2],[top.x-m.tw/2,top.y],[base.x-m.tw/2,base.y],[base.x,base.y-m.th/2]],right=[[top.x,top.y-m.th/2],[top.x+m.tw/2,top.y],[base.x+m.tw/2,base.y],[base.x,base.y-m.th/2]];for(const poly of [left,right]){ctx.beginPath();ctx.moveTo(...poly[0]);poly.slice(1).forEach(q=>ctx.lineTo(...q));ctx.closePath();ctx.fill();ctx.stroke()}diamond(x,y,z+h,color)}
function renderWorld(){const w=canvas.clientWidth,h=canvas.clientHeight;ctx.clearRect(0,0,w,h);const bg=ctx.createLinearGradient(0,0,0,h);bg.addColorStop(0,'#153247');bg.addColorStop(1,'#061019');ctx.fillStyle=bg;ctx.fillRect(0,0,w,h);
 const tiles=[...project.scene.tiles].sort((a,b)=>{const A=rotated(a.x,a.y),B=rotated(b.x,b.y);return(A.x+A.y)-(B.x+B.y)});for(const t of tiles)diamond(t.x,t.y,t.height||0,(t.x+t.y)%2?'#18364a':'