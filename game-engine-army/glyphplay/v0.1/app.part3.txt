#1c4056');
 const ents=[...project.scene.entities].sort((a,b)=>{const A=rotated(a.x,a.y),B=rotated(b.x,b.y);return(A.x+A.y+a.z)-(B.x+B.y+b.z)});for(const e of ents){const d=TYPES[e.type]||TYPES.Wall,p=projectPoint(e.x,e.y,e.z||0),sel=e.id===selectedId;block(e.x,e.y,e.z||0,d.height||1,d.color);ctx.fillStyle='#041018';ctx.font=`900 ${Math.max(11,16*zoom)}px system-ui`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(e.props.word||d.glyph,p.x,p.y-(d.height||1)*metrics().eh);if(sel){ctx.strokeStyle='#fff';ctx.lineWidth=2;ctx.beginPath();ctx.arc(p.x,p.y-(d.height||1)*metrics().eh,18*zoom,0,Math.PI*2);ctx.stroke()}}
}

function canvasPointer(ev){const rect=canvas.getBoundingClientRect(),p=screenToGrid(ev.clientX-rect.left,ev.clientY-rect.top);if(mode==='TEST'){const pl=project.scene.entities.find(e=>e.type==='Player');if(pl){const dx=Math.sign(p.x-pl.x),dy=Math.sign(p.y-pl.y);movePlayer(Math.abs(p.x-pl.x)>Math.abs(p.y-pl.y)?dx:0,Math.abs(p.x-pl.x)>Math.abs(p.y-pl.y)?0:dy)}return}pushUndo();const top=[...entitiesAt(p.x,p.y)].pop();if(tool==='SELECT'){selectedId=top?.id||null}else if(tool==='FLOOR'){const t=tileAt(p.x,p.y);t.height=(t.height+1)%4}else{if(top&&tool!=='Player')project.scene.entities=project.scene.entities.filter(e=>e.id!==top.id);selectedId=addEntity(tool,p.x,p.y,tileAt(p.x,p.y)?.height||0).id}trace(`Edit contact ${tool} @ ${p.x},${p.y}`,'ROUTE');receipt('EDIT',{tool,x:p.x,y:p.y});renderAll()}
function solidAt(x,y){return project.scene.entities.some(e=>e.x===x&&e.y===y&&(TYPES[e.type]?.solid))}
function movePlayer(dx,dy){if(mode!=='TEST'||project.runtime.state!=='ACTIVE')return;const p=project.scene.entities.find(e=>e.type==='Player');if(!p)return;const nx=p.x+dx,ny=p.y+dy;if(nx<0||ny<0||nx>=project.scene.w||ny>=project.scene.h||solidAt(nx,ny)){trace('PermissionGate rejected blocked movement.','FAULT');receipt('MOVE_REJECT',{nx,ny});return}p.x=nx;p.y=ny;p.z=tileAt(nx,ny)?.height||0;resolveCell(p);renderAll()}
function resolveCell(player){for(const e of [...entitiesAt(player.x,player.y)]){if(e.id===player.id)continue;if(e.type==='Coin'){project.runtime.score++;project.scene.entities=project.scene.entities.filter(x=>x.id!==e.id);trace('Coin contact changed score.','ROUTE')}else if(e.type==='Hazard'||e.type==='Enemy'){project.runtime.hp--;trace('Damage held and recorded.','FAULT');if(project.runtime.hp<=0){project.runtime.state='FAIL';showBanner('FAULT HOLD','RecoveryBody can retry the scene.')}}else if(e.type==='Goal'){if(project.scene.objective.open)ding('Goal reached.');else trace('Goal remains locked by body law.','FAULT')}else bodyContact(e)}updateObjective()}
function bodyContact(e){const kind=project.scene.objective.kind,inv=project.runtime.inventory;if(kind==='chain'&&['Mark','Signal','RouteGate','StateCore'].includes(e.type)&&!inv.includes(e.type)){inv.push(e.type);trace(e.type+' entered the Cading route.','ROUTE');if(e.type==='StateCore'&&inv.length>=4)ding('Mark → Signal → Route → State completed.')}if(kind==='quadze'&&e.type==='QuadCore'&&!inv.includes(e.id)){inv.push(e.id);trace('Quarter '+e.props.quarter+' contacted without collapse.','ROUTE');if(inv.length>=4){project.scene.objective.open=true;trace('Four quarters related; centre crown opened.','RECOVERY')}}if(kind==='recorp'&&e.type==='Fragment'&&!inv.includes(e.id)){inv.push(e.id);project.scene.entities=project.scene.entities.filter(x=>x.id!==e.id);trace('Fragment recovered.','RECOVERY')}if(kind==='recorp'&&e.type==='RecoveryCore'){if(inv.length>=4)ding('Scattered body regrouped and locked.');else trace('Recovery Core waiting for all fragments.','FAULT')}if(kind==='sequence'&&e.type==='Word'){const expect=project.runtime.flags.sequence||0;if(e.props.order===expect){project.runtime.flags.sequence=expect+1;trace('Context accepted: '+e.props.word,'ROUTE');if(expect+1>=4){project.scene.objective.open=true;trace('Shared route opened through context.','RECOVERY')}}else trace('Context order changed the meaning; route held.','FAULT')}if(kind==='formula'&&e.type==='Formula'&&!inv.includes(e.id)){inv.push(e.id);trace('Formula term embodied as height '+e.z+'.','ROUTE');if(inv.length>=4)project.scene.objective.open=true}if(kind==='contact'&&e.type==='Contact'&&!inv.includes(e.id)){inv.push(e.id);trace('Contact '+e.props.pressure+' registered as code.','ROUTE');if(inv.length>=4){project.scene.objective.open=true;trace('Contact sequence unlocked goal.','RECOVERY')}}}
function updateObjective(){const o=project.scene.objective,inv=project.runtime.inventory;o.current=Math.min(o.needed,inv.length);$('#hpHud').textContent=project.runtime.hp;$('#scoreHud').textContent=project.runtime.score;$('#progressHud').textContent=`${o.current}/${o.needed}`;renderReceipt()}
function ding(message){project.runtime.state='WIN';trace(message,'DING');receipt('DING',{message});showBanner('GLYPHPLAY DING',message)}
function showBanner(title,sub){$('#bannerText').innerHTML=`${title}<small>${sub}</small>`;$('#banner').classList.add('show')}function hideBanner(){$('#banner').classList.remove('show')}
function retry(){if(testBackup)project.scene=clone(testBackup.scene);project.runtime={hp:5,score:0,state:'ACTIVE',inventory:[],flags:{sequence:0}};hideBanner();trace('RecoveryBody restored the playtest baseline.','RECOVERY');receipt('RECOVERY');renderAll()}
function renderTools(){$('#toolGrid').innerHTML=TOOLS.map(t=>`<button data-tool="${t}" class="${tool===t?'active':''}">${TYPES[t]?.glyph||'◇'} ${t}</button>`).join('');$$('#toolGrid button').forEach(b=>b.onclick=()=>setTool(b.dataset.tool))}
function renderHierarchy(){const list=$('#entityList');list.innerHTML=project.scene.entities.map(e=>`<div class="entityItem ${selectedId===e.id?'active':''}"><button data-select="${e.id}"><b>${e.name}</b><br><small style="color:var(--muted)">${e.type} · ${e.x},${e.y},${e.z}</small></button><span>${TYPES[e.type]?.glyph||'?'}</span></div>`).join('');$$('[data-select]').forEach(b=>b.onclick=()=>{selectedId=b.dataset.select;renderAll()})}
function renderInspector(){const e=entity(selectedId);$('#inspectorEmpty').hidden=!!e;$('#inspector').hidden=!e;if(!e)return;$('#entName').value=e.name;$('#entType').innerHTML=Object.keys(TYPES).filter(x=>!['SELECT','FLOOR'].includes(x)).map(t=>`<option ${t===e.type?'selected':''}>${t}</option>`).join('');$('#entX').value=e.x;$('#entY').value=e.y;$('#entZ').value=e.z;$('#entHp').value=e.hp}
function renderTrace(){$('#trace').innerHTML=project.trace.slice(0,24).map(t=>`<div class="${t.type}"><b>${t.type}</b> ${t.message}<br><small>${t.at.slice(11,19)}</small></div>`).join('')}
function renderReceipt(){const r=lastReceipt||project.receipts[0];$('#receiptView').innerHTML=r?`<b>${r.action}</b><br>Body: ${r.body}<br>Scene: ${r.sceneHash}<br>Receipt: <code>${r.receiptHash}</code>`:'No receipt yet.'}
function renderBody