(){const p=BODY_PROFILES[project.bodyLens];$('#statusBody').textContent=p.name;$('#bodyLens').value=project.bodyLens;$('#bodyDescription').innerHTML=`<b style="color:var(--cyan)">${p.name}</b><br>${p.description}`;$('#routeLine').textContent=p.route;$('#projectName').textContent=project.name}
function renderAll(){renderTools();renderBody();renderHierarchy();renderInspector();renderTrace();renderReceipt();updateObjective();renderWorld()}
function mutateSelected(){const e=entity(selectedId);if(!e)return;pushUndo();e.name=$('#entName').value||e.type;e.type=$('#entType').value;e.x=Math.max(0,Math.min(project.scene.w-1,+$('#entX').value||0));e.y=Math.max(0,Math.min(project.scene.h-1,+$('#entY').value||0));e.z=Math.max(0,Math.min(4,+$('#entZ').value||0));e.hp=Math.max(0,+$('#entHp').value||0);trace('Inspector changed '+e.name+'.','ROUTE');receipt('INSPECTOR_EDIT',{id:e.id});renderAll()}
function save(){localStorage.setItem('glyphplay-sovereign-v01',JSON.stringify(project));trace('Saved to local body storage.','ROUTE');receipt('SAVE');renderAll()}
function load(){const raw=localStorage.getItem('glyphplay-sovereign-v01');if(!raw){trace('No local save found.','FAULT');return}project=JSON.parse(raw);lastReceipt=project.receipts?.[0]||null;selectedId=null;trace('Local body restored.','RECOVERY');receipt('LOAD');renderAll()}
function download(name,text,type='application/json'){const a=document.createElement('a'),u=URL.createObjectURL(new Blob([text],{type}));a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1200)}
function exportProject(){project.updatedAt=now();receipt('EXPORT_PROJECT');download((project.name.replace(/\W+/g,'_')||'GlyphPlay')+'.json',JSON.stringify(project,null,2))}
function exportPlayable(){receipt('EXPORT_PLAYABLE');const boot=JSON.stringify(project).replace(/</g,'\\u003c');let doc='<!doctype html>\n'+document.documentElement.outerHTML;doc=doc.replace('</body>',`<script>setTimeout(()=>window.GlyphPlayApp.loadExternal(${boot}),50)<\/script></body>`);download((project.name.replace(/\W+/g,'_')||'GlyphPlay')+'_PLAYABLE.html',doc,'text/html')}
function importProject(file){const fr=new FileReader();fr.onload=()=>{try{const p=JSON.parse(fr.result);loadExternal(p);trace('Project imported.','RECOVERY');receipt('IMPORT')}catch(e){trace('Import failed: '+e.message,'FAULT')}};fr.readAsText(file)}
function loadExternal(p){if(!p?.scene?.entities)throw new Error('Not a GlyphPlay project');project=clone(p);lastReceipt=project.receipts?.[0]||null;selectedId=null;renderAll()}
function undo(){if(!undoStack.length)return;redoStack.push(JSON.stringify(project));project=JSON.parse(undoStack.pop());selectedId=null;trace('Undo restored prior body state.','RECOVERY');renderAll()}
function redo(){if(!redoStack.length)return;undoStack.push(JSON.stringify(project));project=JSON.parse(redoStack.pop());selectedId=null;trace('Redo re-applied body state.','ROUTE');renderAll()}
function key(ev){if(['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName))return;const map={ArrowUp:[0,-1],w:[0,-1],ArrowDown:[0,1],s:[0,1],ArrowLeft:[-1,0],a:[-1,0],ArrowRight:[1,0],d:[1,0]};if(map[ev.key]){ev.preventDefault();movePlayer(...map[ev.key])}if(ev.key===' '){ev.preventDefault();action()}}
function action(){if(mode==='TEST'){const p=project.scene.entities.find(e=>e.type==='Player');if(p)resolveCell(p)}else if(selectedId){duplicateSelected()}}
function duplicateSelected(){const e=entity(selectedId);if(!e)return;pushUndo();const n=addEntity(e.type,Math.min(project.scene.w-1,e.x+1),e.y,e.z,{...e.props,hp:e.hp});n.name=e.name+' Copy';selectedId=n.id;trace('Entity duplicated.','ROUTE');receipt('DUPLICATE',{from:e.id,to:n.id});renderAll()}
function deleteSelected(){if(!selectedId)return;pushUndo();project.scene.entities=project.scene.entities.filter(e=>e.id!==selectedId);trace('Entity removed through explicit edit.','ROUTE');receipt('DELETE',{id:selectedId});selectedId=null;renderAll()}
function init(){project=baseProject();seedNative();if(window.__GLYPHPLAY_BOOT_PROJECT__)project=clone(window.__GLYPHPLAY_BOOT_PROJECT__);receipt('BOOT',{lineage:LINEAGE.map(x=>x.sha256)});renderAll();resize();$('#app').dataset.ready='true'}
$$('[data-mode]').forEach(b=>b.onclick=()=>setMode(b.dataset.mode));$$('[data-step]').forEach(b=>b.onclick=()=>movePlayer(...b.dataset.step.split(',').map(Number)));$('#applyLens').onclick=()=>applyBodyLens($('#bodyLens').value);$('#generateBtn').onclick=generate;$('#remixBtn').onclick=remix;$('#rotLeft').onclick=()=>{rotation=(rotation+3)%4;renderWorld()};$('#rotRight').onclick=()=>{rotation=(rotation+1)%4;renderWorld()};$('#zoomIn').onclick=()=>{zoom=Math.min(1.5,zoom+.1);renderWorld()};$('#zoomOut').onclick=()=>{zoom=Math.max(.65,zoom-.1);renderWorld()};$('#actionBtn').onclick=action;$('#mobileAction').onclick=action;$('#retryBtn').onclick=retry;$('#saveBtn').onclick=save;$('#loadBtn').onclick=load;$('#exportBtn').onclick=exportProject;$('#exportPlayableBtn').onclick=exportPlayable;$('#importBtn').onclick=()=>$('#importFile').click();$('#importFile').onchange=e=>e.target.files[0]&&importProject(e.target.files[0]);$('#undoBtn').onclick=undo;$('#redoBtn').onclick=redo;$('#resetBtn').onclick=()=>confirm('Reset this GlyphPlay project?')&&resetProject();$('#renameBtn').onclick=()=>{const n=prompt('Project name',project.name);if(n){project.name=n;receipt('RENAME',{name:n});renderAll()}};$('#lineageBtn').onclick=()=>{$('#lineageText').textContent=JSON.stringify({governingLaw:'Restore before rewrite. Shared organs do not erase engine identity.',donors:LINEAGE},null,2);$('#modal').classList.add('open')};$('#closeModal').onclick=()=>$('#modal').classList.remove('open');$('#modal').onclick=e=>{if(e.target===$('#modal'))$('#modal').classList.remove('open')};canvas.addEventListener('pointerdown',canvasPointer);window.addEventListener('keydown',key);window.addEventListener('resize',resize);['entName','entType','entX','entY','entZ','entHp'].forEach(id=>$('#'+id).onchange=mutateSelected);$('#duplicateBtn').onclick=duplicateSelected;$('#deleteBtn').onclick=deleteSelected;
window.GlyphPlayApp={loadExternal,project:()=>project,applyBodyLens,generate,receipt};init();
})();
