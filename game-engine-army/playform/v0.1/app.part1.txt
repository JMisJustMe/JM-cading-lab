(()=>{'use strict';
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const FIELDS=['actor','tool','gesture','space','mode','objective','loop','expression'];
const BODY={playform:{name:'PLAYFORM Native',route:'actor → tool → gesture → space → objective → loop',law:'An expression becomes a Playform only through an observable repeatable loop.'},cading:{name:'Cading',route:'mark → signal → route → state → trace',law:'Each loop step becomes a readable state transition.'},quadze:{name:'Quadze',route:'four distinct functions → relation → crown',law:'The loop must preserve distinct quarters while completing through relation.'},recorp:{name:'RECORP',route:'scatter → recognise → recover → regroup → repeat',law:'The loop proves itself through recovery and regrouping.'},flowtalk:{name:'FlowTalk',route:'context → contact → response → changed meaning → repeat',law:'The loop changes meaning through contextual contact.'},formeula:{name:'FormeULA',route:'formula → visible form → movement → output → repeat',law:'The loop must form a visible structure and movement consequence.'}};
const donor={file:'PLAYFORM_SISTER_SYSTEM_v0_1_EXACT_SOURCE_IMPLEMENTATION.html',bytes:20222,sha256:'6a09d5b196e0c5c9ca344f9c113be83803df87646fbb37caf1c4f5fb11d500c0'};
const TEMPLATES={tboy:{actor:'T‑Boy',tool:'ball / card / gadget',gesture:'choose, aim, pass',space:'arena',mode:'sport / battle',objective:'score and adapt',loop:'choose → act → respond → adapt → repeat',expression:'tactical and stylish'},lyric:{actor:'performer',tool:'voice and lyric cards',gesture:'choose, speak, layer',space:'stage',mode:'performance',objective:'complete a living verse',loop:'choose line → perform → hear response → reshape → repeat',expression:'rhythmic and expressive'},tool:{actor:'creator',tool:'editable app',gesture:'select, change, test',space:'workspace',mode:'build trial',objective:'produce a working tool',loop:'select → edit → test → inspect → repeat',expression:'precise and recoverable'},blank:{actor:'',tool:'',gesture:'',space:'',mode:'',objective:'',loop:'',expression:''}};
let state,compiled=null,mode='EDIT',testBackup=null,autoTimer=null;
const clone=v=>JSON.parse(JSON.stringify(v)),now=()=>new Date().toISOString();
function hash(text){let h=2166136261;for(const c of String(text)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return(h>>>0).toString(16).padStart(8,'0')}
function fresh(){return{schema:'jm.playform-project/0.1',name:'PLAYFORM First Living Loop',values:clone(TEMPLATES.tboy),coding:'playform',trace:[],receipts:[],runtime:{actor:{x:1,y:7},step:0,cycles:0,state:'READY',visited:[]},scene:null}}
function splitLoop(text){return text.split(/(?:→|->|>|\n|;|,)/).map(x=>x.trim()).filter(Boolean)}
function read(){return Object.fromEntries(FIELDS.map(id=>[id,$('#'+id).value.trim()]))}
function validate(values=read()){const steps=splitLoop(values.loop),core=['actor','tool','space','objective'].filter(k=>values[k]),returning=/repeat|again|return|loop|adapt/i.test(values.loop),observable=steps.length>=3,valid=core.length===4&&observable&&returning;return{steps,core,returning,observable,valid,contact:FIELDS.filter(k=>values[k])}}
function trace(message,type='ROUTE'){state.trace.unshift({at:now(),type,message});state.trace=state.trace.slice(0,100);renderTrace()}
function receipt(action,detail={}){const r={schema:'jm.playform-receipt/0.1',at:now(),action,formHash:compiled?.formHash||null,cycles:state.runtime.cycles,detail};r.receiptHash=hash(JSON.stringify(r));state.receipts.unshift(r);state.receipts=state.receipts.slice(0,40);$('#receiptHud').textContent=r.receiptHash;renderReceipt();return r}
function positions(steps,coding){const n=steps.length;return steps.map((_,i)=>{if(coding==='quadze'){const q=[[2,2],[9,2],[9,7],[2,7]];return{x:q[i%4][0],y:q[i%4][1],z:i%2}}if(coding==='recorp'){const q=[[1,1],[10,1],[10,7],[1,7],[6,4]];return{x:q[i%q.length][0],y:q[i%q.length][1],z:i===n-1?2:0}}if(coding==='flowtalk')return{x:2+i*Math.max(1,Math.floor(8/Math.max(1,n-1))),y:i%2?3:6,z:0};if(coding==='formeula')return{x:2+i*Math.max(1,Math.floor(8/Math.max(1,n-1))),y:7-Math.min(5,i),z:(i%3)+1};return{x:2+i*Math.max(1,Math.floor(8/Math.max(1,n-1))),y:6-(i%3),z:i%2}})}
function compile(record=true){state.values=read();const v=validate(state.values);if(!v.valid){compiled=null;trace('Compile held: core components and a returning three-step loop are required.','FAULT');renderAll();return null}const pos=positions(v.steps,state.coding),nodes=v.steps.map((label,i)=>({id:'step-'+i,label,index:i,...pos[i]}));const core={schema:'jm.playform-compiled/0.1',project:state.name,values:clone(state.values),coding:{id:state.coding,...BODY[state.coding]},steps:v.steps,nodes,objective:{cyclesRequired:2},donor};core.formHash=hash(JSON.stringify(core));compiled=core;state.scene=clone(core);state.runtime={actor:{x:nodes[0].x,y:nodes[0].y},step:0,cycles:0,state:'READY',visited:[]};if(record){trace('Playform compiled into an observable route.','PASS');receipt('COMPILE',{steps:v.steps.length,coding:state.coding})}renderAll();return core}
