function renderValidation(){const v=validate();$('#validation').innerHTML=`<b style="color:${v.valid?'var(--green)':'var(--rose)'}">${v.valid?'PLAYFORM CANDIDATE PASSES SOURCE GATE':'CANDIDATE / HOLD'}</b><br>Core ${v.core.length}/4 · Steps ${v.steps.length} · Observable ${v.observable?'yes':'no'} · Returning ${v.returning?'yes':'no'}<br>Contact ${v.contact.length}/8.`;$('#formBadge').textContent=v.valid?'VALID':'CANDIDATE';$('#loopBadge').textContent=v.returning?'RETURNING':'OPEN'}
function renderSteps(){const steps=compiled?.steps||splitLoop(read().loop);$('#steps').innerHTML=steps.map((s,i)=>`<div class="step ${state.runtime.visited.includes(i)?'done':''} ${i===state.runtime.step?'current':''}"><b>${i+1}. ${s}</b><br><small>${state.runtime.visited.includes(i)?'contact proven':i===state.runtime.step?'next required':'waiting'}</small></div>`).join('')||'<div class="copy">No loop steps compiled.</div>';$('#stepHud').textContent=compiled?`${state.runtime.step}/${compiled.steps.length}`:'0/0';$('#cyclesHud').textContent=`${state.runtime.cycles}/2`;$('#cycleBadge').textContent=`${state.runtime.cycles}/2`;$('#nextHud').textContent=compiled?.steps[state.runtime.step]?.toUpperCase()||'COMPILE'}
function renderTrace(){$('#trace').innerHTML=state.trace.slice(0,22).map(t=>`<div class="${t.type}"><b>${t.type}</b> ${t.message}<br><small>${t.at.slice(11,19)}</small></div>`).join('')}
function renderReceipt(){const r=state.receipts[0];$('#receipt').textContent=r?JSON.stringify(r,null,2):'Compile to produce receipt.'}
function renderBody(){const b=BODY[state.coding];$('#routeText').textContent=b.route;$('#stageTitle').textContent=(state.values.actor||'Candidate')+' · '+b.name}
function syncForm(){FIELDS.forEach(k=>$('#'+k).value=state.values[k]||'');$('#codingBody').value=state.coding}
const canvas=$('#world'),ctx=canvas.getContext('2d');let metric={w:600,h:500,tw:60,th:30,eh:22,ox:300,oy:85};function resize(){const r=$('#stage').getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);canvas.width=Math.max(520,r.width*d);canvas.height=Math.max(420,r.height*d);ctx.setTransform(d,0,0,d,0,0);metric={w:r.width,h:r.height,tw:60,th:30,eh:22,ox:r.width/2,oy:Math.max(76,r.height*.16)};draw()}
function pt(x,y,z=0){return{x:metric.ox+(x-y)*metric.tw/2,y:metric.oy+(x+y)*metric.th/2-z*metric.eh}}
function diamond(x,y,z,c){const p=pt(x,y,z);ctx.beginPath();ctx.moveTo(p.x,p.y-metric.th/2);ctx.lineTo(p.x+metric.tw/2,p.y);ctx.lineTo(p.x,p.y+metric.th/2);ctx.lineTo(p.x-metric.tw/2,p.y);ctx.closePath();ctx.fillStyle=c;ctx.fill();ctx.strokeStyle='#34556c';ctx.stroke()}
function draw(){ctx.clearRect(0,0,metric.w,metric.h);const g=ctx.createLinearGradient(0,0,0,metric.h);g.addColorStop(0,'#1d4963');g.addColorStop(1,'#040a10');ctx.fillStyle=g;ctx.fillRect(0,0,metric.w,metric.h);for(let y=0;y<9;y++)for(let x=0;x<12;x++)diamond(x,y,0,(x+y)%2?'#18374a':'#1e4055');if(!compiled)return;compiled.nodes.forEach(n=>{diamond(n.x,n.y,n.z,'#ffd56a');const p=pt(n.x,n.y,n.z+1);ctx.fillStyle='#041018';ctx.font='900 13px system-ui';ctx.textAlign='center';ctx.fillText(String(n.index+1),p.x,p.y);ctx.fillStyle='#f7fbff';ctx.font='700 10px system-ui';ctx.fillText(n.label.slice(0,18),p.x,p.y+17)});const a=pt(state.runtime.actor.x,state.runtime.actor.y,1);ctx.fillStyle='#70ebff';ctx.beginPath();ctx.arc(a.x,a.y-12,13,0,Math.PI*2);ctx.fill();ctx.fillStyle='#041018';ctx.font='900 12px system-ui';ctx.fillText((state.values.actor||'A').slice(0,1).toUpperCase(),a.x,a.y-8)}
function nearestNode(x,y){if(!compiled)return null;return compiled.nodes.reduce((best,n)=>!best||Math.hypot(n.x-x,n.y-y)<Math.hypot(best.x-x,best.y-y)?n:best,null)}
function move(dx,dy){if(mode!=='TEST'||!compiled)return;state.runtime.actor.x=Math.max(0,Math.min(11,state.runtime.actor.x+dx));state.runtime.actor.y=Math.max(0,Math.min(8,state.runtime.actor.y+dy));trace(`Actor moved through ${state.values.gesture||'action'}.`,'ROUTE');draw()}
function act(){if(mode!=='TEST'||!compiled)return;const expected=compiled.nodes[state.runtime.step],here=nearestNode(state.runtime.actor.x,state.runtime.actor.y);if(!here||here.index!==expected.index||Math.hypot(here.x-state.runtime.actor.x,here.y-state.runtime.actor.y)>.8){trace('FaultHold: action contacted the wrong loop step.','FAULT');show('LOOP HELD','Move to the next required step or use RecoveryBody.');return}state.runtime.visited.push(expected.index);trace(`Step ${expected.index+1} contacted: ${expected.label}`,'PASS');state.runtime.step++;if(state.runtime.step>=compiled.steps.length){state.runtime.cycles++;receipt('CYCLE_COMPLETE',{cycle:state.runtime.cycles});if(state.runtime.cycles>=2){state.runtime.state='DING';trace('Observable repeatable loop proven twice.','PASS');show('PLAYFORM DING','The form survived a repeated playable cycle.')}else{state.runtime.step=0;state.runtime.visited=[];state.runtime.actor={x:compiled.nodes[0].x,y:compiled.nodes[0].y};show('FIRST CYCLE PROVEN','Repeat the loop once more to prove playability.')}}renderAll()}
function show(t,s){$('#bannerText').innerHTML=`${t}<small>${s}</small>`;$('#banner').classList.add('show')}function hide(){$('#banner').classList.remove('show')}
function recover(){if(!compiled)return;state.runtime={actor:{x:compiled.nodes[0].x,y:compiled.nodes[0].y},step:0,cycles:0,state:'READY',visited:[]};hide();trace('RecoveryBody restored the compiled loop baseline.','PASS');receipt('RECOVERY');renderAll()}
function setMode(next){mode=next;$('#editBtn').classList.toggle('active',next==='EDIT');$('#testBtn').classList.toggle('active',next==='TEST');if(next==='TEST'){if(!compiled&&!compile())return;testBackup=clone(state.runtime);trace('Playtest entered. A repeatable loop must complete twice.','BUILD')}else{hide();trace('Returned to editable Playform body.','PASS')}renderAll()}
