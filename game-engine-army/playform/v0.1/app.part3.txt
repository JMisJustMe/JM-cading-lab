function autoCycle(){if(mode!=='TEST'||!compiled)return;clearInterval(autoTimer);let i=state.runtime.step;autoTimer=setInterval(()=>{if(state.runtime.state==='DING'||i>=compiled.nodes.length){clearInterval(autoTimer);return}const n=compiled.nodes[i];state.runtime.actor={x:n.x,y:n.y};draw();act();i=state.runtime.step;if(state.runtime.step===0)clearInterval(autoTimer)},450)}
function renderAll(){renderValidation();renderSteps();renderTrace();renderReceipt();renderBody();draw()}
function template(id){state.values=clone(TEMPLATES[id]);compiled=null;syncForm();trace('Template contacted: '+id+'.','BUILD');renderAll()}
function save(){state.values=read();localStorage.setItem('playform-sovereign-v01',JSON.stringify(state));trace('PLAYFORM project saved locally.','PASS')}
function load(){const raw=localStorage.getItem('playform-sovereign-v01');if(!raw)return trace('No local PLAYFORM save found.','FAULT');state=JSON.parse(raw);compiled=null;syncForm();trace('PLAYFORM project restored.','PASS');renderAll()}
function download(name,text,type='application/json'){const a=document.createElement('a'),u=URL.createObjectURL(new Blob([text],{type}));a.href=u;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(u),1000)}
function exportForm(){const c=compiled||compile();if(!c)return;download((state.name.replace(/\W+/g,'_')||'PLAYFORM')+'.jmplayform.json',JSON.stringify({project:state,compiled:c},null,2));trace('Compiled Playform exported.','PASS')}
function playableDocument(c){const data=JSON.stringify(c).replace(/</g,'\\u003c');return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><title>${c.project}</title><style>*{box-sizing:border-box}html,body{margin:0;height:100%;background:#06101a;color:#fff;font-family:system-ui}main{height:100%;display:grid;grid-template-rows:auto 1fr auto;padding:8px;gap:7px}header,footer{display:flex;gap:6px;justify-content:center;align-items:center;flex-wrap:wrap}canvas{width:100%;height:100%;border:1px solid #34556c;border-radius:18px}button{min-width:48px;min-height:42px;border:1px solid #34556c;border-radius:10px;background:#13283b;color:#fff}b{color:#ffd56a}</style></head><body><main><header><b>${c.values.actor}</b><span>${c.steps.join(' → ')}</span><span id="p">0/2</span></header><canvas id="c"></canvas><footer><button id="act">ACT NEXT STEP</button><button id="reset">RESET</button></footer></main><script>const P=${data},cv=document.querySelector('#c'),x=cv.getContext('2d');let step=0,cycles=0;function draw(){const r=cv.getBoundingClientRect(),d=Math.min(2,devicePixelRatio||1);cv.width=Math.max(520,r.width*d);cv.height=Math.max(420,r.height*d);x.setTransform(d,0,0,d,0,0);x.fillStyle='#081724';x.fillRect(0,0,r.width,r.height);const cx=r.width/2,cy=r.height/2,R=Math.min(r.width,r.height)*.32;P.steps.forEach((s,i)=>{const a=-Math.PI/2+i*Math.PI*2/P.steps.length,px=cx+Math.cos(a)*R,py=cy+Math.sin(a)*R;x.fillStyle=i===step?'#ffd56a':'#24506a';x.beginPath();x.arc(px,py,25,0,Math.PI*2);x.fill();x.fillStyle='#fff';x.textAlign='center';x.font='700 11px system-ui';x.fillText((i+1)+'. '+s.slice(0,12),px,py+4)})}function act(){step++;if(step>=P.steps.length){step=0;cycles++;document.querySelector('#p').textContent=cycles+'/2';if(cycles>=2)setTimeout(()=>alert('PLAYFORM DING — repeatable loop proven'),20)}draw()}document.querySelector('#act').onclick=act;document.querySelector('#reset').onclick=()=>{step=cycles=0;document.querySelector('#p').textContent='0/2';draw()};addEventListener('resize',draw);draw();<\/script></body></html>`}
function exportPlayable(){const c=compiled||compile();if(!c)return;download((state.name.replace(/\W+/g,'_')||'PLAYFORM')+'_PLAYABLE.html',playableDocument(c),'text/html');trace('Self-contained repeatable loop exported.','PASS')}
function importProject(file){const r=new FileReader();r.onload=()=>{try{const d=JSON.parse(r.result);state=d.project||d;compiled=d.compiled||null;syncForm();trace('PLAYFORM import restored.','PASS');renderAll()}catch(e){trace('Import held: '+e.message,'FAULT')}};r.readAsText(file)}
FIELDS.forEach(id=>$('#'+id).oninput=()=>{state.values=read();compiled=null;renderAll()});$('#codingBody').onchange=e=>{state.coding=e.target.value;compiled=null;trace('Coding-body interpretation changed to '+BODY[state.coding].name+'.','BUILD');renderAll()};$$('[data-template]').forEach(b=>b.onclick=()=>template(b.dataset.template));$('#validateBtn').onclick=()=>{const v=validate();trace(v.valid?'Source validation gate passed.':'Source validation held. ',v.valid?'PASS':'FAULT');receipt('VALIDATE',{valid:v.valid})};$('#compileBtn').onclick=()=>compile();$('#editBtn').onclick=()=>setMode('EDIT');$('#testBtn').onclick=()=>setMode('TEST');$('#recoverBtn').onclick=recover;$('#actBtn').onclick=act;$('#mobileAct').onclick=act;$('#autoBtn').onclick=autoCycle;$$('[data-move]').forEach(b=>b.onclick=()=>move(...b.dataset.move.split(',').map(Number)));$('#saveBtn').onclick=save;$('#loadBtn').onclick=load;$('#importBtn').onclick=()=>$('#importFile').click();$('#importFile').onchange=e=>e.target.files[0]&&importProject(e.target.files[0]);$('#exportBtn').onclick=exportForm;$('#playableBtn').onclick=exportPlayable;$('#resetBtn').onclick=()=>confirm('Reset PLAYFORM?')&&reset();canvas.addEventListener('pointerdown',e=>{if(mode!=='TEST'||!compiled)return;const r=canvas.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top,n=compiled.nodes.reduce((best,node)=>{const p=pt(node.x,node.y,node.z+1),d=Math.hypot(p.x-x,p.y-y);return !best||d<best.d?{node,d}:best},null);if(n&&n.d<45){state.runtime.actor={x:n.node.x,y:n.node.y};draw()}});window.addEventListener('keydown',e=>{if(['INPUT','TEXTAREA','SELECT'].includes(document.activeElement?.tagName))return;const m={ArrowUp:[0,-1],w:[0,-1],ArrowDown:[0,1],s:[0,1],ArrowLeft:[-1,0],a:[-1,0],ArrowRight:[1,0],d:[1,0]};if(m[e.key]){e.preventDefault();move(...m[e.key])}if(e.key===' '){e.preventDefault();act()}});window.addEventListener('resize',resize);window.PLAYFORMApp={compile,validate,state:()=>state,compiled:()=>compiled,act,recover};function reset(){state=fresh();compiled=null;mode='EDIT';syncForm();trace('PLAYFORM first living loop floor opened.','PASS');renderAll();resize();$('#app').dataset.ready='true'}reset();
})();
