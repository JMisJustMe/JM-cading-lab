# JM Game Engine Army — Five Max Pushes v0.2

This is the source-visible CI lane replacing the clipped v0.1 package-text carrier. The failed reconstruction remains in the trace; no semantic gate was lowered.

Five distinct first floors:

1. **JM GameCore** — identity-preserving BodyRegistry, ServiceBus, InputService, StateVault, TraceService, RecoveryService and HostBridge.
2. **JM GAME NATIVE CORE** — deterministic fixed-step runtime, profile-bound verbs, collision, fault/recovery and explicit C++17/Rust 2024/Wasm32 ABI contracts.
3. **Kading Game Estate Engine** — Kading source → AST → Game IR → interpreter → receipt.
4. **JM Game Engine Console** — selects, launches and services engines without owning or silently mutating their internals.
5. **Army Federation / Coding-Body Trials** — seven identity-preserving handoff stages plus Cading, Quadze, RECORP, FlowTalk and FormeULA difference routes.

## Claim boundary

These are functional browser/JavaScript first floors with deterministic receipts and executable semantic gates. Native C++/Rust/Wasm binaries, self-hosting, direct phone/laptop proof, sustained real-project use and final crowns remain open.
