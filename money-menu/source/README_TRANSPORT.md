# Money Menu source transport

`index.part.001.fix` is the exact base64 transport for the 7,058-byte second carrier chunk. `index.part.001.decode.py` deterministically restores `index.part.001` before the complete carrier SHA-256 gate runs.
