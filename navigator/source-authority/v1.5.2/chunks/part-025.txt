chanism may vary by source body. Word Estate post-crown delta","source":"JM WORD ESTATE — Post-Crown Deep Search Expansion v1.1 2026-09-07 · POST-CROWN RECOVERED v0.1 recovered baseline"},{"n":"161","pow":"Everyone’s entitled to (or has) an opinion, not all opinions are onions hiding pi. opinion → onion → pi","type":"Split / Sound / Saying","prov":"DIRECT USER SOURCE","context":"Opinion/onion/pi layered sound and hidden-structure play. Empiramid / opinion governance","source":"Saved memory / Empiramid governance · SOURCE-LINE RECOVERED v0.1 recovered baseline"},{"n":"162","pow":"Gossip with front lead (e)nergy = … Egos sip gossip → (e)gos sip / egos sip","type":"Split / Sound / Saying","prov":"DIRECT USER SOURCE","context":"Turns 'gossip' toward 'egos sip' while keeping the ego/gossip relation active. Social comment / observation","source":"Current conversation screenshot/comment 2026-09-25 · CURRENT CHAT EXACT v0.1 recovered baseline"},{"n":"163","pow":"agreed → a greed agreed → a greed","type":"Split Word","prov":"MEMORY-RECOVERED USER SOURCE","context":"Resegmentation changes consensus into desire/greed. Prior wordplay recovery","source":"Prior conversation memory · MEMORY DIRECT / LIBRARY SOURCE NOT YET LOCATED v0.1 recovered baseline"},{"n":"164","pow":"nowhere → now here NOWHERE → NOW HERE","type":"Split Word","prov":"DIRECT USER SOURCE","context":"A space/gap changes the reading while preserving the letters. Gap logic","source":"JM gap-logic source · SOURCE-LINE RECOVERED v0.1 recovered baseline"},{"n":"165","pow":"showman-ship showmanship ↔ showman + ship","type":"Split Word / Lyric Bar","prov":"DIRECT USER SOURCE","context":"Opens ship/wavy/coastal/flowing/oceans chain. Wisdom's Loading","source":"Wisdom's Loading recovered source body 2026-05-17 · SOURCE VERBATIM v0.1 recovered baseline"},{"n":"166","pow":"CraftikBridge file-recovery / reconstruction bridge","type":"System Wordplay","prov":"ESTATE SOURCE-BACKED","context":"FLL-family bridge that understands selected file/folder material and rebuilds it into recoverable structure while TraceBox proves the route. Word Estate / ECOSTATE","source":"FLL CRAFTIKBridge v1.0 Final checkpoint / Native File Grabber Contract starter. 2026-09-07 · SOURCE_RESOLVED_FUNCTION v0.1 recovered baseline"},{"n":"167","pow":"DotSpace native-logic / mark-field concept","type":"System Wordplay","prov":"ESTATE SOURCE-BACKED","context":"Underlying coding/logic view in which space is field and dot is Ding/mark; a dot changing a space becomes the minimal readable state/contact event. Word Estate / ECOSTATE","source":"Cold Ding / Routeware / DotSpace Part 10 comparison. 2026-09-07 · SOURCE_RESOLVED_FUNCTION v0.1 recovered baseline"},{"n":"168","pow":"FileRoom bounded FTR sorter/storage face","type":"System Wordplay","prov":"ESTATE SOURCE-BACKED","context":"Source-backed name face inside FTR Sorter / FileRoom, related to TraceBox, RouteOS, Estate OS and Zionfolder in the Radius relation layer. Word Estate / ECOSTATE","source":"Radius Lexicon FTR Sorter / FileRoom relations. 2026-09-07 · SOURCE_COMPONENT_AND_RELATIONS_CONFIRMED v0.1 recovered baseline"},{"n":"169","pow":"Geo-Computer bounded title-component / hypothesis face","type":"System Wordplay","prov":"ESTATE SOURCE-BACKED","context":"Source-backed component of Geo-Computer / MaterialRoute Thinking, a Contact & Transformation hypothesis/framework. Word Estate / ECOSTATE","source":"JM-THE-038 Theory access card. 2026-09-07 · SOURCE_COMPONENT_CONFIRMED_PRIMARY_WORDING_OPEN v0.1 recovered baseline"},{"n":"170","pow":"Jigma alias face","type":"System Wordplay","prov":"ESTATE SOURCE-BACKED","context":"Source-backed alias/name face of the canonical Jigma / Jigmore entry. Word Estate / ECOSTATE","source":"Radius Lexicon relation layer. 2026-09-07 · RADIUS_ALIAS_RESOLVED v0.1 recovered baseline"},{"n":"171","pow":"Jigmore alias face","type":"System Wordplay","prov":"ESTATE SOURCE-BACKED","context":"Source-backed alias/name face of the canonical Jigma / Jigmore entry. Word Estate / ECOSTATE","source":"Radius Lexicon relation layer. 2026-09-07 · RADIUS_ALIAS_RESOLVED v0.1 recovered baseline"},{"n":"172","pow":"June’s May Post-crown deep-search recovery","type":"Word / Time Play","prov":"ESTATE SOURCE-BACKED","context":"Recovered language item; exact inner mechanism may vary by source body. Word Estate post-crown delta","source":"JM WORD ESTATE — Post-Crown Deep Search Expansion v1.1 2026-09-07 · POST-CROWN RECOVERED v0.1 recovered baseline"},{"n":"173","pow":"Com-mit Crime — com-mit crime","type":"Split Word / Lyric POW","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A source split that makes “commit” visibly carry “com” and “mit,” drawing attention to how a familiar instruction can be re-heard when broken apart.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 11 · GTKM-SRC-006 | GTKM-GTKY - GROUNDZEROJM.txt · line 15 · GTKM-SRC-008 | GTKM-GTKY - GROUNDZEROJM.txt · line 19 · GTKM-SRC-010 | +3 duplicate/source carriers · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"174","pow":"Play My-self — play my-self","type":"Split Word / Lyric POW","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A hyphenated split that turns “myself” into both identity and an action directed toward the self.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 11 · GTKM-SRC-006 | GTKM-GTKY - GROUNDZEROJM.txt · line 15 · GTKM-SRC-008 | GTKM-GTKY - GROUNDZEROJM.txt · line 19 · GTKM-SRC-010 | +3 duplicate/source carriers · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"175","pow":"Snake Them-selves — snake them-selves","type":"Split Word / Lyric POW","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A split that makes betrayal or self-betrayal readable inside “themselves.”","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 11 · GTKM-SRC-006 | GTKM-GTKY - GROUNDZEROJM.txt · line 15 · GTKM-SRC-008 | GTKM-GTKY - GROUNDZEROJM.txt · line 19 · GTKM-SRC-010 | +3 duplicate/source carriers · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"176","pow":"Paper Chase — paper chase","type":"Phrase / Meaning Family","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A recurring phrase connecting money pursuit, paperwork, social pressure and movement through systems.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 11 · GTKM-SRC-006 | GTKM-GTKY - GROUNDZEROJM.txt · line 15 · GTKM-SRC-008 | GTKM-GTKY - GROUNDZEROJM.txt · line 19 · GTKM-SRC-010 | +3 duplicate/source carriers · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"177","pow":"Old Paper / Owed Paper — old paper now owed paper","type":"Sound / Phonetic Bridge","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A sound-and-spelling relation connecting inherited money systems with present debt.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 13 · GTKM-SRC-007 | GTKM-GTKY - GROUNDZEROJM.txt · line 190 · GTKM-SRC-095 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"178","pow":"Mouthful of Loudness — mouthful of loudness","type":"Phrase / Image","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A compressed image where volume, speech and physical mouth-space become one verbal object.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 17 · GTKM-SRC-009 | GTKM-GTKY - GROUNDZEROJM.txt · line 194 · GTKM-SRC-097 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"179","pow":"Faith Affects / Effects Faith Has — faith affects","type":"Sound / Phonetic Pivot","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A grammatical and sonic loop using affect/effect to turn faith into both cause and consequence.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 3 · GTKM-SRC-002 | GTKM-GTKY - GROUNDZEROJM.txt · line 180 · GTKM-SRC-090 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"180","pow":"Hope Less / Smoke Less — hope less","type":"Split / Rhyme","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A split and rhyme relation where hopelessness, reduced hope and reducing smoking are held in one sound route.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 5 · GTKM-SRC-003 | GTKM-GTKY - GROUNDZEROJM.txt · line 182 · GTKM-SRC-091 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"181","pow":"Slow / Progress — Slow/progress","type":"Split / Slash Reading","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A visible split that lets slow progress remain progress while making the pace part of the meaning.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 5 · GTKM-SRC-003 | GTKM-GTKY - GROUNDZEROJM.txt · line 182 · GTKM-SRC-091 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"182","pow":"In Emergency Break Glass — in emergency break glass","type":"Repurposed Saying / Metaphor","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A familiar instruction repurposed as a pressure-and-release metaphor inside a personal letter.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 3 · GTKM-SRC-002 | GTKM-GTKY - GROUNDZEROJM.txt · line 180 · GTKM-SRC-090 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"183","pow":"Booked In / Booking — booking when you're booked in","type":"Extended Wordplay / Metaphor","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A sustained word family that maps prison entry and sentence-time through hotel reservation language.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 25 · GTKM-SRC-013 | GTKM-GTKY - GROUNDZEROJM.txt · line 202 · GTKM-SRC-101 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"184","pow":"Mis-guidance / Missed Guidance — mis-guidance","type":"Sound / Phonetic Bridge","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A sound/spelling pair connecting bad direction with support or teaching that did not arrive.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 37 · GTKM-SRC-019 | GTKM-GTKY - GROUNDZEROJM.txt · line 214 · GTKM-SRC-107 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"185","pow":"Dreamed Speech — dreamed speech","type":"Compressed Image / Phrase","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"Language compressed into a phrase where need, aspiration and expression become spoken dream-form.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 37 · GTKM-SRC-019 | GTKM-GTKY - GROUNDZEROJM.txt · line 214 · GTKM-SRC-107 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"186","pow":"Moving Pictured Images / Hieroglyphics — moving pictured images like hieroglyphics","type":"Pictographic / Language Bridge","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A phrase linking lived moving images, words and pictographic reading.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 37 · GTKM-SRC-019 | GTKM-GTKY - GROUNDZEROJM.txt · line 214 · GTKM-SRC-107 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"187","pow":"Just Me / JM — more than Just Me","type":"Name / Identity POW","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A name-and-initial route that can function as personal identity, project mark and wordplay without every use becoming identical.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 31 · GTKM-SRC-016 | GTKM-GTKY - GROUNDZEROJM.txt · line 208 · GTKM-SRC-104 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"188","pow":"Love Is Money (But It Actually Isn't) — love is money","type":"Concept / Self-Correction","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A deliberately self-correcting concept title that proposes an analogy and immediately protects the distinction.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 72 · IKC-SRC-028 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"189","pow":"Mind Set / Mindset / Set Minds Free — mind set in the mindset","type":"Word Order / Recursion","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A repeated word-order construction that turns mindset from a noun into an active route for setting minds free.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 70 · IKC-SRC-027 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"190","pow":"Wealth of Self-Worth — wealth of self-worth","type":"Semantic Transfer","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A phrase connecting wealth language to internal value while resisting the idea that worth is only monetary.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 76 · IKC-SRC-030 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"191","pow":"Redshift the Blueprint — redshift the blueprint","type":"Image Transfer","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A science-and-design image used for changing an inherited or expected plan through movement, perspective or transformation.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 14 · IKC-SRC-007 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"192","pow":"Stationary Keeps Me Moving — stationary keeps me moving","type":"Paradox / Saying","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A contradiction-shaped phrase in which writing materials or stillness become the means of movement.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 14 · IKC-SRC-007 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"193","pow":"Inner Knowing — Inner knowing","type":"Source Phrase","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A source phrase for knowledge felt or carried internally before full external wording, proof or explanation.","source":"I'm knowing I can't but thinking I - GROUNDZEROJM.txt · line 23 · IKN-SRC-010 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"194","pow":"Search / Seek / Sought / Sort — seek the sought and sort the search","type":"Verb Chain / Sound","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A verb-family chain connecting inquiry, intended finding, past finding and organisation.","source":"I'm knowing I can't but thinking I - GROUNDZEROJM.txt · line 32 · IKN-SRC-013 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"195","pow":"Men You Wait / Word Games — Men you wait or play word games","type":"Sound / Re-segmentation Candidate","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A sound-led fragment where “men you wait” opens toward “manipulate” or a warning about waiting while language games operate.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 17 · FSP-SRC-009 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"196","pow":"The Ripples Will Rip You — ripples will rip you","type":"Sound / Consequence","prov":"ESTATE SOURCE-GROUNDED CANDIDATE","context":"A compact sound relation turning ripple into rip, carrying consequence from small disturbance toward personal effect.","source":"I'm knowing I can't but thinking I - GROUNDZEROJM.txt · lines 1–3 · IKN-SRC-001 · SOURCE_GROUNDED_CANDIDATE deep lyric reconciliation"},{"n":"197","pow":"Berry-d(buried)","type":"Name / Respelling / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 804 · GTKM-SRC-386 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"198","pow":"Cay-ces(cases)","type":"Spelling / Sound / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 604 · GTKM-SRC-295 | LYRICS THROUGH THE YEARS - GROUNDZEROJM.txt · line 18 · LTY-SRC-007 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"199","pow":"I (Miami)","type":"Name / Re-segmentation / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 378 · GTKM-SRC-187 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"200","pow":"Klaus (clause)","type":"Name / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 782 · GTKM-SRC-375 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"201","pow":"am (Gemma-am)","type":"Name / Re-segmentation / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 804 · GTKM-SRC-386 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"202","pow":"an (brazilian)","type":"Name / Re-segmentation / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 515 · FSP-SRC-227 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"203","pow":"boos (booze)","type":"Sound / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 394 · GTKM-SRC-193 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"204","pow":"da (tudor)","type":"Name / Re-segmentation / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 804 · GTKM-SRC-386 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"205","pow":"death (deaf)","type":"Sound / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 778 · GTKM-SRC-373 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"206","pow":"does (Judas)","type":"Name / Re-segmentation / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"I'm knowing I can't but thinking I - GROUNDZEROJM.txt · line 289 · IKN-SRC-105 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"207","pow":"eight (ate)","type":"Number / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 774 · GTKM-SRC-371 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"208","pow":"free dum","type":"Split Word / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 321 · FSP-SRC-149 | LYRICS THROUGH THE YEARS - GROUNDZEROJM.txt · line 22 · LTY-SRC-009 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"209","pow":"her (hair)","type":"Sound / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 564 · GTKM-SRC-276 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"210","pow":"in (writin')","type":"Split / Embedded Word / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 800 · GTKM-SRC-384 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"211","pow":"mall (animal)","type":"Sound / Embedded Word / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 222 · FSP-SRC-103 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"212","pow":"pic(k)","type":"Spelling / Embedded Letter / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"CALMER KARMA - - GROUNDZEROJM.txt · lines 311–344 · CK-007 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"213","pow":"pur-pose","type":"Split Word / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"LYRICS THROUGH THE YEARS - GROUNDZEROJM.txt · line 50 · LTY-SRC-021 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"214","pow":"re'listically","type":"Respelling / Lyric Language","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"LYRICS THROUGH THE YEARS - GROUNDZEROJM.txt · line 50 · LTY-SRC-021 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"215","pow":"sell/cells","type":"Sound / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 173 · GTKM-SRC-087 | GTKM-GTKY - GROUNDZEROJM.txt · line 350 · GTKM-SRC-175 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"216","pow":"shipped (worshipped)","type":"Split / Embedded Word / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 790 · GTKM-SRC-379 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"217","pow":"wrap (rap)","type":"Sound / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 778 · GTKM-SRC-373 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"218","pow":"you (Ryu)","type":"Name / Homophone / Lyric Bar","prov":"DIRECT SOURCE — PASS 9 EXPLICIT WORDWORK","context":"Source text carries an explicit alternate spelling, segmentation, parenthetical reading, or sound-form. The source form is preserved; any broader interpretation remains bounded.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 782 · GTKM-SRC-375 · PASS 9 Language Depth explicit_wordwork deep lyric reconciliation"},{"n":"219","pow":"movers (move us)","type":"Sound / Re-segmentation / Lyric Line","prov":"DIRECT SOURCE — INLINE PARENTHETICAL POW","context":"The source explicitly parenthesises “move us” inside “movers,” turning movement/delivery into a spoken split.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 620 · GTKM-SRC-303 | I keep changing my mind on what to - GROUNDZEROJM.txt · line 183 · IKC-SRC-057 deep lyric reconciliation"},{"n":"220","pow":"bye son (M.Bison)","type":"Name / Homophone / Lyric Bar","prov":"DIRECT SOURCE — INLINE PARENTHETICAL POW","context":"Street Fighter name-play: “bye son” is explicitly marked as M. Bison in the source.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 782 · GTKM-SRC-375 deep lyric reconciliation"},{"n":"221","pow":"two two (22), 2 by 2","type":"Number / Sound / Lyric Bar","prov":"DIRECT SOURCE — INLINE PARENTHETICAL POW","context":"The source explicitly links spoken “two two” to 22 and then to “2 by 2.”","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 800 · GTKM-SRC-384 deep lyric reconciliation"},{"n":"222","pow":"detention (de tension)","type":"Split Word / Lyric Line","prov":"DIRECT SOURCE — INLINE PARENTHETICAL POW","context":"The source explicitly splits detention into “de tension,” making release of tension part of the line’s operation.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 1 · IKC-SRC-001 deep lyric reconciliation"},{"n":"223","pow":"teacher (teach a)","type":"Sound / Re-segmentation / Lyric Line","prov":"DIRECT SOURCE — INLINE PARENTHETICAL POW","context":"The source explicitly marks “teach a” inside teacher.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 1 · IKC-SRC-001 deep lyric reconciliation"},{"n":"224","pow":"fairer piece of peace","type":"Sound / Homophone / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"piece/peace sound-pivot embedded in a phrase about therapy and fairness.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 289 · FSP-SRC-135 deep lyric reconciliation"},{"n":"225","pow":"what I hear here can't be / what you fear is the air","type":"Sound Chain / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"hear/here/fear/air chain; multiple sound-neighbours steer the scene.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 796 · GTKM-SRC-382 deep lyric reconciliation"},{"n":"226","pow":"People dying hair / people dying here / dying hear","type":"Sound Chain / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"hair/here/hear chain; spelling changes while the sound route continues.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · line 38 · IKC-SRC-014 deep lyric reconciliation"},{"n":"227","pow":"fish in the sea / can't see what makes seeing possible","type":"Sound / Homophone / Concept Line","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"sea/see relation used inside a perception/field observation.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 618 · GTKM-SRC-302 deep lyric reconciliation"},{"n":"228","pow":"sorcerers son / source of the sun","type":"Sound Chain / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"son/source/sun chain linking lineage, source and light.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 670 · GTKM-SRC-327 deep lyric reconciliation"},{"n":"229","pow":"can't save no thyme / Time's up","type":"Sound / Homophone / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"time/thyme homophone used through a chef/time-saving setup.","source":"CALMER KARMA - - GROUNDZEROJM.txt · lines 661–712 · CK-014 deep lyric reconciliation"},{"n":"230","pow":"caught up in a fake court","type":"Sound / Homophone / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"caught/court sound relation inside a legal-system line.","source":"LYRICS THROUGH THE YEARS - GROUNDZEROJM.txt · line 14 · LTY-SRC-005 deep lyric reconciliation"},{"n":"231","pow":"palace hell / maddest cell / magic spells","type":"Sound Chain / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"hell/cell/spell chain inside a prison passage.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 173 · GTKM-SRC-087 | GTKM-GTKY - GROUNDZEROJM.txt · line 350 · GTKM-SRC-175 deep lyric reconciliation"},{"n":"232","pow":"see, we see a sea of AIDS comin'","type":"Sound Chain / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"see/sea repetition made explicit in the line.","source":"I keep changing my mind on what to - GROUNDZEROJM.txt · lines 306–307 · IKC-SRC-115 deep lyric reconciliation"},{"n":"233","pow":"heaven's own eye / heaven owes I / heaven owns I","type":"Sound / Letter / Semantic Chain","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"eye/I plus owns/owes sequence; the source cycles the relation rather than settling one reading.","source":"LYRICS THROUGH THE YEARS - GROUNDZEROJM.txt · lines 26–27 · LTY-SRC-011 deep lyric reconciliation"},{"n":"234","pow":"make a girl my life but she weren't live","type":"Sound / Semantic Pivot / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"life/live semantic-sound pivot.","source":"CALMER KARMA - - GROUNDZEROJM.txt · lines 1–64 · CK-001 deep lyric reconciliation"},{"n":"235","pow":"if you know you know the unknowns","type":"Sound / Repetition / Lyric Line","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"know/no/unknown recursion.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 27 · FSP-SRC-014 deep lyric reconciliation"},{"n":"236","pow":"who nose how to sniff out dope?","type":"Sound / Homophone / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"nose/knows sound-play reinforced by “sniff.”","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 868 · GTKM-SRC-417 deep lyric reconciliation"},{"n":"237","pow":"liver than live may die / sickest move ill / poorly lead","type":"Semantic Sound Chain / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"live/liver and sick/ill/poorly lexical chain.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 804 · GTKM-SRC-386 deep lyric reconciliation"},{"n":"238","pow":"backwards in corpses usin' corpus","type":"Sound / Word-family / Lyric Bar","prov":"PASS 9 / SOURCE-BACKED SOUND CANDIDATE","context":"corpse/corpus word-family and sound relation inside a legal/reporting passage.","source":"LYRICS THROUGH THE YEARS - GROUNDZEROJM.txt · line 22 · LTY-SRC-009 deep lyric reconciliation"},{"n":"239","pow":"Hue-man","type":"Spelling / Split / Lyric Word","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"Nonstandard split makes “human” visibly carry hue inside a passage about nationwide experience.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 199 · FSP-SRC-092 deep lyric reconciliation"},{"n":"240","pow":"neigh-sayers","type":"Sound / Respelling / Lyric Word","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"Respells naysayers through “neigh,” in a chain with neighbours/saviours.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 199 · FSP-SRC-092 deep lyric reconciliation"},{"n":"241","pow":"mic-roexpressions","type":"Split Word / Lyric Word","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"Splits microexpressions so “mic” becomes visible inside the word.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · lines 415–416 · FSP-SRC-189 deep lyric reconciliation"},{"n":"242","pow":"a-d-d HD vision","type":"Letter / Condition / Sound Play","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"The source spells a-d-d before “HD vision,” making ADD/ADHD-like letter-play audible/visible without requiring a diagnostic reading.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 489 · FSP-SRC-214 deep lyric reconciliation"},{"n":"243","pow":"camomile → ammo-mile","type":"Sound / Re-segmentation / Lyric Bar","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"“no camomile … more like an ammo-mile trip” turns a calming plant-word into ammunition/distance imagery.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 491 · FSP-SRC-215 deep lyric reconciliation"},{"n":"244","pow":"unfair yet over-fare","type":"Sound / Semantic Pivot / Lyric Line","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"Taxi/fare context turns unfair into over-fare.","source":"Fuckin 'ell, stuck in a cell I've p - GROUNDZEROJM.txt · line 493 · FSP-SRC-216 deep lyric reconciliation"},{"n":"245","pow":"God-complexion","type":"Compound / Sound / Lyric Coin","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"Source coin bends “God complex” toward complexion in a line about identity and divinity.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 479 · GTKM-SRC-234 deep lyric reconciliation"},{"n":"246","pow":"gobbledy-goop / a word for made-up words","type":"Meta-wordplay / Lyric Line","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"The line asks when a word for made-up words will be made, then supplies “gobbledy-goop.”","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 790 · GTKM-SRC-379 deep lyric reconciliation"},{"n":"247","pow":"nooses hanging from merry-go-rounds be merry go round","type":"Phrase Re-segmentation / Lyric Line","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"merry-go-rounds is immediately re-read as “merry go round.”","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 829 · GTKM-SRC-398 deep lyric reconciliation"},{"n":"248","pow":"Debtman come around / ballers wanna get broke","type":"Money Inversion / Lyric Bar","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"Debt and baller/broke language are inverted in the same bar.","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 745 · GTKM-SRC-364 | I keep changing my mind on what to - GROUNDZEROJM.txt · lines 106–125 · IKC-SRC-045 deep lyric reconciliation"},{"n":"249","pow":"mind blocks / mind block","type":"Word Recursion / Lyric Bar","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"The phrase moves from the mind blocking things to the mind itself having “mind block.”","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 686 · GTKM-SRC-335 deep lyric reconciliation"},{"n":"250","pow":"D' evil / devil","type":"Split Word / Lyric Line","prov":"DIRECT SOURCE — ORTHOGRAPHIC/SOUND CANDIDATE","context":"The source visibly splits D' evil before returning to “the devil.”","source":"GTKM-GTKY - GROUNDZEROJM.txt · line 652 · GTKM-SRC-319 deep lyric reconciliation"}]</script>
<script>
'use strict';
const LEXICON=JSON.parse(document.getElementById('lexicon-data').textContent);
const DEFAULT_BODIES=JSON.parse(document.getElementById('registry-data').textContent);
const BUILTIN_DOCS=JSON.parse(document.getElementById('builtin-docs').textContent);
const META=JSON.parse(document.getElementById('build-meta').textContent);
const LIBRARY_SEED=JSON.parse(document.getElementById('library-ledger-seed').textContent);
const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const MODES=[['unified','Unified','⌘'],['web','Internet','◎'],['estate','Estate','◆'],['words','Words','✦'],['library','Library','▦'],['local','Local','▣'],['map','Visualang','◌'],['rooms','Rooms','▤'],['saved','Saved','★'],['receipts','Receipts','⌁'],['settings','Settings','⚙']];
const QUICK=['RootWords','Word Estate','POW Lab','Library Ledger','Device Continuity','Radius Lexicon','FlowTalk','TraceBox','JM3232 Navigator'];
const STORE_KEY='jm3232UnifiedBrowserV1_1';
const LEGACY_NAV_STATE_KEY='jm3232-navigator-state-v01';
const LEGACY_NAV_RECEIPTS_KEY='jm3232-stringreceipts-v01';
const LEGACY_STRINGLINE_KEY='jm-stringline-local-v01';
const AUTOCONTACT_CARRIER_PREFIX='#jm-ac=';
const AUTOCONTACT_RESULT_PREFIX='#jm-ac-result=';
const PREVIOUS_STORE_KEY='jm3232UnifiedBrowserV1';
const WORD_LEXICON=JSON.parse(document.getElementById('word-estate-v15').textContent);
const WORD_META=JSON.parse(document.getElementById('word-estate-meta-v15').textContent);
const POW_ESTATE=JSON.parse(document.getElementById('pow-estate-v10').textContent);
let memoryPersist=null;
let memoryDocs=[];
const emptyStore=()=>({version:'1.1.2',tabs:[{id:uid(),title:'Unified',query:'',mode:'unified',history:[]}],activeTab:null,bookmarks:[],history:[],receipts:[],rooms:[{id:'room-browser',name:'Unified Browser Build',description:'The current browser crown and its source bodies.',items:['BODY-JM3232-NAV-LEGACY','BODY-RADIUS-LEXICON','BODY-FLOWTALK','BODY-FTR','BODY-TRACEBOX','BODY-VISUALANG']}],bodies:DEFAULT_BODIES,settings:{theme:'dark',engine:'duckduckgo',remote:true,github:true,wikipedia:true,receiptDetail:'full'},compare:[],libraryRows:[],libraryMeta:{importedAt:null,source:'embedded authoritative seed'},wordDeck:[],wordTrace:[],legacyNavigatorV011:null});
let store=loadStore(); if(!store.activeTab) store.activeTab=store.tabs[0]?.id; ensureDefaults();
let state={mode:'unified',query:'',wordTab:'lexicon',wordQuery:'',wordInput:'Gossip with front lead (e)nergy = … Egos sip',wordRoutes:[],wordSelected:null,rootInput:'REMAKE',rootResult:null,estateResults:[],libraryResults:[],libraryHouse:'ALL',webResults:[],localResults:[],selected:null,selectedKind:null,remoteStatus:'idle',localDocs:[],viewStack:[],voice:null,inspectorRequested:false};
const dbPromise=openDB();
function uid(){return 'id-'+Date.now().toString(36)+'-'+Math.random().toString(36).slice(2,8)}
function esc(v=''){return String(v).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function norm(v=''){return String(v).toLowerCase().normalize('NFKD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim()}
function clip(v='',n=160){v=String(v);return v.length>n?v.slice(0,n-1)+'…':v}
function safeStoredJSON(key){try{const raw=localStorage.getItem(key);return raw?JSON.parse(raw):null}catch(e){return null}}
function legacyBookmarkKey(b,i){return 'legacy:v011:bookmark:'+(b?.id||i)+':'+String(b?.url||'')}
function legacyV011ToCurrent(nav,legacyReceipts,stringline,source='localStorage'){
 const next=emptyStore(),stamp=new Date().toISOString(),n=nav&&typeof nav==='object'?nav:{},rs=Array.isArray(legacyReceipts)?legacyReceipts:[],sl=stringline&&typeof stringline==='object'?stringline:null;
 const oldBookmarks=Array.isArray(n.bookmarks)?n.bookmarks:[];
 next.bookmarks=oldBookmarks.map((b,i)=>({key:legacyBookmarkKey(b,i),kind:'legacy-route',title:b?.title||'Legacy Navigator route',description:'Recovered from JM3232 Navigator v0.1.1',url:b?.url||'',id:b?.id||('legacy-bookmark-'+i)}));
 const oldHistory=Array.isArray(n.history)?n.history:[];
 next.history=oldHistory.map((h,i)=>({id:'legacy-v011-history-'+(h?.id||i),time:h?.at||h?.time||stamp,action:'LEGACY_OPEN_ROUTE',target:h?.title||h?.url||'Legacy Navigator route',source:h?.url||'JM3232 Navigator v0.1.1'}));
 next.receipts=rs.map((r,i)=>({id:'legacy-v011-receipt-'+(r?.id||i),time:r?.at||r?.time||stamp,action:r?.action||'LEGACY_RECEIPT',target:r?.detail||r?.target||r?.bodyId||'Navigator v0.1.1',source:'JM3232 Navigator v0.1.1',legacyBodyId:r?.bodyId||''}));
 next.receipts.unshift({id:'legacy-v011-migration-'+stamp.replace(/[^0-9]/g,''),time:stamp,action:'MIGRATE_NAVIGATOR_V0_1_1',target:'canonical door browser state',source:source});
 next.legacyNavigatorV011={source,migratedAt:stamp,keys:{state:LEGACY_NAV_STATE_KEY,receipts:LEGACY_NAV_RECEIPTS_KEY,stringline:LEGACY_STRINGLINE_KEY},navigator:n,receipts:rs,stringline:sl,counts:{bookmarks:oldBookmarks.length,history:oldHistory.length,receipts:rs.length,stringlineProjects:Array.isArray(sl?.projects)?sl.projects.length:0}};
 return next;
}
function mergeLegacyV011IntoStore(nav,legacyReceipts,stringline,source='legacy SavePack'){
 const incoming=legacyV011ToCurrent(nav,legacyReceipts,stringline,source),seen=new Set((store.bookmarks||[]).map(b=>b.url||b.key));
 store.bookmarks=[...incoming.bookmarks.filter(b=>!seen.has(b.url||b.key)),...(store.bookmarks||[])];
 store.history=[...incoming.history,...(store.history||[])].slice(0,500);
 store.receipts=[...incoming.receipts,...(store.receipts||[])].slice(0,500);
 store.legacyNavigatorV011=incoming.legacyNavigatorV011;
}
function loadStore(){try{const current=safeStoredJSON(STORE_KEY);if(current&&current.version)return current;const prior=safeStoredJSON(PREVIOUS_STORE_KEY);if(prior&&prior.version){return {...emptyStore(),...prior,version:'1.1',libraryRows:prior.libraryRows||[],libraryMeta:prior.libraryMeta||{importedAt:null,source:'migrated v1.0 profile'}}}const legacyNav=safeStoredJSON(LEGACY_NAV_STATE_KEY),legacyReceipts=safeStoredJSON(LEGACY_NAV_RECEIPTS_KEY),legacyStringline=safeStoredJSON(LEGACY_STRINGLINE_KEY);if(legacyNav||legacyReceipts||legacyStringline){const migrated=legacyV011ToCurrent(legacyNav,legacyReceipts,legacyStringline,'same-origin localStorage');try{localStorage.setItem(STORE_KEY,JSON.stringify(migrated))}catch(e){}return migrated}return memoryPersist||emptyStore()}catch(e){return memoryPersist||emptyStore()}}
function saveStore(){try{localStorage.setItem(STORE_KEY,JSON.stringify(store))}catch(e){memoryPersist=JSON.parse(JSON.stringify(store))}}
function ensureDefaults(){if(!Array.isArray(store.libraryRows))store.libraryRows=[];if(!store.libraryMeta)store.libraryMeta={importedAt:null,source:'embedded authoritative seed'};if(!('ownerDeviceSelfTest' in store))store.ownerDeviceSelfTest=null;if(!Array.isArray(store.wordDeck))store.wordDeck=[];if(!Array.isArray(store.wordTrace))store.wordTrace=[];if(!('legacyNavigatorV011' in store))store.legacyNavigatorV011=null;const byId=new Map((store.bodies||[]).map(x=>[x.id,x])); DEFAULT_BODIES.forEach(x=>{if(!byId.has(x.id))store.bodies.push(x)}); if(!store.tabs?.length)store.tabs=emptyStore().tabs; if(!store.activeTab)store.activeTab=store.tabs[0].id; saveStore()}
function activeTab(){return store.tabs.find(t=>t.id===store.activeTab)||store.tabs[0]}
function toast(msg,type='good'){const el=document.createElement('div');el.className='toast '+type;el.textContent=msg;$('#toasts').appendChild(el);setTimeout(()=>el.remove(),3300)}
function setMode(mode,push=true){if(push&&state.mode!==mode)state.viewStack.push(state.mode);state.mode=mode;const tab=activeTab();if(tab){tab.mode=mode;saveStore()}render()}
function trace(action,data={}){const rec={id:uid(),time:new Date().toISOString(),action,query:state.query,mode:state.mode,...data};store.receipts.unshift(rec);store.receipts=store.receipts.slice(0,800);saveStore();return rec}
function history(action,target,source='browser'){store.history.unshift({id:uid(),time:new Date().toISOString(),action,target,source});store.history=store.history.slice(0,500);saveStore()}
function openDB(){return new Promise(resolve=>{try{if(typeof indexedDB==='undefined')return resolve(null);const req=indexedDB.open('JM3232UnifiedBrowser',1);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains('docs'))db.createObjectStore('docs',{keyPath:'id'})};req.onsuccess=()=>resolve(req.result);req.onerror=()=>resolve(null)}catch(e){resolve(null)}})}
async function dbAll(){const db=await dbPromise;if(!db)return [...memoryDocs];return new Promise((res,rej)=>{const r=db.transaction('docs','readonly').objectStore('docs').getAll();r.onsuccess=()=>res(r.result||[]);r.onerror=()=>rej(r.error)})}
async function dbPut(doc){const db=await dbPromise;if(!db){const i=memoryDocs.findIndex(x=>x.id===doc.id);if(i>=0)memoryDocs[i]=doc;else memoryDocs.push(doc);return}return new Promise((res,rej)=>{const r=db.transaction('docs','readwrite').objectStore('docs').put(doc);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}
async function dbDelete(id){const db=await dbPromise;if(!db){memoryDocs=memoryDocs.filter(x=>x.id!==id);return}return new Promise((res,rej)=>{const r=db.transaction('docs','readwrite').objectStore('docs').delete(id);r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}
async function dbClear(){const db=await dbPromise;if(!db){memoryDocs=[];return}return new Promise((res,rej)=>{const r=db.transaction('docs','readwrite').objectStore('docs').clear();r.onsuccess=()=>res();r.onerror=()=>rej(r.error)})}
function levenshtein(a,b){if(a===b)return 0;if(!a.length)return b.length;if(!b.length)return a.length;let prev=Array.from({length:b.length+1},(_,i)=>i);for(let i=1;i<=a.length;i++){const cur=[i];for(let j=1;j<=b.length;j++)cur[j]=Math.min(cur[j-1]+1,prev[j]+1,prev[j-1]+(a[i-1]===b[j-1]?0:1));prev=cur}return prev[b.length]}
function entrySearchText(e){return norm([e.canonical_name,e.names?.source_names?.join(' '),e.names?.aliases?.join(' '),e.names?.speech_variants?.join(' '),e.definitions?.plain,e.definitions?.jm_source,e.definitions?.public_safe,e.shelf,e.tags?.join(' '),e.relationships?.meaning_neighbours?.join(' '),e.relationships?.bridge_terms?.join(' '),e.code_face?.canonical_identifier].join(' '))}
function estateSearch(q,limit=30){const nq=norm(q);if(!nq)return LEXICON.slice(0,18);const terms=nq.split(' ').filter(Boolean);const scored=[];for(const e of LEXICON){const name=norm(e.canonical_name);const aliases=(e.names?.aliases||[]).map(norm);const sourceNames=(e.names?.source_names||[]).map(norm);const text=entrySearchText(e);let score=0;if(name===nq)score+=220;if(sourceNames.includes(nq))score+=180;if(aliases.includes(nq))score+=160;if(name.startsWith(nq))score+=95;if(name.includes(nq))score+=70;if(text.includes(nq))score+=40;for(const t of terms){if(name===t)score+=40;else if(name.startsWith(t))score+=25;else if(text.includes(t))score+=9}if(nq.length>3&&levenshtein(name,nq)<=2)score+=55;if(score>0)scored.push({item:e,score})}return scored.sort((a,b)=>b.score-a.score||a.item.canonical_name.localeCompare(b.item.canonical_name)).slice(0,limit).map(x=>x.item)}
function bodySearch(q,limit=12){const nq=norm(q);return store.bodies.map(b=>({item:b,score:nq?((norm(b.name)===nq?100:0)+(norm(b.name).includes(nq)?45:0)+(norm([b.role,b.type,b.status,b.project].join(' ')).includes(nq)?18:0)):10})).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,limit).map(x=>x.item)}
async function localSearch(q,limit=20){const docs=[...BUILTIN_DOCS,...await dbAll()];state.localDocs=docs;const nq=norm(q);return docs.map(d=>{const name=norm(d.name),text=norm(d.text||'');let score=nq?((name===nq?100:0)+(name.includes(nq)?45:0)+(text.includes(nq)?15:0)):10;return{item:d,score}}).filter(x=>x.score>0).sort((a,b)=>b.score-a.score).slice(0,limit).map(x=>x.item)}
function engineURL(q){const s=store.settings.engine||'duckduckgo';const urls={duckduckgo:'https://duckduckgo.com/?q=',google:'https://www.google.com/search?q=',bing:'https://www.bing.com/search?q='};return (urls[s]||urls.duckduckgo)+encodeURIComponent(q)}
function validURL(raw){try{let s=raw.trim();if(!/^https?:\/\//i.test(s)&&/^([\w-]+\.)+[a-z]{2,}(\/|$)/i.test(s))s='https://'+s;const u=new URL(s);return ['http:','https:'].includes(u.protocol)?u.href:null}catch(e){return null}}
async function fetchInternet(q){state.remoteStatus='loading';state.webResults=[];renderMainOnly();const results=[{id:'web-search-'+norm(q),kind:'search',title:'Search the wider public web',description:'Open the full query with '+(store.settings.engine||'DuckDuckGo')+' through your device browser.',url:engineURL(q),source:'Public web handoff'}];const jobs=[];if(store.settings.remote&&store.settings.wikipedia){jobs.push(fetch('https://en.wikipedia.org/w/api.php?action=opensearch&search='+encodeURIComponent(q)+'&limit=6&namespace=0&format=json&origin=*').then(r=>r.json()).then(d=>{(d[1]||[]).forEach((title,i)=>results.push({id:'wiki-'+title,kind:'wikipedia',title,description:d[2]?.[i]||'Wikipedia result',url:d[3]?.[i],source:'Wikipedia'}))}).catch(()=>{}))}if(store.settings.remote&&store.settings.github){jobs.push(fetch('https://api.github.com/search/repositories?q='+encodeURIComponent(q)+'&per_page=5',{headers:{Accept:'application/vnd.github+json'}}).then(r=>r.ok?r.json():Promise.reject()).then(d=>{(d.items||[]).forEach(x=>results.push({id:'gh-'+x.id,kind:'github',title:x.full_name,description:x.description||'GitHub repository',url:x.html_url,source:'GitHub',meta:(x.language||'')+(x.stargazers_count?' · ★ '+x.stargazers_count:'')}))}).catch(()=>{}))}await Promise.allSettled(jobs);state.webResults=results;state.remoteStatus='done';renderMainOnly()}
function allLibraryRows(){const map=new Map();for(const r of [...(LIBRARY_SEED.rows||[]),...(store.libraryRows||[])])map.set(r.id||('lib-'+norm((r.physicalPath||'')+' '+(r.name||''))),r);return [...map.values()]}
function librarySearch(q,limit=60){const rows=allLibraryRows();const nq=norm(q);if(!nq)return rows.slice(0,limit);const terms=nq.split(' ').filter(Boolean);const scored=[];for(const r of rows){const name=norm(r.name), path=norm(r.physicalPath), text=norm([r.name,r.kind,r.house,r.houseTitle,r.subject,r.family,r.version,r.carrierType,r.status,r.authority,r.routeClass,r.physicalPath,r.notes,(r.secondaryHouses||[]).join(' ')].join(' '));let score=0;if(name===nq)score+=220;if(name.startsWith(nq))score+=120;if(name.includes(nq))score+=85;if(path.includes(nq))score+=70;if(text.includes(nq))score+=45;for(const t of terms){if(name===t)score+=45;else if(name.includes(t))score+=24;else if(text.includes(t))score+=10}if(score>0)scored.push({r,score})}return scored.sort((a,b)=>b.score-a.score||String(a.r.name).localeCompare(String(b.r.name))).slice(0,limit).map(x=>x.r)}
function filteredLibraryRows(){let rows=state.query?librarySearch(state.query,500):allLibraryRows();if(state.libraryHouse&&state.libraryHouse!=='ALL')rows=rows.filter(r=>r.house===state.libraryHouse);return rows}
function libraryHouseCounts(){const counts={};for(const r of allLibraryRows())counts[r.house]=(counts[r.house]||0)+1;return counts}
function renderLibraryResult(r){return `<article class="result library-result" data-action="inspect-library" data-id="${esc(r.id)}"><h3>${esc(r.name)}</h3><p>${esc(clip([r.houseTitle,r.kind,r.status].filter(Boolean).join(' · '),130))}</p><div class="route">${esc(r.physicalPath||'No physical path recorded')}</div><div class="result-actions"><button data-action="inspect-library" data-id="${esc(r.id)}">Inspect</button><button data-action="save-library" data-id="${esc(r.id)}">Save</button><button data-action="trace-item" data-id="${esc(r.id)}" data-kind="library">Trace</button></div></article>`}
function renderLibraryRow(r){return `<tr data-action="inspect-library" data-id="${esc(r.id)}"><td><div class="lib-name">${esc(r.name)}</div><div class="lib-path">${esc(r.physicalPath||'')}</div></td><td><span class="lib-chip house">${esc(r.house==='UNROUTED'?'UNROUTED':r.house+' '+(r.houseTitle||''))}</span></td><td>${esc(r.kind||'')}</td><td>${esc(r.carrierType||'')}</td><td>${r.bytes?esc(formatBytes(r.bytes)):'—'}</td><td><span class="lib-chip live">${esc(r.status||'')}</span></td><td><button class="small-btn" data-action="inspect-library" data-id="${esc(r.id)}">Inspect</button></td></tr>`}
function selfTestSummary(){const t=store.ownerDeviceSelfTest;if(!t)return `<div class="selftest-strip"><div><strong>Owner-device AutoContact</strong><br><span>Not run yet. This build will run it automatically once.</span></div><button class="small-btn" data-action="owner-device-selftest">Run now</button></div>`;const cls=t.phase==='PASS'?'pass':(t.phase==='FAIL'?'fail':'');const c=t.checks||{};const done=Object.values(c).filter(Boolean).length,total=Object.keys(c).length;return `<div class="selftest-strip ${cls}"><div><strong>Owner-device AutoContact · ${esc(t.phase||'PENDING')}</strong><br><span>${done}/${total} checks · ${esc(t.targetName||'Device Continuity')} · ${esc(t.finishedAt||t.startedAt||'')}</span></div><div style="display:flex;gap:6px"><button class="small-btn" data-action="owner-device-selftest">Run again</button>${t.phase==='PASS'||t.phase==='FAIL'?`<button class="small-btn" data-action="clear-selftest">Clear</button>`:''}</div></div>`}
function hostContinuityClass(){const p=(location.protocol||'').toLowerCase();return (p==='content:'||p==='file:'||location.origin==='null')?'opaque-carrier-host':'origin-capable-host'}
function b64urlEncodeText(s){try{return btoa(unescape(encodeURIComponent(s))).replace(/\+/g,'-').replace(/\//g,'_').replace(/=+$/,'')}catch(e){return ''}}
function b64urlDecodeText(s){try{s=s.replace(/-/g,'+').replace(/_/g,'/');while(s.length%4)s+='=';return decodeURIComponent(escape(atob(s)))}catch(e){return ''}}
function makeAutoContactCapsule(target,bookmark,receipt,checks){return {format:'JM3232_AUTOCONTACT_CAPSULE',version:'1.1.2',createdAt:new Date().toISOString(),hostClass:hostContinuityClass(),targetId:target.id,targetName:target.name,bookmark,receipt,checks:{...checks},route:'FIND→INSPECT→SAVE→TRACE→CARRIER→RELOAD→RECOVER'}}
function writeAutoContactCarrier(capsule,prefix=AUTOCONTACT_CARRIER_PREFIX){try{const enc=b64urlEncodeText(JSON.stringify(capsule));if(!enc)return false;location.hash=prefix.slice(1)+enc;return location.hash.includes(enc.slice(0,Math.min(24,enc.length)))}catch(e){return false}}
function readAutoContactCarrier(){try{const h=location.hash||'';let prefix=h.startsWith(AUTOCONTACT_RESULT_PREFIX)?AUTOCONTACT_RESULT_PREFIX:(h.startsWith(AUTOCONTACT_CARRIER_PREFIX)?AUTOCONTACT_CARRIER_PREFIX:null);if(!prefix)return null;const raw=b64urlDecodeText(h.slice(prefix.length));const x=raw?JSON.parse(raw):null;return x&&x.format==='JM3232_AUTOCONTACT_CAPSULE'?x:null}catch(e){return null}}
function testNativeOriginStorage(targetId){try{localStorage.setItem('__jm3232_probe__',targetId);const ok=localStorage.getItem('__jm3232_probe__')===targetId;localStorage.removeItem('__jm3232_probe__');return ok}catch(e){return false}}
function rehydrateFromCapsule(capsule){if(!capsule)return false;try{if(capsule.bookmark&&!store.bookmarks.some(x=>x.key===capsule.bookmark.key))store.bookmarks.push(capsule.bookmark);if(capsule.receipt&&!store.receipts.some(x=>x.id===capsule.receipt.id))store.receipts.push(capsule.receipt);return true}catch(e){return false}}
function selfTestTarget(){return librarySearch('Device Continuity').find(r=>r.subject==='Device Continuity')||librarySearch('Device Continuity')[0]||null}
function selfTestRecoveryRoundTrip(target,capsule=null){try{const payload={format:'JM3232_UNIFIED_BROWSER_CONTINUITY_SELFTEST',version:'1.1.2',rows:allLibraryRows(),bookmark:capsule?.bookmark||null,receipt:capsule?.receipt||null,meta:store.libraryMeta};const wire=JSON.stringify(payload);const recovered=JSON.parse(wire);return Array.isArray(recovered.rows)&&recovered.rows.some(r=>r.id===target.id)&&recovered.format==='JM3232_UNIFIED_BROWSER_CONTINUITY_SELFTEST'&&(!capsule||recovered.bookmark?.key===capsule.bookmark?.key)&&(!capsule||recovered.receipt?.id===capsule.receipt?.id)}catch(e){return false}}
function selfTestSummary(){const t=store.ownerDeviceSelfTest||readAutoContactCarrier();if(!t)return `<div class="selftest-strip"><div><strong>Owner-device AutoContact</strong><br><span>Not run yet. This build will run it automatically once.</span></div><button class="small-btn" data-action="owner-device-selftest">Run now</button></div>`;const cls=t.phase==='PASS'?'pass':(t.phase==='FAIL'?'fail':'');const c=t.checks||{};const required=['find','inspect','save','trace','continuityCarrier','reloadPersist','recoveryRoundTrip'];const done=required.filter(k=>!!c[k]).length,total=required.length;const native=c.originStorage?'native origin storage':'carrier continuity';return `<div class="selftest-strip ${cls}"><div><strong>Owner-device AutoContact · ${esc(t.phase||'PENDING')}</strong><br><span>${done}/${total} required checks · ${esc(t.targetName||'Device Continuity')} · ${esc(native)} · ${esc(t.finishedAt||t.startedAt||t.createdAt||'')}</span></div><div style="display:flex;gap:6px"><button class="small-btn" data-action="owner-device-selftest">Run again</button>${t.phase==='PASS'||t.phase==='FAIL'?`<button class="small-btn" data-action="clear-selftest">Clear</button>`:''}</div></div>`}
async function runOwnerDeviceSelfTest(){const target=selfTestTarget();const checks={find:false,inspect:false,save:false,trace:false,originStorage:false,continuityCarrier:false,reloadPersist:false,recoveryRoundTrip:false};checks.find=!!target;if(!target){store.ownerDeviceSelfTest={format:'JM3232_AUTOCONTACT_CAPSULE',version:'1.1.2',phase:'FAIL',startedAt:new Date().toISOString(),finishedAt:new Date().toISOString(),targetName:'Device Continuity',checks,error:'No Library target resolved'};saveStore();render();showOwnerDeviceSelfTestResult(store.ownerDeviceSelfTest);return}state.query='Device Continuity';state.mode='library';state.libraryResults=librarySearch(state.query);state.selected=target;state.selectedKind='library';state.inspectorRequested=true;checks.inspect=inspectLibrary(target).includes(target.name);const b=libraryBookmark(target);addBookmark(b);checks.save=store.bookmarks.some(x=>x.key===b.key);const rec=trace('OWNER_DEVICE_AUTOCONTACT_PRE_RELOAD',{target:target.name,source:'Estate Library Mesh',targetId:target.id,hostClass:hostContinuityClass()});checks.trace=store.receipts.some(x=>x.id===rec.id);checks.originStorage=testNativeOriginStorage(target.id);const capsule=makeAutoContactCapsule(target,b,rec,checks);capsule.phase='RELOAD_PENDING';capsule.startedAt=new Date().toISOString();capsule.bookmarkKey=b.key;capsule.receiptId=rec.id;checks.continuityCarrier=writeAutoContactCarrier(capsule);capsule.checks={...checks};store.ownerDeviceSelfTest=capsule;saveStore();if(!checks.continuityCarrier){capsule.phase='FAIL';capsule.finishedAt=new Date().toISOString();capsule.error='Neither native origin storage nor carrier-resident continuity could establish a reload handoff';store.ownerDeviceSelfTest=capsule;saveStore();render();showOwnerDeviceSelfTestResult(capsule);return}render();modal('Owner-device AutoContact',`<p class="inspect-def">Phase 1 passed. Host: <strong>${esc(capsule.hostClass)}</strong>. ${checks.originStorage?'Native storage is available; carrier backup also armed.':'Native origin storage is blocked here, so the JM carrier-resident continuity route is armed.'} Reloading automatically now…</p>`);setTimeout(()=>location.reload(),900)}
function resumeOwnerDeviceSelfTest(){const carrier=readAutoContactCarrier();const stored=store.ownerDeviceSelfTest;const t=(stored&&stored.phase==='RELOAD_PENDING')?stored:(carrier&&carrier.phase==='RELOAD_PENDING'?carrier:null);if(!t)return null;const target=allLibraryRows().find(r=>r.id===t.targetId)||selfTestTarget();const c={...(t.checks||{})};let nativePersist=false;try{const raw=localStorage.getItem(STORE_KEY);const parsed=raw?JSON.parse(raw):null;nativePersist=!!(parsed&&Array.isArray(parsed.bookmarks)&&parsed.bookmarks.some(x=>x.key===t.bookmarkKey)&&Array.isArray(parsed.receipts)&&parsed.receipts.some(x=>x.id===t.receiptId))}catch(e){nativePersist=false}const carrierPersist=!!carrier&&carrier.targetId===t.targetId&&carrier.bookmark?.key===t.bookmarkKey&&carrier.receipt?.id===t.receiptId;c.originStorage=!!c.originStorage;c.continuityCarrier=carrierPersist||!!c.continuityCarrier;if(carrierPersist)rehydrateFromCapsule(carrier);c.reloadPersist=nativePersist||carrierPersist;c.recoveryRoundTrip=!!target&&selfTestRecoveryRoundTrip(target,carrier||t);if(target){state.query='Device Continuity';state.mode='library';state.libraryResults=librarySearch(state.query);state.selected=target;state.selectedKind='library';state.inspectorRequested=true}const required=['find','inspect','save','trace','continuityCarrier','reloadPersist','recoveryRoundTrip'];t.checks=c;t.phase=required.every(k=>!!c[k])?'PASS':'FAIL';t.finishedAt=new Date().toISOString();t.persistenceRoute=nativePersist?'native-origin-storage':(carrierPersist?'carrier-resident-url-fragment':'none');t.hostClass=t.hostClass||hostContinuityClass();store.ownerDeviceSelfTest=t;trace('OWNER_DEVICE_AUTOCONTACT_COMPLETE',{target:t.targetName,source:'Estate Library Mesh',outcome:t.phase,checks:c,persistenceRoute:t.persistenceRoute,hostClass:t.hostClass});saveStore();writeAutoContactCarrier(t,AUTOCONTACT_RESULT_PREFIX);return t}
function showOwnerDeviceSelfTestResult(t){if(!t)return;const labels={find:'find',inspect:'inspect',save:'save',trace:'trace',originStorage:'native origin storage',continuityCarrier:'carrier continuity',reloadPersist:'reload persistence',recoveryRoundTrip:'recovery round-trip'};const required=new Set(['find','inspect','save','trace','continuityCarrier','reloadPersist','recoveryRoundTrip']);const rows=Object.entries(t.checks||{}).map(([k,v])=>{let status=v?'PASS':(k==='originStorage'&&!required.has(k)?'BYPASSED':'FAIL');let note=(k==='originStorage'&&!v)?'content:// / opaque host: expected boundary; carrier route used':'';return `<div class="list-row"><div><h3>${esc(labels[k]||k)}</h3><p>${esc(status)}${note?` · ${esc(note)}`:''}</p></div></div>`}).join('');modal('Owner-device AutoContact — '+t.phase,`<p class="inspect-def"><strong>${esc(t.targetName||'Device Continuity')}</strong></p><p class="inspect-def">Host: ${esc(t.hostClass||hostContinuityClass())}<br>Continuity route: <strong>${esc(t.persistenceRoute||((t.checks||{}).originStorage?'native-origin-storage':'carrier-resident-url-fragment'))}</strong></p><div class="list-view">${rows}</div><p class="inspect-def">Route tested: FIND → INSPECT → SAVE → TRACE → CARRIER/STORE → RELOAD → RECOVER.</p>`)}
function renderLibrary(){const rows=filteredLibraryRows(),counts=libraryHouseCounts(),c=LIBRARY_SEED.physicalCensusSnapshot||{};const houseCards=(LIBRARY_SEED.houses||[]).map(h=>`<button class="house-card ${state.libraryHouse===h.id?'active':''}" data-action="library-house" data-house="${esc(h.id)}"><b>${esc(h.id+' '+h.title)}</b><span>${counts[h.id]||0} indexed routes · ${esc(h.purpose)}</span></button>`).join('');return `<section class="library-hero"><div class="library-kernel"><h2>The Estate is now browsable as a body.</h2><p>FTR finds. Radius relates. Stringline preserves identity. TraceBox receipts the route. Physical carrier, logical body, subject belonging and authority state remain separately visible.</p><div class="law-strip" style="margin-top:14px">${(LIBRARY_SEED.laws||[]).slice(0,4).map(x=>`<span class="law">${esc(x)}</span>`).join('')}</div></div><div class="library-stats"><div class="library-stat"><strong>${allLibraryRows().length}</strong><span>seeded live routes</span></div><div class="library-stat"><strong>${LIBRARY_SEED.houses.length}</strong><span>logical houses</span></div><div class="library-stat"><strong>${(c.totalItems||0).toLocaleString('en-GB')}</strong><span>23 Jul census items</span></div><div class="library-stat"><strong>${formatBytes(c.bytes||0)}</strong><span>23 Jul census bytes</span></div></div></section><div class="section-title"><h2>Nine-house router</h2><span>One front door → subjects → sovereign source rooms</span></div><div class="house-grid"><button class="house-card ${state.libraryHouse==='ALL'?'active':''}" data-action="library-house" data-house="ALL"><b>ALL HOUSES</b><span>${allLibraryRows().length} visible routes across live and preserved carriers.</span></button>${houseCards}</div><div class="library-toolbar"><select id="libraryHouseFilter"><option value="ALL">All houses</option>${(LIBRARY_SEED.houses||[]).map(h=>`<option value="${esc(h.id)}" ${state.libraryHouse===h.id?'selected':''}>${esc(h.id+' '+h.title)}</option>`).join('')}<option value="UNROUTED" ${state.libraryHouse==='UNROUTED'?'selected':''}>Unrouted live roots</option></select><button class="small-btn" data-action="export-ledger-json">Export JSON</button><button class="small-btn" data-action="export-ledger-csv">Export CSV</button><button class="small-btn" data-action="import-ledger">Import/refresh ledger</button>${store.libraryRows.length?`<button class="small-btn" data-action="clear-imported-ledger">Clear imported rows</button>`:''}<span class="muted" style="font-size:10px">${rows.length} rows visible${state.query?' for “'+esc(state.query)+'”':''}</span></div>${selfTestSummary()}<div class="library-scroll"><table class="library-table"><thead><tr><th>Name / path</th><th>House</th><th>Body/route type</th><th>Carrier</th><th>Bytes</th><th>Authority/status</th><th></th></tr></thead><tbody>${rows.map(renderLibraryRow).join('')||`<tr><td colspan="7"><div class="empty">No Library rows match this field.</div></td></tr>`}</tbody></table></div><div class="section-title"><h2>Physical census snapshot</h2><span>Dated truth remains dated.</span></div><div class="list-view"><div class="list-row"><div><h3>23 July 2026 complete metadata census</h3><p>${Number(c.totalItems||0).toLocaleString('en-GB')} items · ${Number(c.folders||0).toLocaleString('en-GB')} folders · ${Number(c.files||0).toLocaleString('en-GB')} files · ${formatBytes(c.bytes||0)}</p><p class="mono" style="font-size:10px;margin-top:6px">${esc(c.truthBoundary||'')}</p></div></div><div class="list-row"><div><h3>Largest carrier populations</h3><p>${Object.entries(c.fileTypes||{}).map(([k,v])=>`${esc(k)} ${Number(v).toLocaleString('en-GB')}`).join(' · ')}</p></div></div></div>`}
function inspectLibrary(r){return `<div class="inspect-kicker">${esc(r.kind||'Library route')} · ${esc(r.status||'')}</div><h2 class="inspect-name">${esc(r.name)}</h2><p class="muted" style="font-size:11px">${esc(r.house==='UNROUTED'?'UNROUTED':(r.house||'')+' '+(r.houseTitle||''))}</p><div class="inspect-block"><h4>Physical carrier</h4><p class="route mono">${esc(r.physicalPath||'No path recorded')}</p><p class="inspect-def">${esc(r.carrierType||'')} ${r.bytes?' · '+esc(formatBytes(r.bytes)):''}</p></div><div class="inspect-block"><h4>Logical belonging</h4><p class="inspect-def"><strong>Subject:</strong> ${esc(r.subject||'')}<br><strong>Family:</strong> ${esc(r.family||'')}<br><strong>Version:</strong> ${esc(r.version||'Not stated')}</p></div><div class="inspect-block"><h4>Authority state</h4><p class="inspect-def">${esc(r.authority||'')}</p><p class="inspect-def">${esc(r.notes||'')}</p></div><div class="inspect-block"><h4>Route law</h4><p class="pictograph">PHYSICAL CARRIER → LOGICAL BODY → SUBJECT / HOUSE → AUTHORITY / LINEAGE → TRACE / RECOVERY</p></div><div class="inspect-actions"><button data-action="save-library" data-id="${esc(r.id)}">Save</button><button data-action="trace-item" data-id="${esc(r.id)}" data-kind="library">Trace receipt</button><button data-action="mode" data-mode="library">Back to ledger</button></div>`}
function libraryBookmark(r){return{key:'library:'+r.id,kind:'library',title:r.name,description:[r.houseTitle,r.kind,r.status,r.physicalPath].filter(Boolean).join(' · '),id:r.id}}
function exportLibraryLedger(fmt='json'){const payload={...LIBRARY_SEED,exportedAt:new Date().toISOString(),rows:allLibraryRows(),importedMeta:store.libraryMeta};if(fmt==='json'){download('JM_LIBRARY_MASTER_LEDGER_v0_1_'+new Date().toISOString().slice(0,10)+'.json',JSON.stringify(payload,null,2));}else{const cols=['id','name','kind','house','houseTitle','subject','family','version','carrierType','bytes','physicalPath','status','authority','routeClass','sourceSnapshot','notes'];const q=v=>'"'+String(v??'').replace(/"/g,'""')+'"';const csv=[cols.join(','),...allLibraryRows().map(r=>cols.map(k=>q(Array.isArray(r[k])?r[k].join(' | '):r[k])).join(','))].join('\n');download('JM_LIBRARY_MASTER_LEDGER_v0_1_'+new Date().toISOString().slice(0,10)+'.csv',csv)}trace('EXPORT_LIBRARY_LEDGER',{target:fmt.toUpperCase(),source:'Estate Library Mesh',rows:allLibraryRows().length});toast('Library ledger '+fmt.toUpperCase()+' exported.')}
async function importLibraryLedger(file){try{let imported=[];if(file.name.toLowerCase().endsWith('.csv')){const lines=(await file.text()).split(/\r?\n/).filter(Boolean);const parse=line=>{const out=[];let cur='',quote=false;for(let i=0;i<line.length;i++){const ch=line[i];if(ch==='"'){if(quote&&line[i+1]==='"'){cur+='"';i++}else quote=!quote}else if(ch===','&&!quote){out.push(cur);cur=''}else cur+=ch}out.push(cur);return out};const head=parse(lines.shift());imported=lines.map((line,idx)=>{const vals=parse(line),r={};head.forEach((h,i)=>r[h]=vals[i]??'');r.id=r.id||('LIB-IMPORT-'+uid()+'-'+idx);r.bytes=r.bytes?Number(r.bytes):null;return r})}else{const d=JSON.parse(await file.text());imported=Array.isArray(d)?d:(d.rows||[])}if(!Array.isArray(imported))throw new Error('No rows');store.libraryRows=imported.map((r,i)=>({...r,id:r.id||('LIB-IMPORT-'+uid()+'-'+i),authority:r.authority||'IMPORTED LEDGER ROW'}));store.libraryMeta={importedAt:new Date().toISOString(),source:file.name};saveStore();state.libraryResults=state.query?librarySearch(state.query):[];trace('IMPORT_LIBRARY_LEDGER',{target:file.name,source:'chosen ledger file',rows:store.libraryRows.length});toast(store.libraryRows.length+' ledger rows imported/refreshed.');setMode('library')}catch(e){toast('Could not import that Library ledger.','bad')}}

function parseCommand(raw){const v=raw.trim();if(!v.startsWith('/'))return null;const [cmd,...rest]=v.slice(1).split(/\s+/);return{cmd:cmd.toLowerCase(),arg:rest.join(' ')}}
async function runSearch(raw,scope){const q=raw.trim();if(!q){setMode('unified');return}const command=parseCommand(q);if(command){if(await runCommand(command))return}const url=validURL(q);if(url&&(!scope||scope==='unified')){openExternal(url,'typed URL');return}state.query=q;const tab=activeTab();if(tab){tab.query=q;tab.title=clip(q,24);tab.history.push({q,time:new Date().toISOString()});tab.mode=scope||state.mode;saveStore()}state.mode=scope||state.mode||'unified';state.wordQuery=q;state.rootInput=q;state.rootResult=rootAnalyse(q);if(state.mode==='words'&&!state.wordTab)state.wordTab='lexicon';state.estateResults=estateSearch(q);state.libraryResults=librarySearch(q);state.localResults=await localSearch(q);state.selected=state.estateResults[0]||bodySearch(q)[0]||state.libraryResults[0]||state.localResults[0]||null;state.selectedKind=state.estateResults[0]?'estate':(bodySearch(q)[0]?'body':(state.libraryResults[0]?'library':(state.localResults[0]?'local':null)));store.history.unshift({id:uid(),time:new Date().toISOString(),action:'search',target:q,source:state.mode});store.history=store.history.slice(0,500);trace('SEARCH',{target:q,source:'Whole Contact Field',resultCounts:{estate:state.estateResults.length,library:state.libraryResults.length,local:state.localResults.length,lexicon:wordLexSearch(q,999).length,pow:powSearch(q,999).length,root:(state.rootResult?.hits?.length||0)+(state.rootResult?.prefixHits?.length||0)}});saveStore();render();if(['unified','web'].includes(state.mode))fetchInternet(q)}
async function runCommand(c){switch(c.cmd){case'help':openHelp();return true;case'web':setMode('web');await runSearch(c.arg,'web');return true;case'estate':setMode('estate');await runSearch(c.arg,'estate');return true;case'words':setMode('words');state.wordTab='lexicon';await runSearch(c.arg,'words');return true;case'root':state.query=c.arg;state.rootInput=c.arg;state.rootResult=rootAnalyse(c.arg);state.wordTab='rootwords';setMode('words');return true;case'pow':state.query=c.arg;state.wordQuery=c.arg;state.wordTab='pow';setMode('words');return true;case'library':setMode('library');await runSearch(c.arg,'library');return true;case'local':setMode('local');await runSearch(c.arg,'local');return true;case'open':{const u=validURL(c.arg);if(u)openExternal(u,'/open command');else toast('That route is not a valid public URL.','bad');return true}case'room':openRoomModal(c.arg);return true;case'trace':setMode('receipts');return true;case'body':openBodyModal(c.arg);return true;case'home':setMode('unified');state.query='';render();return true;default:toast('Unknown command. Use /help for the command map.','bad');return true}}
function openExternal(url,label='external route'){const safe=validURL(url);if(!safe){toast('Blocked an invalid or unsupported route.','bad');return}window.open(safe,'_blank','noopener,noreferrer');history('open',safe,label);trace('OPEN_EXTERNAL',{target:safe,source:label,boundary:'Rendered by device browser'});toast('Opened through the device browser. Route receipt stored.')}
function render(){applyTheme();renderTabs();renderModes();renderSidebar();renderHead();renderMain();renderInspector();renderMobileDock();renderCompare()}
function renderTabs(){$('#tabs').innerHTML=store.tabs.map(t=>`<button class="tab ${t.id===store.activeTab?'active':''}" data-action="activate-tab" data-id="${esc(t.id)}"><span class="tab-title">${esc(t.title||'New tab')}</span>${store.tabs.length>1?`<span class="tab-close" data-action="close-tab" data-id="${esc(t.id)}">×</span>`:''}</button>`).join('')}
function renderModes(){const html=MODES.map(([id,label,icon])=>`<button class="mode-btn ${state.mode===id?'active':''}" data-action="mode" data-mode="${id}">${icon} ${label}</button>`).join('');$('#modeRail').innerHTML=html;$('#scopeSelect').value=['web','estate','words','library','local'].includes(state.mode)?state.mode:'unified';$('#quickChips').innerHTML=QUICK.map(q=>`<button class="quick-chip" data-action="quick-search" data-query="${esc(q)}">${esc(q)}</button>`).join('')}
function renderSidebar(){const saved=store.bookmarks.length,receipts=store.receipts.length,rooms=store.rooms.length,bodies=store.bodies.length,library=allLibraryRows().length;$('#sidebar').innerHTML=`<div class="side-title">Navigate</div>${[['unified','Unified Search','⌘'],['web','Web Explorer','◎'],['estate','Estate Explorer','◆'],['words','Words · POW · Roots','✦'],['library','Library Ledger','▦'],['local','Local Bodies','▣'],['saved','Bookmarks','★'],['receipts','Trace Receipts','⌁'],['rooms','Rooms','▤']].map(([m,l,i])=>`<button class="side-action ${state.mode===m?'active':''}" data-action="mode" data-mode="${m}"><span>${i}</span><b>${l}</b>${m==='saved'?`<span class="side-count">${saved}</span>`:m==='receipts'?`<span class="side-count">${receipts}</span>`:m==='rooms'?`<span class="side-count">${rooms}</span>`:m==='library'?`<span class="side-count">${library}</span>`:''}</button>`).join('')}<div class="side-title" style="margin-top:15px">Estate tools</div>${[['map','Visualang Map','◌'],['words','Words / RootWords','✦'],['estate','Radius Lexicon','◉'],['settings','Settings & Access','⚙']].map(([m,l,i])=>`<button class="side-action" data-action="mode" data-mode="${m}"><span>${i}</span><b>${l}</b></button>`).join('')}<button class="side-action" data-action="add-body"><span>＋</span><b>Register Body</b><span class="side-count">${bodies}</span></button><button class="side-action" data-action="import-local"><span>⇧</span><b>Import Local Files</b></button><button class="side-action" data-action="export-savepack"><span>⇩</span><b>Export SavePack</b></button><button class="side-action" data-action="import-savepack"><span>↥</span><b>Import SavePack</b></button><div class="owner-card"><strong>You are the owner.</strong><p class="muted" style="font-size:11px;line-height:1.5;margin:6px 0 0">Your searches, bodies, rooms and receipts remain local to this browser profile until you export or deliberately open a public route.</p></div>`}
function renderHead(){const m={unified:['Unified Browser','Internet + Estate + Library + Words + Local. One governed contact field.'],web:['Internet Explorer','Live public connectors plus a full-device-browser handoff.'],estate:['Estate Explorer','Radius Lexicon, registered sovereign bodies and the routed Library mesh.'],words:['Word Estate + POW Workbench','Current Lexicon v1.5 authority + FLAZed POW v1.0 + executable language routing.'],library:['Library Master Ledger','Nine houses → subjects → bodies → versions → physical carriers → authority state.'],local:['Local Bodies','Search user-imported files without moving or deleting the originals.'],map:['Visualang Relationship Map','See terms, bridges, false twins and route relations.'],rooms:['Rooms & Workspaces','Gather bodies without flattening them into one pile.'],saved:['Saved Routes','Bookmarks and comparison bodies held on this device.'],receipts:['TraceBox Receipts','Every meaningful action leaves a local route receipt.'],settings:['Settings & Permissions','Control remote connectors, search handoff and local data.']}[state.mode];$('#viewTitle').textContent=m[0];$('#viewSubtitle').textContent=m[1];$('#headActions').innerHTML=state.mode==='library'?`<button class="small-btn" data-action="export-ledger-json">Export ledger</button><button class="small-btn" data-action="import-ledger">Refresh/import</button>`:state.mode==='local'?`<button class="small-btn" data-action="import-local">Import files</button><button class="small-btn" data-action="clear-local">Clear imported</button>`:state.mode==='receipts'?`<button class="small-btn" data-action="export-receipts">Export receipts</button><button class="small-btn" data-action="clear-receipts">Clear</button>`:state.mode==='rooms'?`<button class="small-btn" data-action="create-room">Create room</button>`:state.mode==='saved'?`<button class="small-btn" data-action="export-savepack">Export SavePack</button>`:`<button class="small-btn" data-action="new-tab">New tab</button><button class="small-btn" data-action="compare-open">Compare (${store.compare.length})</button>`}

// --- v1.3.0 WORD ESTATE / POW / ROOTWORDS WORKBENCH ---
const WORD_STOP=new Set(['the','and','with','from','this','that','your','you','are','for','into','onto','then','than','but','not','was','were','has','have','had','its','our','out','all','one','two','too','to','of','in','on','at','as','is','it','a','an','or','if','so','by','be']);
const WORD_SOUND_PAIRS=[['know','no','Analyzer'],['see','sea','Analyzer'],['hear','here','Analyzer'],['right','write','Analyzer'],['son','sun','Analyzer'],['peace','piece','POW'],['cell','sell','POW'],['court','caught','POW'],['weight','wait','Analyzer'],['weak','week','Analyzer'],['break','brake','Analyzer'],['soul','sole','Analyzer'],['i','eye','Analyzer'],['roll','role','POW'],['author','arthur','POW'],['prey','pray','POW'],['ewe','you','POW'],['death','deaf','POW'],['eight','ate','POW'],['her','hair','POW'],['wrap','rap','POW'],['thyme','time','POW'],['life','live','POW'],['nose','knows','POW']];
const WORD_GLYPH=[['one','1'],['two','2'],['three','3'],['four','4'],['for','4'],['to','2'],['too','2'],['see','C'],['be','B'],['why','Y'],['you','U'],['are','R']];
const JM_ROOTWORD_32=Object.freeze([
 'THIS','THAT','HERE','THERE','NOW','BEFORE','AFTER','AGAIN',
 'I','YOU','WE','THEY','THING','BODY','PLACE','PART',
 'BE','DO','MAKE','HAVE','GO','COME','GIVE','TAKE',
 'GOOD','BAD','SAME','DIFFERENT','YES','NO','TRUE','FALSE'
]);
const JM_ROOTWORD_GROUPS=Object.freeze([
 {name:'Point / Time / Route',words:['THIS','THAT','HERE','THERE','NOW','BEFORE','AFTER','AGAIN']},
 {name:'Relation / Participants',words:['I','YOU','WE','THEY']},
 {name:'World / Body Structure',words:['THING','BODY','PLACE','PART']},
 {name:'Core Operation',words:['BE','DO','MAKE','HAVE','GO','COME','GIVE','TAKE']},
 {name:'Value / Comparison',words:['GOOD','BAD','SAME','DIFFERENT']},
 {name:'Answer / Truth State',words:['YES','NO','TRUE','FALSE']}
]);
const JM_PREFIX_EXAMPLES=Object.freeze(['PRE','RE','UN','OVER','UNDER','ANTI','PRO','THEO','FLOW','ROUTE','ROOT','SIN']);
const JM_ROOT_SET=new Set(JM_ROOTWORD_32.map(x=>x.toLowerCase()));
function rootAction(word){const w=String(word||'').toUpperCase();if(['DO','MAKE','GO','COME','GIVE','TAKE'].includes(w))return w;if(['THIS','THAT','HERE','THERE','NOW','BEFORE','AFTER','AGAIN'].includes(w))return 'ROUTE';if(['YES','NO','TRUE','FALSE'].includes(w))return 'STATE';if(['GOOD','BAD','SAME','DIFFERENT'].includes(w))return 'RELATE';if(['I','YOU','WE','THEY'].includes(w))return 'POSITION';if(['THING','BODY','PLACE','PART'].includes(w))return 'BODY';if(w==='HAVE')return 'HOLD';return 'BE'}
function rootAnalyse(input){const exact=String(input||'').trim(),tokens=norm(exact).split(' ').filter(Boolean),hits=[],prefixHits=[];tokens.forEach((t,idx)=>{if(JM_ROOT_SET.has(t))hits.push({root:t.toUpperCase(),token:t,index:idx,action:rootAction(t),source:'JM_ROOTWORD_32'});JM_PREFIX_EXAMPLES.forEach(p=>{const pl=p.toLowerCase(),bare=t.replace(/-/g,'');if(!bare.startsWith(pl)||bare.length<=pl.length+1)return;const remainder=bare.slice(pl.length);if(JM_ROOT_SET.has(remainder))prefixHits.push({token:t,prefix:p,root:remainder.toUpperCase(),action:rootAction(remainder),root32:true});else if(remainder==='longing')prefixHits.push({token:t,prefix:p,root:'LONGING',action:'ROUTE',root32:false,sourceExample:true})})});
 // Explicit source-law examples remain available even when entered with punctuation/hyphen.
 const tight=exact.toLowerCase().replace(/[^a-z]/g,'');if(['alonging','belonging'].includes(tight)){prefixHits.push({token:exact,prefix:tight==='alonging'?'A':'BE',root:'LONGING',action:'ROUTE',root32:false,sourceExample:true})}
 const uniq=(arr,keyfn)=>{const seen=new Set();return arr.filter(x=>{const k=keyfn(x);if(seen.has(k))return false;seen.add(k);return true})};
 return{input:exact,hits:uniq(hits,x=>x.root+'|'+x.index),prefixHits:uniq(prefixHits,x=>x.prefix+'|'+x.root+'|'+x.token),lexicon:wordNearest(exact,5),pow:powNearest(exact,5),laws:['PREFIX ≠ ERASURE','PREFIX = ROUTE MODIFIER'],boundary:'JM RootWord 32 is an executable recovered continuity set. RootWord / Prefix Theory remains a distinct source-backed theory identity; this UI does not claim to reconstruct its full primary wording.'}}

const WORD_BASE_SIGNAL={'ESTATE ECHO':38,'SOUND FAMILY':62,'SOUND SNIPE':58,'PARENTHETICAL LEAD':58,'FRONT-LEAD RESEGMENT':64,'INITIAL SPINE':42,'LETTER / NUMBER FACE':42,'SPLIT WORD':30,'SPACE SHIFT':28,'WORD FLIP':24,'LETTER FLIP':18,'EMBEDDED WORD':66,'EXPLICIT SPLIT':62,'NUMBER / SOUND FACE':70,'LETTER / HOMOPHONE':72,'LETTER REVERSAL':68,'LETTER SOUND':58};
function wtoks(s){return[...new Set(norm(s).split(' ').filter(x=>x.length>2&&!WORD_STOP.has(x)))]}
function wtrigrams(s){const x='  '+norm(s)+'  ',r=new Set();for(let i=0;i<x.length-2;i++)r.add(x.slice(i,i+3));return r}
function wjac(a,b){if(!a.size&&!b.size)return 0;let n=0;a.forEach(x=>{if(b.has(x))n++});return n/(a.size+b.size-n||1)}
function wscore(q,text,context=''){const qn=norm(q),pn=norm(text);if(!qn||!pn)return 0;if(qn===pn)return 100;const qt=wtoks(q),pt=wtoks(text),qset=new Set(qt),pset=new Set(pt),shared=qt.filter(t=>pset.has(t));const tokenJac=wjac(qset,pset),tri=wjac(wtrigrams(q),wtrigrams(text));let score=shared.length*18+tokenJac*34+tri*22;if((pn.includes(qn)||qn.includes(pn))&&Math.min(qn.length,pn.length)>=5)score+=26;if(shared.length||tokenJac>.12||tri>.2){const ct=wtoks(context);score+=Math.min(8,qt.filter(t=>ct.includes(t)&&!pset.has(t)).length*2)}return Math.max(0,Math.min(100,Math.round(score)))}
function wordLexSearch(q='',limit=80){const nq=norm(q);const rows=!nq?WORD_LEXICON.map(x=>({x,s:0})):WORD_LEXICON.map(x=>{const text=[x.name,x.definition,x.readings,x.aliases,x.routes,x.provenance,x.classes,x.boundary].join(' ');return{x,s:wscore(q,text,x.provenance)}}).filter(z=>z.s>=10).sort((a,b)=>b.s-a.s);return rows.slice(0,limit).map(z=>z.x)}
function powSearch(q='',limit=80){const nq=norm(q);const rows=!nq?POW_ESTATE.map(x=>({x,s:0})):POW_ESTATE.map(x=>({x,s:wscore(q,x.pow,(x.context||'')+' '+(x.source||''))})).filter(z=>z.s>=10).sort((a,b)=>b.s-a.s);return rows.slice(0,limit).map(z=>z.x)}
function wordInputParts(clean){const m=String(clean||'').match(/^([\s\S]*?)(?:\s(?:=|→|->)\s)([\s\S]+)$/);return m?{source:m[1].trim(),declared:m[2].trim().replace(/^[.…·\s]+/,'')}:{source:String(clean||'').trim(),declared:''}}
function wsquash(s){return norm(s).replace(/\s+/g,'')}
function powNearest(q,limit=5){return POW_ESTATE.map(e=>({e,s:wscore(q,e.pow,e.context||'')})).filter(x=>x.s>=12).sort((a,b)=>b.s-a.s).slice(0,limit)}
function wordNearest(q,limit=5){return WORD_LEXICON.map(e=>({e,s:wscore(q,e.name,[e.definition,e.readings,e.aliases,e.provenance].join(' '))})).filter(x=>x.s>=12).sort((a,b)=>b.s-a.s).slice(0,limit)}
function makeWordRoutes(q){let routes=[];const add=(type,text,note,origin='MECHANICAL CANDIDATE',mechanism='',meta={})=>{if(!text)return;const key=type+'|'+String(text).toLowerCase();if(routes.some(r=>r.key===key))return;routes.push({id:'wr-'+routes.length+'-'+Date.now().toString(36),key,type,text,note,origin,mechanism,state:'open',signal:0,powContacts:[],wordContacts:[],...meta})};const clean=String(q||'').trim();if(!clean)return[];const parts=wordInputParts(clean),working=parts.source||clean,declared=parts.declared,words=working.split(/\s+/).filter(Boolean),low=norm(working);const cores=words.map(raw=>({raw,core:raw.toLowerCase().replace(/[^a-z0-9'-]/g,'').replace(/'s$/,'')})).filter(x=>x.core);cores.forEach(x=>{if(x.raw.includes('-'))add('EXPLICIT SPLIT',x.raw.replace(/-/g,' · '),'Source-marked boundary preserved.','SOURCE-MARKED OPERATION','Visible hyphen/split.')});for(let i=0;i<cores.length;i++)for(let j=0;j<cores.length;j++){if(i===j)continue;const a=cores[i].core,b=cores[j].core;if(a.length>=3&&b.length>a.length&&b.includes(a))add('EMBEDDED WORD',a+' inside '+b,'A source token remains visibly embedded in another token.','ESTATE-RECOVERED OPERATION','Source-token containment.')}
if(/\b3\.14\b/.test(working))add('NUMBER / SOUND FACE','3.14 → pi / pie','Recovered numeric/sound face.','ESTATE-RECOVERED OPERATION','Explicit 3.14 contact.');if(/\bi\s+o\s+u\b/i.test(working))add('LETTER / HOMOPHONE','I O U → I owe you','Letter names perform the spoken phrase.','ESTATE-RECOVERED OPERATION','Visible I-O-U sequence.');if(/\bABC\b/.test(working)&&/\bCBA\b/.test(working))add('LETTER REVERSAL','ABC ↔ CBA','Visible letter reversal.','ESTATE-RECOVERED OPERATION','Explicit ABC/CBA reversal.');if(/\bC\b/.test(working))add('LETTER SOUND','C ↔ see','Standalone C can carry “see” when context supports it.','ESTATE-RECOVERED OPERATION','Letter-name sound route.');if(/\bB\b/.test(working))add('LETTER SOUND','B ↔ be','Standalone B can carry “be” when context supports it.','ESTATE-RECOVERED OPERATION','Letter-name sound route.');
words.forEach(w=>{const bare=w.replace(/[^A-Za-z0-9']/g,'');if(bare.length>=5&&bare.length<=16)for(let i=2;i<=bare.length-2;i++)add('SPLIT WORD',bare.slice(0,i)+' · '+bare.slice(i),'Mechanical re-segmentation.','MECHANICAL CANDIDATE','Same letters, new boundary at '+i+'.')});
const fm=working.match(/([A-Za-z0-9']+)\s+with\s+front\s+lead\s+\(([A-Za-z0-9])\)/i);if(fm){const target=fm[1],L=fm[2],joined=((target[0]&&target[0]===target[0].toUpperCase())?L.toUpperCase():L)+target.toLowerCase();add('PARENTHETICAL LEAD',L+' + '+target+' → '+joined,'Explicit front-lead instruction.','INPUT-LED OPERATION','Parsed target + lead.',{declaredMatch:declared&&wsquash(declared)===wsquash(joined)});if(declared&&wsquash(declared)===wsquash(joined))add('FRONT-LEAD RESEGMENT',declared.replace(/\s+/g,' · '),`${L} + ${target} → ${joined} → ${declared}`,'INPUT-LED OPERATION','Lead + target then boundary shift.',{declaredMatch:true});else if(joined.length<=18)for(let i=2;i<=joined.length-2;i++)add('FRONT-LEAD RESEGMENT',joined.slice(0,i)+' · '+joined.slice(i),`${L} + ${target} → ${joined} → split at ${i}`,'INPUT-LED OPERATION','Lead + target then boundary shift.')}
if(words.length>1){add('SPACE SHIFT',words.join('·'),'Remove ordinary spacing and inspect new boundaries.','MECHANICAL CANDIDATE','Spacing changes while order is preserved.');add('WORD FLIP',[...words].reverse().join(' '),'Reverse word order.','MECHANICAL CANDIDATE','Same word components, reversed sequence.');add('INITIAL SPINE',words.map(w=>w[0]).join('').toUpperCase(),'Initial/acrostic spine.','MECHANICAL CANDIDATE','First visible character of each word.')}add('LETTER FLIP',[...working].reverse().join(''),'Character reversal.','MECHANICAL CANDIDATE','All characters reversed.');
WORD_SOUND_PAIRS.forEach(([a,b,src])=>{const reA=new RegExp('\\b'+a+'\\b','i'),reB=new RegExp('\\b'+b+'\\b','i'),hasA=reA.test(low),hasB=reB.test(low);if(!hasA&&!hasB)return;const origin=src==='POW'?'ESTATE-RECOVERED SOUND CONTACT':'ANALYZER SOUND CONTACT';if(hasA&&hasB){add('SOUND FAMILY',a+' ↔ '+b,'Both sound-family faces are already visible. '+src,origin,'Visible pair activation.',{directPair:true});return}if((a==='i'&&hasA)||(a==='ewe'&&hasB)||(a==='her'&&hasA))return;if(hasA)add('SOUND FAMILY',working.replace(reA,b),a+' ≈ '+b+'; context still decides.',origin,'Recovered phonetic family.');else add('SOUND FAMILY',working.replace(reB,a),b+' ≈ '+a+'; context still decides.',origin,'Recovered phonetic family.')});WORD_GLYPH.forEach(([a,b])=>{const re=new RegExp('\\b'+a+'\\b','ig');if(re.test(working))add('LETTER / NUMBER FACE',working.replace(re,b),a+' → '+b+' mechanical glyph/sound substitution.','MECHANICAL CANDIDATE','Spoken/letter/number face substitution.')});(working.match(/([a-zA-Z])\1{2,}/g)||[]).forEach(x=>add('SOUND SNIPE',x,'Repeated sound-body may hit before semantic explanation.','INPUT OBSERVATION','Visible repeated sound body.'));
const echoes=powNearest(clean,8);echoes.filter(x=>x.s>=24).slice(0,3).forEach(x=>add('ESTATE ECHO',x.e.pow,`Recovered POW body with ${x.s} contact strength.`,'ESTATE CONTACT','Comparison against POW v1.0.',{estateScore:x.s}));routes.forEach(r=>{r.powContacts=powNearest(r.text,4);r.wordContacts=wordNearest(r.text,4);let sig=WORD_BASE_SIGNAL[r.type]??28;if(r.origin==='ESTATE CONTACT')sig+=5;if(r.origin.includes('ANALYZER'))sig+=6;if(r.origin.includes('ESTATE-RECOVERED'))sig+=8;if(r.declaredMatch)sig+=22;if(r.directPair)sig+=18;if(r.powContacts.length)sig+=Math.min(28,r.powContacts[0].s*.28);if(r.wordContacts.length)sig+=Math.min(14,r.wordContacts[0].s*.12);r.signal=Math.max(0,Math.min(100,Math.round(sig)))});routes.sort((a,b)=>b.signal-a.signal);const caps={'ESTATE ECHO':3,'SPLIT WORD':6,'PARENTHETICAL LEAD':2,'FRONT-LEAD RESEGMENT':4,'SPACE SHIFT':1,'WORD FLIP':1,'LETTER FLIP':1,'INITIAL SPINE':1,'SOUND FAMILY':4,'LETTER / NUMBER FACE':4,'SOUND SNIPE':3,'EMBEDDED WORD':4,'EXPLICIT SPLIT':4,'NUMBER / SOUND FACE':2,'LETTER / HOMOPHONE':2,'LETTER REVERSAL':2,'LETTER SOUND':4},used={};return routes.filter(r=>{const cap=caps[r.type]??3;used[r.type]=(used[r.type]||0)+1;return used[r.type]<=cap}).slice(0,18)}
function wordGateChip(x){const cls=x.gate==='CORE_DICTIONARY_READY'||x.gate==='CORE_DICTIONARY_WORKING'?'core':x.gate==='WORD_ESTATE_IDENTITY_INDEX'?'id':'assist';return`<span class="word-chip ${cls}">${esc(x.gate||'')}</span>`}
function renderWordEntry(x){return`<div class="word-entry"><h3>${esc(x.name)}</h3><div>${wordGateChip(x)}<span class="word-chip">${esc(x.authorship||'')}</span><span class="word-chip">${esc(x.authority||'')}</span></div><p>${esc(x.definition||x.readings||'Meaning remains bounded/open in the current authority row.')}</p><div class="word-meta">${esc(x.classes||'')} · ${esc(x.provenance||'')}</div><div class="row-actions" style="margin-top:8px"><button class="small-btn" data-action="word-open" data-id="${esc(x.id)}">Inspect</button><button class="small-btn" data-action="word-use" data-id="${esc(x.id)}">Use in POW</button><button class="small-btn" data-action="word-trace" data-id="${esc(x.id)}">Trace</button></div></div>`}
function renderPowEntry(x){return`<div class="pow-entry"><h3>#${esc(x.n)} ${esc(x.pow)}</h3><div><span class="word-chip">${esc(x.type)}</span><span class="word-chip">${esc(x.prov)}</span></div><p>${esc(x.context||'')}</p><div class="word-meta">${esc(x.source||'')}</div><div class="row-actions" style="margin-top:8px"><button class="small-btn" data-action="pow-use" data-n="${esc(x.n)}">Use in Workbench</button><button class="small-btn" data-action="pow-trace" data-n="${esc(x.n)}">Trace</button></div></div>`}
function renderWordInspector(){const id=state.wordSelected;if(!id)return'';const x=WORD_LEXICON.find(w=>w.id===id);if(!x)return'';const h=x.history||{};return`<div class="word-entry word-open"><h3>${esc(x.name)}</h3><div>${wordGateChip(x)}<span class="word-chip">${esc(x.authorship||'')}</span></div><p>${esc(x.definition||'')}</p>${x.readings?`<p><b>Readings:</b> ${esc(x.readings)}</p>`:''}${x.aliases?`<p><b>Aliases:</b> ${esc(x.aliases)}</p>`:''}${x.routes?`<p><b>Routes:</b> ${esc(x.routes)}</p>`:''}<p><b>Boundary:</b> ${esc(x.boundary||'No extra boundary text in compact authority surface.')}</p><div class="word-meta">Authority: ${esc(x.authority)} · Provenance: ${esc(x.provenance)} · Source layers: ${esc(x.source_layers)}</div>${Object.keys(h).length?`<div class="word-history"><b>History / evolution contact</b>\n${esc(JSON.stringify(h,null,2))}</div>`:''}</div>`}
function renderWordLexicon(){const q=state.wordQuery||state.query||'',rows=wordLexSearch(q,80);return`<div class="word-toolbar"><input id="wordQueryInput" value="${esc(q)}" placeholder="Search 2,550 current authority rows…"><select id="wordGateFilter"><option value="">All authority surfaces</option><option>CORE_DICTIONARY_READY</option><option>CORE_DICTIONARY_WORKING</option><option>WORD_ESTATE_IDENTITY_INDEX</option><option>ASSISTED_LANGUAGE_REVIEW</option></select><button class="small-btn" data-action="word-search">Search</button></div>${renderWordInspector()}<div class="list-view" id="wordRows">${rows.map(renderWordEntry).join('')||'<div class="word-empty">No current authority row matched that route.</div>'}</div>`}
function renderPowIndex(){const q=state.wordQuery||state.query||'',rows=powSearch(q,80);return`<div class="word-toolbar"><input id="wordQueryInput" value="${esc(q)}" placeholder="Search 250 FLAZed POW entries…"><button class="small-btn" data-action="word-search">Search</button></div><div class="list-view">${rows.map(renderPowEntry).join('')||'<div class="word-empty">No POW match.</div>'}</div>`}
function renderWorkbench(){if(!state.wordRoutes.length&&state.wordInput)state.wordRoutes=makeWordRoutes(state.wordInput);const parts=wordInputParts(state.wordInput),best=state.wordRoutes[0];return`<div class="pow-grid"><div><textarea class="pow-source" id="powWorkbenchInput">${esc(state.wordInput||'')}</textarea><div class="pow-actions"><button class="primary" data-action="pow-route">ROUTE IT</button><button class="small-btn" data-action="pow-example">Load JM example</button><button class="small-btn" data-action="pow-clear">Clear</button></div><div class="word-meta">${parts.declared?'VERIFY MODE · declared output: '+esc(parts.declared):'DISCOVER MODE · candidates stay candidates until you keep them.'}</div><div style="margin-top:10px">${state.wordRoutes.map((r,i)=>`<div class="pow-route ${r.state}"><span class="route-score">${r.signal}</span><h3>#${i+1} · ${esc(r.type)}</h3><div class="word-meta">${esc(r.origin)}</div><p>${esc(r.text)}</p><div class="pow-contact">${esc(r.note)}${r.powContacts?.[0]?`<br>POW contact: #${esc(r.powContacts[0].e.n)} ${esc(r.powContacts[0].e.pow)} (${r.powContacts[0].s})`:''}${r.wordContacts?.[0]?`<br>Lexicon contact: ${esc(r.wordContacts[0].e.name)} (${r.wordContacts[0].s})`:''}</div><div class="row-actions" style="margin-top:8px"><button class="small-btn" data-action="pow-route-state" data-id="${esc(r.id)}" data-state="KEEP">Keep</button><button class="small-btn" data-action="pow-route-state" data-id="${esc(r.id)}" data-state="HOLD">Hold</button><button class="small-btn" data-action="pow-route-state" data-id="${esc(r.id)}" data-state="REJECT">Reject</button><button class="small-btn" data-action="pow-branch" data-id="${esc(r.id)}">Branch</button></div></div>`).join('')||'<div class="word-empty">Route an input to open candidate paths.</div>'}</div></div><aside><div class="word-panel"><div class="word-meta">STRONGEST LIVE ROUTE</div><h2>${best?esc(best.text):'—'}</h2><p>${best?esc(best.type+' · signal '+best.signal):'No route yet.'}</p><div class="word-rule">YOUR INPUT ≠ ESTATE MATCH ≠ MACHINE CANDIDATE ≠ YOU KEPT IT.</div></div><div class="word-panel" style="margin-top:10px"><div class="word-meta">KEEPER DECK</div>${store.wordDeck.length?store.wordDeck.slice(0,12).map(x=>`<div class="pow-entry"><b>${esc(x.state)} · ${esc(x.type)}</b><p>${esc(x.text)}</p><div class="word-meta">${esc(x.source)}</div></div>`).join(''):'<p>No kept/held routes yet.</p>'}</div></aside></div>`}
function renderRootWords(){const a=state.rootResult||rootAnalyse(state.rootInput||'');const active=new Set((a.hits||[]).map(x=>x.root));const groups=JM_ROOTWORD_GROUPS.map(g=>`<div class="root-family"><h3>${esc(g.name)}</h3><div class="root-32">${g.words.map(w=>`<button class="root-key ${active.has(w)?'active':''}" data-action="root-load" data-root="${esc(w)}">${esc(w)}</button>`).join('')}</div></div>`).join('');const hits=(a.hits||[]).map(x=>`<div class="root-hit"><div><strong>${esc(x.root)}</strong><div class="root-note">Operating handle · action ${esc(x.action)} · token position ${x.index+1}</div></div><span class="root-badge">ROOTWORD 32</span></div>`).join('');const ph=(a.prefixHits||[]).map(x=>`<div class="root-hit"><div><strong>${esc(x.prefix)} + ${esc(x.root)}</strong><div class="root-note">${esc(x.token)} · prefix aims/modifies the root route; the root remains visible.</div></div><span class="root-badge root-prefix">${x.root32?'PREFIX → ROOT 32':'PREFIX-LAW EXAMPLE'}</span></div>`).join('');const flow=(a.prefixHits||[])[0];return`<div class="root-grid"><div><div class="word-panel"><div class="word-meta">ROOT METHOD · SOURCE-CONFIRMED 32</div><h2>Find the operating root before dressing the word.</h2><p>The recovered RootWord 32 supplies irreducible handles. Prefix contact can modify direction without erasing the root. RootWord / Prefix Theory remains its own bounded theory body rather than being silently replaced by this executable lens.</p><div class="root-law"><b>PREFIX ≠ ERASURE</b><span class="root-note">A prefix changes route; it does not kill the root.</span></div><div class="root-law"><b>PREFIX = ROUTE MODIFIER</b><span class="root-note">Prefixes aim the root.</span></div></div><div class="word-toolbar" style="margin-top:12px"><input id="rootInput" value="${esc(state.rootInput||'')}" placeholder="Try REMAKE, UNDERDO, THIS, A-LONGING…"><button class="primary" data-action="root-analyse">ROOT IT</button><button class="small-btn" data-action="root-example" data-example="REMAKE">REMAKE</button><button class="small-btn" data-action="root-example" data-example="A-LONGING">A-LONGING</button><button class="small-btn" data-action="root-to-workbench">→ POW WORKBENCH</button></div>${flow?`<div class="root-flow"><span class="root-node">${esc(flow.token)}</span><span class="root-arrow">→</span><span class="root-node">PREFIX ${esc(flow.prefix)}</span><span class="root-arrow">→</span><span class="root-node">ROOT ${esc(flow.root)}</span><span class="root-arrow">→</span><span class="root-node">${esc(flow.action)}</span></div>`:''}<div class="root-result"><div class="word-meta">CURRENT ROOT CONTACT</div>${hits||'<div class="root-note">No exact RootWord 32 token detected yet.</div>'}${ph||'<div class="root-note" style="margin-top:8px">No recovered prefix→root contact detected in this input.</div>'}${a.lexicon?.[0]?`<div class="root-hit"><div><strong>Lexicon neighbour: ${esc(a.lexicon[0].e.name)}</strong><div class="root-note">Current Word Estate contact strength ${a.lexicon[0].s}.</div></div><button class="small-btn" data-action="root-to-lexicon" data-query="${esc(a.lexicon[0].e.name)}">Open</button></div>`:''}${a.pow?.[0]?`<div class="root-hit"><div><strong>POW neighbour: ${esc(a.pow[0].e.pow)}</strong><div class="root-note">Recovered POW contact strength ${a.pow[0].s}.</div></div></div>`:''}</div><div class="root-source-boundary">${esc(a.boundary)}</div></div><aside><div class="word-panel"><div class="word-meta">JM ROOTWORD 32</div><h2>32 operating handles</h2><p class="root-note">The group headings below are a navigation convenience in this descendant; they are not retroactively claimed as the historical taxonomy of RootWord / Prefix Theory.</p>${groups}</div></aside></div>`}
function renderWordTrace(){return`<div class="list-view">${store.wordTrace.length?store.wordTrace.map(x=>`<div class="word-entry"><h3>${esc(x.state)} · ${esc(x.type||x.kind||'WORD TRACE')}</h3><p>${esc(x.text||x.name||'')}</p><div class="word-meta">BEFORE: ${esc(x.before||'')}<br>AFTER: ${esc(x.after||'')}<br>${esc(x.time||'')}</div></div>`).join(''):'<div class="word-empty">No Word Estate / POW trace yet.</div>'}</div>`}
function renderWords(){const audit=WORD_META.counts||{};let body=state.wordTab==='lexicon'?renderWordLexicon():state.wordTab==='pow'?renderPowIndex():state.wordTab==='rootwords'?renderRootWords():state.wordTab==='workbench'?renderWorkbench():renderWordTrace();return`<section class="word-hero"><div class="word-panel"><div class="word-meta">JM WORD ESTATE × POW × ROOTWORDS × UNIFIED BROWSER</div><h2>Find it. Root it. Turn it. Trace it.</h2><p>Lexicon v1.5 remains authority. POW v1.0 remains the recovered specialised corpus. RootWords exposes the recovered 32 operating handles and prefix-route laws. The Workbench turns contact into candidate routes without flattening any of those authority layers.</p><div class="law-strip"><span class="law">MESH ≠ MERGE</span><span class="law">PREFIX ≠ ERASURE</span><span class="law">SOURCE BEFORE SUGGESTION</span><span class="law">NO DING, NO CLAIM</span></div></div><div class="word-stats"><div class="word-stat"><strong>${WORD_LEXICON.length}</strong><span>v1.5 authority rows</span></div><div class="word-stat"><strong>${POW_ESTATE.length}</strong><span>FLAZed POW entries</span></div><div class="word-stat"><strong>${JM_ROOTWORD_32.length}</strong><span>RootWord handles</span></div><div class="word-stat"><strong>${store.wordDeck.length}</strong><span>kept / held here</span></div></div></section><div class="word-tabs"><button class="word-tab ${state.wordTab==='lexicon'?'active':''}" data-action="word-tab" data-tab="lexicon">LEXICON v1.5</button><button class="word-tab ${state.wordTab==='pow'?'active':''}" data-action="word-tab" data-tab="pow">POW v1.0</button><button class="word-tab ${state.wordTab==='rootwords'?'active':''}" data-action="word-tab" data-tab="rootwords">ROOTWORDS 32</button><button class="word-tab ${state.wordTab==='workbench'?'active':''}" data-action="word-tab" data-tab="workbench">WORKBENCH</button><button class="word-tab ${state.wordTab==='trace'?'active':''}" data-action="word-tab" data-tab="trace">TRACE</button></div>${body}`}
function wordTracePush(x){store.wordTrace.unshift({...x,time:new Date().toISOString()});store.wordTrace=store.wordTrace.slice(0,300);saveStore();trace('WORD_ROUTE',{target:x.text||x.name||'',source:x.kind||'Word Estate / POW',state:x.state||''})}
// --- END WORD ESTATE / POW / ROOTWORDS WORKBENCH ---
function renderMain(){const el=$('#mainBody');if(state.mode==='unified')el.innerHTML=state.query?renderUnified():renderHome();else if(state.mode==='web')el.innerHTML=renderWeb();else if(state.mode==='estate')el.innerHTML=renderEstate();else if(state.mode==='words')el.innerHTML=renderWords();else if(state.mode==='library')el.innerHTML=renderLibrary();else if(state.mode==='local')el.innerHTML=renderLocal();else if(state.mode==='map')el.innerHTML=renderMap();else if(state.mode==='rooms')el.innerHTML=renderRooms();else if(state.mode==='saved')el.innerHTML=renderSaved();else if(state.mode==='receipts')el.innerHTML=renderReceipts();else if(state.mode==='settings')el.innerHTML=renderSettings();bindMapNodes()}
function renderMainOnly(){if(['unified','web'].includes(state.mode)){renderMain();renderInspector()}}
function renderHome(){return `<section class="hero"><div class="hero-copy"><h2>One search.<br>Whole contact field.<br>One living route.</h2><p>JM3232 now treats one search as a shared contact event across the public web, embedded Radius Lexicon, current Word Estate v1.5 authority surface, FLAZed POW v1.0, RootWords 32, registered Estate bodies, Library carriers, rooms, saved routes and files you deliberately import. FlowTalk preserves the source. FTR finds, traces and routes. Visualang shows the relation. TraceBox leaves the receipt.</p><div class="hero-cta"><button class="primary" data-action="focus-search">Start unified search</button><button class="secondary" data-action="import-local">Import local bodies</button><button class="secondary" data-action="open-legacy">Open Navigator v0.1.1</button></div><div class="law-strip">${['SOURCE BEFORE SUMMARY','MEANING BEFORE MATCHING','HOST ≠ SOURCE','PRIVATE BY DEFAULT','MESH ≠ MERGE','NO DING, NO CLAIM'].map(x=>`<span class="law">${x}</span>`).join('')}</div></div><div class="hero-side"><div class="metric blue"><span class="label">Radius Lexicon</span><strong>${LEXICON.length}</strong><span class="muted">governed entries embedded</span></div><div class="metric blue"><span class="label">Word Estate v1.5</span><strong>${WORD_LEXICON.length}</strong><span class="muted">current authority rows embedded</span></div><div class="metric amber"><span class="label">Estate Registry</span><strong>${store.bodies.length}</strong><span class="muted">source bodies and mount targets</span></div><div class="metric green"><span class="label">Local Proof</span><strong>${store.receipts.length}</strong><span class="muted">receipts stored on this device</span></div><div class="metric green"><span class="label">Host Authority</span><strong>NONE</strong><span class="muted">carriers and adapters stay replaceable</span></div><div class="metric amber"><span class="label">Library Mesh</span><strong>${allLibraryRows().length}</strong><span class="muted">routed live/preserved entries</span></div></div></section><div class="section-title"><h2>Quick JM tools</h2><span>Every control opens a working route.</span></div><div class="tool-grid">${toolCards()}</div><div class="section-title"><h2>Mounted foundation</h2><span>Current body versus donor body remains visible.</span></div><div class="list-view">${store.bodies.slice(0,8).map(renderBodyRow).join('')}</div>`}
function toolCards(){return [{a:'quick-search',q:'FlowTalk',n:'FlowTalk',d:'Speech → source meaning'},{a:'mode',m:'words',n:'Word Estate + POW',d:'Current lexicon authority + playable wordwork'},{a:'quick-search',q:'Radius Lexicon',n:'Radius Lexicon',d:'Meaning + radius + relations'},{a:'mode',m:'map',n:'Visualang Map',d:'See connections and false twins'},{a:'mode',m:'receipts',n:'TraceBox',d:'Receipt + proof + recovery'},{a:'mode',m:'library',n:'Library Ledger',d:'Nine-house body + carrier mesh'},{a:'import-local',n:'Local Intake',d:'Import files without moving them'},{a:'add-body',n:'Stringline Registry',d:'Connect a sovereign body'},{a:'export-savepack',n:'SavePack',d:'Carry your local browser state'},{a:'new-tab',n:'New Tab',d:'Open another working route'}].map(x=>`<button class="tool" data-action="${x.a}" ${x.q?`data-query="${esc(x.q)}"`:''}${x.m?` data-mode="${x.m}"`:''}><strong>${esc(x.n)}</strong><span>${esc(x.d)}</span></button>`).join('')}
function roomSearch(q='',limit=5){const nq=norm(q);if(!nq)return[];return (store.rooms||[]).map(r=>({r,s:(norm(r.name)===nq?100:0)+(norm(r.name).includes(nq)?50:0)+(norm(r.description||'').includes(nq)?20:0)})).filter(x=>x.s>0).sort((a,b)=>b.s-a.s).slice(0,limit).map(x=>x.r)}
function savedSearch(q='',limit=5){const nq=norm(q);if(!nq)return[];return (store.bookmarks||[]).map((b,i)=>({b,i,s:(norm(b.title||'')===nq?100:0)+(norm(b.title||'').includes(nq)?50:0)+(norm([b.description,b.kind,b.url,b.id].join(' ')).includes(nq)?18:0)})).filter(x=>x.s>0).sort((a,b)=>b.s-a.s).slice(0,limit)}
function wholeContactField(q){const lex=wordLexSearch(q,5),pow=powSearch(q,5),root=rootAnalyse(q),radius=estateSearch(q,5),bodies=bodySearch(q,4),library=librarySearch(q,5),rooms=roomSearch(q,4),saved=savedSearch(q,4);return{q,lex,pow,root,radius,bodies,library,rooms,saved,local:state.localResults.slice(0,5),web:state.webResults.slice(0,6)}}
function rootContactLabel(a){if(a.prefixHits?.length){const x=a.prefixHits[0];return `${x.prefix} + ${x.root} → ${x.action}`}if(a.hits?.length){const x=a.hits[0];return `${x.root} → ${x.action}`}return 'No exact RootWord 32 hit — route remains open.'}
function renderWholeContactField(cf){const lex=cf.lex[0],pow=cf.pow[0],root=cf.root,workspace=[...cf.rooms.map(x=>`<span class="workspace-hit">ROOM · <b>${esc(x.name)}</b></span>`),...cf.saved.map(x=>`<span class="workspace-hit">SAVED · <b>${esc(x.b.title||x.b.url||'route')}</b></span>`)].join('');const activeRoots=(root.hits?.length||0)+(root.prefixHits?.length||0);return `<section class="contact-spine"><div class="contact-spine-head"><div><div class="contact-kind">WHOLE CONTACT FIELD · v1.5.2 · AIRBORNE</div><h2>${esc(cf.q)}</h2><p>One query contacts distinct organs. Matching does not merge their authority.</p></div><button class="small-btn" data-action="unified-open-workbench">TURN IT →</button></div><div class="contact-route"><span class="contact-node hot">SOURCE</span><span class="contact-arrow">→</span><span class="contact-node">LEXICON ${cf.lex.length}</span><span class="contact-arrow">→</span><span class="contact-node">ROOT ${activeRoots}</span><span class="contact-arrow">→</span><span class="contact-node">POW ${cf.pow.length}</span><span class="contact-arrow">→</span><span class="contact-node">ESTATE ${cf.radius.length+cf.bodies.length}</span><span class="contact-arrow">→</span><span class="contact-node">LIBRARY ${cf.library.length}</span><span class="contact-arrow">→</span><span class="contact-node">LOCAL ${cf.local.length}</span><span class="contact-arrow">→</span><span class="contact-node">WEB ${state.remoteStatus==='loading'?'…':cf.web.length}</span><span class="contact-arrow">→</span><span class="contact-node hot">TRACE</span></div><div class="contact-grid"><article class="contact-card"><div class="contact-kind">WORD AUTHORITY · LEXICON v1.5</div>${lex?`<h3>${esc(lex.name)}</h3><p>${esc(clip(lex.definition||lex.boundary||'Source-backed lexical contact.',180))}</p><div class="contact-actions"><button class="small-btn" data-action="unified-open-lexicon">OPEN LEXICON</button><button class="small-btn" data-action="unified-open-workbench">USE IN WORKBENCH</button></div>`:'<p>No current v1.5 authority match above threshold.</p>'}</article><article class="contact-card"><div class="contact-kind">OPERATING ROOT · ROOTWORDS 32</div><h3>${esc(rootContactLabel(root))}</h3>${root.prefixHits?.slice(0,2).map(x=>`<div class="contact-mini"><b>${esc(x.token)}</b><span>PREFIX ${esc(x.prefix)} · ROOT ${esc(x.root)} · ${esc(x.action)}</span></div>`).join('')||root.hits?.slice(0,2).map(x=>`<div class="contact-mini"><b>${esc(x.root)}</b><span>ROOTWORD 32 · ${esc(x.action)}</span></div>`).join('')||'<p>No exact root hit; inspect as a provisional root field.</p>'}<div class="contact-actions"><button class="small-btn" data-action="unified-open-root">ROOT IT</button></div></article><article class="contact-card"><div class="contact-kind">WORDPLAY CONTACT · POW v1.0</div>${pow?`<h3>${esc(pow.pow)}</h3><p>${esc(clip(pow.context||pow.source||'Recovered POW contact.',180))}</p><div class="contact-actions"><button class="small-btn" data-action="unified-open-pow">OPEN POW</button><button class="small-btn" data-action="unified-open-workbench">TURN QUERY</button></div>`:'<p>No recovered POW match above threshold. Mechanical candidate routes remain available in Workbench.</p>'}</article></div>${workspace?`<div class="contact-kind">WORKSPACE CONTACT</div><div class="workspace-strip">${workspace}</div>`:''}</section>`}
function renderUnified(){const cf=wholeContactField(state.query),web=cf.web,estate=[...state.estateResults.slice(0,5),...bodySearch(state.query,3).map(b=>({...b,_body:true})),...librarySearch(state.query,4).map(r=>({...r,_library:true}))].slice(0,10),local=state.localResults.slice(0,7),rootCount=(cf.root.hits?.length||0)+(cf.root.prefixHits?.length||0);return `<div class="results-summary"><b>Results for “${esc(state.query)}”</b><div class="realm-counts"><span class="realm-count web">Internet ${state.remoteStatus==='loading'?'…':web.length}</span><span class="realm-count estate">Estate ${estate.length}</span><span class="realm-count estate">Words ${cf.lex.length} · POW ${cf.pow.length} · Root ${rootCount}</span><span class="realm-count estate">Library ${librarySearch(state.query,999).length}</span><span class="realm-count local">Local ${local.length}</span></div></div>${renderWholeContactField(cf)}<div class="realm-grid"><section class="realm web"><div class="realm-head"><span>◎ Internet</span><span>${state.remoteStatus==='loading'?'Searching':'Live + handoff'}</span></div>${state.remoteStatus==='loading'?`<div class="load-state">Checking enabled public connectors…</div>`:`<div class="realm-list">${web.length?web.map(renderWebResult).join(''):`<div class="empty">No live connector result yet. Open the full web search route.</div>`}</div>`}</section><section class="realm estate"><div class="realm-head"><span>◆ Estate + Library</span><span>Radius + Registry + carriers</span></div><div class="realm-list">${estate.length?estate.map(x=>x._library?renderLibraryResult(x):(x._body?renderBodyResult(x):renderEstateResult(x))).join(''):`<div class="empty">No Estate match inside the present meaning radius.</div>`}</div></section><section class="realm local"><div class="realm-head"><span>▣ Local</span><span>Imported + built-in</span></div><div class="realm-list">${local.length?local.map(renderLocalResult).join(''):`<div class="empty">Import files to add your local bodies to this field.</div>`}</div></section></div><div class="section-title"><h2>Route tools</h2><span>Find → Root → Turn → Trace → Recover</span></div><div class="tool-grid">${toolCards()}</div>`}
function renderWeb(){const q=state.query;return q?`<div class="results-summary"><b>Internet routes for “${esc(q)}”</b><div class="realm-counts"><span class="realm-count web">${state.remoteStatus==='loading'?'Searching':state.webResults.length+' routes'}</span></div></div><div class="list-view">${state.remoteStatus==='loading'?`<div class="empty">Checking public connectors…</div>`:state.webResults.map(renderWebListRow).join('')}</div>`:`<div class="hero-copy"><h2>Explore outward.<br>String inward.</h2><p>Use Wikipedia and GitHub as live no-key connectors, or hand the query to your chosen search engine through the device browser. External sites are not falsely claimed as rendered inside this shell.</p><div class="hero-cta"><button class="primary" data-action="focus-search">Search the Internet</button><button class="secondary" data-action="open-url" data-url="https://jmisjustme-estate.pages.dev/">Open Estate home</button></div></div>`}
function renderEstate(){const q=state.query;const es=q?state.estateResults:LEXICON.slice(0,30);const bodies=q?bodySearch(q,20):store.bodies;const libs=q?librarySearch(q,24):allLibraryRows().slice(0,18);return `<div class="results-summary"><b>${q?`Estate matches for “${esc(q)}”`:'Estate foundation bodies'}</b><div class="realm-counts"><span class="realm-count estate">Lexicon ${es.length}</span><span class="realm-count estate">Bodies ${bodies.length}</span><span class="realm-count estate">Library ${libs.length}</span></div></div><div class="section-title"><h2>Library mesh</h2><span>Physical carrier ≠ logical body ≠ authority state</span></div><div class="list-view">${libs.map(r=>`<div class="list-row"><div><h3>${esc(r.name)}</h3><p>${esc(r.houseTitle||r.house)} · ${esc(r.kind)} · ${esc(r.status)}</p><p class="mono" style="font-size:10px;margin-top:5px">${esc(r.physicalPath||'')}</p></div><div class="row-actions"><button class="small-btn" data-action="inspect-library" data-id="${esc(r.id)}">Inspect</button><button class="small-btn" data-action="mode" data-mode="library">Ledger</button></div></div>`).join('')}</div><div class="section-title"><h2>Radius Lexicon</h2><span>Dictionary · thesaurus · pictograph · code · trace</span></div><div class="list-view">${es.map(renderEstateListRow).join('')}</div><div class="section-title"><h2>Stringline body registry</h2><span>Connected without flattening.</span></div><div class="list-view">${bodies.map(renderBodyRow).join('')}</div>`}
function renderLocal(){const docs=state.query?state.localResults:state.localDocs.length?state.localDocs:[...BUILTIN_DOCS];return `<div class="hero-copy" style="padding:18px"><h2 style="font-size:34px">Local means chosen.</h2><p>Import text, Markdown, JSON, HTML, Cading, QZ, CSV and related readable files. The browser builds a virtual search map; it does not move, rename or delete your originals.</p><div class="hero-cta"><button class="primary" data-action="import-local">Choose files</button><button class="secondary" data-action="export-savepack">Export SavePack</button></div></div><div class="section-title"><h2>Local bodies</h2><span>${docs.length} available to this browser profile</span></div><div class="list-view">${docs.map(renderLocalRow).join('')}</div>`}
function renderRooms(){return `<div class="list-view">${store.rooms.map(r=>`<div class="list-row"><div><h3>${esc(r.name)}</h3><p>${esc(r.description||'No description')}</p><p class="mono" style="margin-top:6px">${r.items.length} mounted item${r.items.length===1?'':'s'}</p></div><div class="row-actions"><button class="small-btn" data-action="open-room" data-id="${esc(r.id)}">Open</button><button class="small-btn" data-action="delete-room" data-id="${esc(r.id)}">Delete</button></div></div>`).join('')||`<div class="empty">No rooms yet.</div>`}</div>`}
function renderSaved(){return `<div class="section-title" style="margin-top:0"><h2>Bookmarks</h2><span>${store.bookmarks.length} saved</span></div><div class="list-view">${store.bookmarks.map((b,i)=>`<div class="list-row"><div><h3>${esc(b.title)}</h3><p>${esc(b.description||b.kind||'Saved route')}</p><p class="route mono">${esc(b.url||b.id||'')}</p></div><div class="row-actions"><button class="small-btn" data-action="open-bookmark" data-index="${i}">Open</button><button class="small-btn" data-action="remove-bookmark" data-index="${i}">Remove</button></div></div>`).join('')||`<div class="empty">Nothing saved yet. Use Save on any result.</div>`}</div><div class="section-title"><h2>History</h2><span>${store.history.length} traces</span></div><div class="list-view">${store.history.slice(0,80).map(h=>`<div class="list-row"><div><h3>${esc(h.target)}</h3><p>${esc(h.action)} · ${esc(h.source)} · ${new Date(h.time).toLocaleString('en-GB')}</p></div><div class="row-actions"><button class="small-btn" data-action="history-reopen" data-target="${esc(h.target)}">Re-open</button></div></div>`).join('')||`<div class="empty">No history yet.</div>`}</div>`}
function renderReceipts(){return `<div class="list-view">${store.receipts.slice(0,200).map(r=>`<div class="list-row"><div><h3>${esc(r.action)}</h3><p>${esc(r.target||r.query||'Browser action')}</p><p class="mono" style="font-size:10px;margin-top:5px">${esc(r.id)} · ${new Date(r.time).toLocaleString('en-GB')}</p></div><div class="row-actions"><button class="small-btn" data-action="inspect-receipt" data-id="${esc(r.id)}">Inspect</button></div></div>`).join('')||`<div class="empty">No receipts yet. Search, open, save or import something.</div>`}</div>`}
function renderSettings(){const s=store.settings,legacy=store.legacyNavigatorV011,lc=legacy?.counts||{};const legacyStatus=legacy?`<div class="inspect-block" style="margin-bottom:14px"><h4>Canonical-door state recovery ✓</h4><p class="inspect-def">Recovered JM3232 Navigator v0.1.1 state by copy-forward migration. Legacy keys remain untouched. ${Number(lc.bookmarks||0)} bookmarks · ${Number(lc.history||0)} history routes · ${Number(lc.receipts||0)} receipts · ${Number(lc.stringlineProjects||0)} Stringline projects retained.</p><p class="route mono">${esc(legacy.source||'legacy source')} · ${esc(legacy.migratedAt||'')}</p></div>`:`<div class="inspect-block" style="margin-bottom:14px"><h4>Canonical-door migration ready</h4><p class="inspect-def">No legacy v0.1.1 browser-profile state was present in this carrier at load time. Legacy SavePack import remains supported.</p></div>`;return `${legacyStatus}<div class="form-grid"><div class="field"><label>Public web handoff</label><select id="setEngine"><option value="duckduckgo" ${s.engine==='duckduckgo'?'selected':''}>DuckDuckGo</option><option value="google" ${s.engine==='google'?'selected':''}>Google</option><option value="bing" ${s.engine==='bing'?'selected':''}>Bing</option></select></div><div class="field"><label>Appearance</label><select id="setTheme"><option value="dark" ${s.theme==='dark'?'selected':''}>Dark</option><option value="light" ${s.theme==='light'?'selected':''}>Light</option></select></div><div class="field"><label>Live public connectors</label><select id="setRemote"><option value="true" ${s.remote?'selected':''}>Enabled</option><option value="false" ${!s.remote?'selected':''}>Disabled — handoff only</option></select></div><div class="field"><label>Receipt detail</label><select id="setReceipt"><option value="full" ${s.receiptDetail==='full'?'selected':''}>Full route receipt</option><option value="compact" ${s.receiptDetail==='compact'?'selected':''}>Compact receipt</option></select></div><div class="field full"><label>Source and privacy boundary</label><textarea readonly>This pass prefers browser-profile storage when the host exposes it. On opaque Android content:// or file:// hosts where origin storage is blocked, the AutoContact proof uses a carrier-resident URL-fragment capsule across reload; that fallback is a continuity/recovery proof, not a claim of durable profile storage. Imported files are copied into IndexedDB only when that API is available and after you choose them; originals are not moved or deleted. Public searches leave the shell through enabled live connectors or your device browser. There is no account backend, hidden synchronisation, ad network or tracking layer in this package. Source authority remains JM. OpenAI, ChatGPT, GitHub, Cloudflare, localhost, Android, browser engines and future hosts are replaceable carriers/adapters; none is the canonical identity of this app.</textarea></div></div><div class="section-title"><h2>Local data controls</h2><span>Recovery before loss.</span></div><div class="tool-grid"><button class="tool" data-action="export-savepack"><strong>Export SavePack</strong><span>Carry bookmarks, rooms, receipts, registry and local docs.</span></button><button class="tool" data-action="import-savepack"><strong>Import SavePack</strong><span>Merge a prior JM3232 browser state.</span></button><button class="tool" data-action="clear-browser-data"><strong>Reset browser state</strong><span>Clear local browser state after confirmation.</span></button><button class="tool" data-action="open-about"><strong>Build boundary</strong><span>See exactly what v1.0 claims and does not claim.</span></button></div>`}
function renderMap(){const e=state.selectedKind==='estate'?state.selected:(state.query?estateSearch(state.query,1)[0]:LEXICON.find(x=>x.canonical_name==='Radius Lexicon'));if(!e)return`<div class="empty">Select an Estate entry first.</div>`;const rels=[...(e.relationships?.meaning_neighbours||[]).map(x=>({name:x,type:'related'})),...(e.relationships?.bridge_terms||[]).map(x=>({name:x,type:'bridge'})),...(e.relationships?.false_twins||[]).map(x=>({name:x,type:'false'}))].slice(0,12);const w=900,h=520,cx=450,cy=260,r=190;let lines='',nodes='';rels.forEach((x,i)=>{const a=Math.PI*2*i/Math.max(rels.length,1)-Math.PI/2,xx=cx+Math.cos(a)*r,yy=cy+Math.sin(a)*r;lines+=`<line class="map-line" x1="${cx}" y1="${cy}" x2="${xx}" y2="${yy}"/>`;nodes+=`<g class="map-node" data-action="map-search" data-query="${esc(x.name)}"><circle class="${x.type==='false'?'map-false':x.type==='bridge'?'map-bridge':'map-related'}" cx="${xx}" cy="${yy}" r="47"/><text class="map-label" x="${xx}" y="${yy}" text-anchor="middle"><tspan x="${xx}" dy="0">${esc(clip(x.name,15))}</tspan></text></g>`});return `<div class="map-wrap"><svg viewBox="0 0 ${w} ${h}" role="img" aria-label="Visualang relationship map"><g>${lines}</g>${nodes}<g class="map-node" data-action="inspect-estate" data-id="${esc(e.id)}"><circle class="map-center" cx="${cx}" cy="${cy}" r="70"/><text class="map-label" x="${cx}" y="${cy-5}" text-anchor="middle"><tspan x="${cx}">${esc(clip(e.canonical_name,20))}</tspan><tspan x="${cx}" dy="17" style="fill:#b7d6e9;font-size:9px">${esc(e.shelf||'Estate')}</tspan></text></g></svg></div><div class="section-title"><h2>Map key</h2><span>Blue = neighbour · Purple = bridge · Red = false twin</span></div><div class="token-row">${rels.map(x=>`<button class="token ${x.type}" data-action="quick-search" data-query="${esc(x.name)}">${esc(x.name)}</button>`).join('')}</div>`}
function renderEstateResult(e){return `<article class="result" data-action="inspect-estate" data-id="${esc(e.id)}"><h3>${esc(e.canonical_name)}</h3><p>${esc(clip(e.definitions?.plain||e.definitions?.public_safe||'',130))}</p><div class="route">${esc(e.shelf||'Radius Lexicon')} · ${esc(e.claim?.claim_status||'')}</div><div class="result-actions"><button data-action="inspect-estate" data-id="${esc(e.id)}">Inspect</button><button data-action="save-estate" data-id="${esc(e.id)}">Save</button><button data-action="compare-estate" data-id="${esc(e.id)}">Compare</button><button data-action="trace-item" data-id="${esc(e.id)}" data-kind="estate">Trace</button></div></article>`}
function renderEstateListRow(e){return `<div class="list-row"><div><h3>${esc(e.canonical_name)}</h3><p>${esc(clip(e.definitions?.plain||e.definitions?.public_safe||'',220))}</p><p class="mono" style="font-size:10px;margin-top:5px">${esc(e.shelf||'')} · ${esc(e.authority?.confirmation_status||'')}</p></div><div class="row-actions"><button class="small-btn" data-action="inspect-estate" data-id="${esc(e.id)}">Inspect</button><button class="small-btn" data-action="save-estate" data-id="${esc(e.id)}">Save</button><button class="small-btn" data-action="compare-estate" data-id="${esc(e.id)}">Compare</button></div></div>`}
function renderBodyResult(b){return `<article class="result" data-action="inspect-body" data-id="${esc(b.id)}"><h3>${esc(b.name)}</h3><p>${esc(clip(b.role,130))}</p><div class="route">${esc(b.status)}${b.route?' · '+esc(b.route):''}</div><div class="result-actions"><button data-action="inspect-body" data-id="${esc(b.id)}">Inspect</button>${b.route?`<button data-action="open-body" data-id="${esc(b.id)}">Open</button>`:''}<button data-action="save-body" data-id="${esc(b.id)}">Save</button><button data-action="trace-item" data-id="${esc(b.id)}" data-kind="body">Trace</button></div></article>`}
function renderBodyRow(b){return `<div class="list-row"><div><h3>${esc(b.name)}</h3><p>${esc(b.role)}</p><p class="mono" style="font-size:10px;margin-top:5px">${esc(b.type)} · ${esc(b.status)}</p></div><div class="row-actions"><button class="small-btn" data-action="inspect-body" data-id="${esc(b.id)}">Inspect</button>${b.route?`<button class="small-btn" data-action="open-body" data-id="${esc(b.id)}">Open</button>`:''}<button class="small-btn" data-action="save-body" data-id="${esc(b.id)}">Save</button></div></div>`}
function renderWebResult(r){return `<article class="result" data-action="inspect-web" data-id="${esc(r.id)}"><h3>${esc(r.title)}</h3><p>${esc(clip(r.description,130))}</p><div class="route">${esc(r.source)}${r.meta?' · '+esc(r.meta):''}</div><div class="result-actions"><button data-action="open-web" data-id="${esc(r.id)}">Open</button><button data-action="inspect-web" data-id="${esc(r.id)}">Inspect</button><button data-action="save-web" data-id="${esc(r.id)}">Save</button><button data-action="compare-web" data-id="${esc(r.id)}">Compare</button></div></article>`}
function renderWebListRow(r){return `<div class="list-row"><div><h3>${esc(r.title)}</h3><p>${esc(r.description)}</p><p class="mono" style="font-size:10px;margin-top:5px">${esc(r.source)} · ${esc(r.url)}</p></div><div class="row-actions"><button class="small-btn" data-action="open-web" data-id="${esc(r.id)}">Open</button><button class="small-btn" data-action="inspect-web" data-id="${esc(r.id)}">Inspect</button><button class="small-btn" data-action="save-web" data-id="${esc(r.id)}">Save</button></div></div>`}
function renderLocalResult(d){return `<article class="result" data-action="inspect-local" data-id="${esc(d.id)}"><h3>${esc(d.name)}</h3><p>${esc(clip(d.text||'',130))}</p><div class="route">${esc(d.type||'local file')} · ${esc(d.updated||d.importedAt||'')}</div><div class="result-actions"><button data-action="inspect-local" data-id="${esc(d.id)}">Inspect</button><button data-action="save-local" data-id="${esc(d.id)}">Save</button><button data-action="compare-local" data-id="${esc(d.id)}">Compare</button><button data-action="trace-item" data-id="${esc(d.id)}" data-kind="local">Trace</button></div></article>`}
function renderLocalRow(d){const built=BUILTIN_DOCS.some(x=>x.id===d.id);return `<div class="list-row"><div><h3>${esc(d.name)}</h3><p>${esc(clip(d.text||'',230))}</p><p class="mono" style="font-size:10px;margin-top:5px">${esc(d.type||'local file')} · ${esc(d.size?formatBytes(d.size):'built-in')}</p></div><div class="row-actions"><button class="small-btn" data-action="inspect-local" data-id="${esc(d.id)}">Read</button><button class="small-btn" data-action="save-local" data-id="${esc(d.id)}">Save</button>${!built?`<button class="small-btn" data-action="delete-local" data-id="${esc(d.id)}">Remove copy</button>`:''}</div></div>`}
function formatBytes(n){if(!n)return'0 B';const u=['B','KB','MB','GB'];let i=0;while(n>=1024&&i<u.length-1){n/=1024;i++}return n.toFixed(i?1:0)+' '+u[i]}
function renderInspector(){const el=$('#inspectContent');const x=state.selected;if(!x){el.innerHTML=`<div class="inspect-empty"><p>Select an Internet, Estate or local result to inspect its source, meaning radius, relationships, route and claim boundary.</p><p><strong>FlowTalk:</strong> wording may change while source meaning survives.</p><p><strong>TraceBox:</strong> inspection does not equal proof; the route remains visible.</p></div>`;return}if(state.selectedKind==='estate')el.innerHTML=inspectEstate(x);else if(state.selectedKind==='body')el.innerHTML=inspectBody(x);else if(state.selectedKind==='web')el.innerHTML=inspectWeb(x);else if(state.selectedKind==='local')el.innerHTML=inspectLocal(x);else if(state.selectedKind==='library')el.innerHTML=inspectLibrary(x);else if(state.selectedKind==='receipt')el.innerHTML=inspectReceipt(x);if(window.innerWidth<1181&&state.inspectorRequested)$('#inspector').classList.add('open')}
function inspectEstate(e){const rel=e.relationships||{},mr=e.meaning_radius||{},vf=e.visual_face||{},lin=e.lineage||[];return `<div class="inspect-kicker">${esc(e.entry_type||'Estate entry')} · ${esc(e.authority?.confirmation_status||'')}</div><h2 class="inspect-name">${esc(e.canonical_name)}</h2><p class="muted" style="font-size:11px">${esc(e.shelf||'Radius Lexicon')}</p><p class="inspect-def">${esc(e.definitions?.plain||'')}</p>${e.definitions?.jm_source&&e.definitions.jm_source!==e.definitions.plain?`<div class="inspect-block"><h4>JM / source definition</h4><p class="inspect-def">${esc(e.definitions.jm_source)}</p></div>`:''}<div class="inspect-block"><h4>Meaning radius</h4><p class="inspect-def">${esc(mr.core_use||e.use_law||'No radius note recorded.')}</p>${(mr.misuse_edges||[]).length?`<div class="token-row">${mr.misuse_edges.map(x=>`<span class="token false">${esc(x)}</span>`).join('')}</div>`:''}</div><div class="inspect-block"><h4>Meaning neighbours</h4><div class="token-row">${(rel.meaning_neighbours||[]).map(x=>`<button class="token" data-action="quick-search" data-query="${esc(x)}">${esc(x)}</button>`).join('')||'<span class="muted">None recorded</span>'}</div></div><div class="inspect-block"><h4>Bridge terms</h4><div class="token-row">${(rel.bridge_terms||[]).map(x=>`<button class="token bridge" data-action="quick-search" data-query="${esc(x)}">${esc(x)}</button>`).join('')||'<span class="muted">None recorded</span>'}</div></div>${(rel.false_twins||[]).length?`<div class="inspect-block"><h4>False twins</h4><div class="token-row">${rel.false_twins.map(x=>`<button class="token false" data-action="quick-search" data-query="${esc(x)}">${esc(x)}</button>`).join('')}</div></div>`:''}<div class="inspect-block"><h4>Visualang / pictograph</h4><div class="pictograph">${esc(vf.text_glyph||'◎')}\n${esc(vf.route_diagram||vf.shape_logic||'No visual route recorded.')}</div></div><div class="inspect-block"><h4>Source lineage</h4>${lin.slice(0,6).map(x=>`<p class="inspect-def" style="font-size:11px"><strong>${esc(x.source_file||'Source')}</strong><br>${esc(x.locator||'')} · ${esc(x.authority||'')}</p>`).join('')||'<span class="muted">No lineage visible</span>'}</div><div class="inspect-actions"><button data-action="save-estate" data-id="${esc(e.id)}">Save</button><button data-action="compare-estate" data-id="${esc(e.id)}">Compare</button><button data-action="mode" data-mode="map">Map relations</button><button data-action="trace-item" data-id="${esc(e.id)}" data-kind="estate">Trace receipt</button></div>`}
function inspectBody(b){return `<div class="inspect-kicker">${esc(b.type)} · ${esc(b.status)}</div><h2 class="inspect-name">${esc(b.name)}</h2><p class="inspect-def">${esc(b.role)}</p><div class="inspect-block"><h4>Project string</h4><p class="inspect-def">${esc(b.project)}</p></div><div class="inspect-block"><h4>Authority</h4><p class="inspect-def">${esc(b.authority)}</p></div><div class="inspect-block"><h4>Route</h4><p class="route mono">${esc(b.route||'No public/local route mounted in this package.')}</p></div><div class="inspect-actions">${b.route?`<button data-action="open-body" data-id="${esc(b.id)}">Open</button>`:''}<button data-action="save-body" data-id="${esc(b.id)}">Save</button><button data-action="room-add-item" data-id="${esc(b.id)}" data-kind="body">Add to room</button><button data-action="trace-item" data-id="${esc(b.id)}" data-kind="body">Trace</button></div>`}
function inspectWeb(r){return `<div class="inspect-kicker">${esc(r.source)} · Public route</div><h2 class="inspect-name">${esc(r.title)}</h2><p class="inspect-def">${esc(r.description)}</p><div class="inspect-block"><h4>Rendering boundary</h4><p class="inspect-def">This route opens through your device browser. The Unified Browser retains the search, source, bookmark and TraceBox receipt without pretending to own the external page renderer.</p></div><div class="inspect-block"><h4>URL</h4><p class="route mono">${esc(r.url)}</p></div><div id="readerArea"></div><div class="inspect-actions"><button data-action="open-web" data-id="${esc(r.id)}">Open</button>${r.kind==='wikipedia'?`<button data-action="reader-web" data-id="${esc(r.id)}">Reader</button>`:''}<button data-action="save-web" data-id="${esc(r.id)}">Save</button><button data-action="trace-item" data-id="${esc(r.id)}" data-kind="web">Trace</button></div>`}
function inspectLocal(d){return `<div class="inspect-kicker">${esc(d.type||'Local body')} · Local copy</div><h2 class="inspect-name">${esc(d.name)}</h2><p class="muted" style="font-size:11px">${esc(d.path||'Chosen local file / built-in document')} ${d.size?' · '+formatBytes(d.size):''}</p><div class="inspect-block"><h4>Readable content</h4><p class="inspect-def" style="white-space:pre-wrap">${esc(clip(d.text||'',5000))}</p></div><div class="inspect-actions"><button data-action="save-local" data-id="${esc(d.id)}">Save</button><button data-action="compare-local" data-id="${esc(d.id)}">Compare</button><button data-action="room-add-item" data-id="${esc(d.id)}" data-kind="local">Add to room</button><button data-action="trace-item" data-id="${esc(d.id)}" data-kind="local">Trace</button></div>`}
function inspectReceipt(r){return `<div class="inspect-kicker">TraceBox receipt</div><h2 class="inspect-name">${esc(r.action)}</h2><p class="inspect-def">${esc(r.target||r.query||'Browser action')}</p><div class="inspect-block"><h4>Time</h4><p>${new Date(r.time).toLocaleString('en-GB')}</p></div><div class="inspect-block"><h4>Full receipt</h4><pre class="mono" style="white-space:pre-wrap;font-size:10px;color:#a9c2d6">${esc(JSON.stringify(r,null,2))}</pre></div>`}
function renderMobileDock(){$('#mobileDock').innerHTML=[['unified','⌘','Unified'],['estate','◆','Estate'],['words','✦','Words'],['library','▦','Library'],['saved','★','Saved']].map(([m,i,l])=>`<button class="${state.mode===m?'active':''}" data-action="mode" data-mode="${m}"><span>${i}</span>${l}</button>`).join('')}
function renderCompare(){const d=$('#compareDrawer');if(!store.compare.length){d.classList.add('hidden');d.innerHTML='';return}d.innerHTML=`<div class="compare-head"><strong>Compare field · ${store.compare.length}/3</strong><div><button class="small-btn" data-action="compare-clear">Clear</button><button class="small-btn" data-action="compare-close">Close</button></div></div><div class="compare-grid">${store.compare.map((x,i)=>`<div class="compare-card"><h3>${esc(x.title)}</h3><p>${esc(x.description||'')}</p><p class="mono" style="font-size:10px">${esc(x.kind||'')}</p><button data-action="compare-remove" data-index="${i}">Remove</button></div>`).join('')}</div>`;if(d.dataset.open==='true')d.classList.remove('hidden');else d.classList.add('hidden')}
function addCompare(x){if(store.compare.some(y=>y.key===x.key)){toast('Already in the compare field.');return}if(store.compare.length>=3){toast('Compare holds three bodies at once.','bad');return}store.compare.push(x);saveStore();$('#compareDrawer').dataset.open='true';trace('COMPARE_ADD',{target:x.title,source:x.kind});renderCompare();toast('Added to compare field.')}
function addBookmark(x){if(store.bookmarks.some(y=>y.key===x.key)){toast('Already saved.');return}store.bookmarks.unshift({...x,savedAt:new Date().toISOString()});saveStore();trace('BOOKMARK_SAVE',{target:x.title,source:x.kind});toast('Saved locally.')}
function modal(title,body,foot=''){const root=$('#modalRoot');root.innerHTML=`<div class="modal"><div class="modal-head"><strong>${esc(title)}</strong><button class="icon-btn" style="width:32px;height:32px;min-width:32px" data-action="close-modal">×</button></div><div class="modal-body">${body}</div>${foot?`<div class="modal-foot">${foot}</div>`:''}</div>`;root.classList.remove('hidden')}
function closeModal(){$('#modalRoot').classList.add('hidden');$('#modalRoot').innerHTML=''}
function openHelp(){modal('Command map',`<div class="list-view">${[['/web query','Search public connectors and open the full web route.'],['/estate term','Search Radius Lexicon, Estate registry and Library mesh.'],['/library words','Search the Library Master Ledger across house, subject, family, path, carrier and authority state.'],['/local words','Search deliberately imported local bodies.'],['/open URL','Open a public route through the device browser.'],['/body name','Register a sovereign Estate body.'],['/room name','Create a room/workspace.'],['/trace','Open TraceBox receipts.'],['/home','Return to the Unified Browser home.']].map(x=>`<div class="list-row"><div><h3 class="mono">${esc(x[0])}</h3><p>${esc(x[1])}</p></div></div>`).join('')}</div>`)}
function openAbout(){modal('JM3232 Unified Browser v1.5.2 — Canonical Door Migration',`<p class="inspect-def"><strong>Source owner:</strong> ${esc(META.source_owner)}</p><p class="inspect-def"><strong>Assistant build support:</strong> ${esc(META.assistant_support)}</p><p class="inspect-def"><strong>Status:</strong> ${esc(META.status)}</p><div class="inspect-block"><h4>What this pass completes</h4><p class="inspect-def">A working mobile-first descendant joining public Internet routes, Radius Lexicon, current Word Estate v1.5 authority, POW Master v1.0, the recovered JM RootWord 32 operating lens, prefix-route laws, a POW workbench, registered Estate bodies, the nine-house Library Master Ledger, chosen local files, bookmarks, history, tabs, rooms, Visualang maps, compare fields, SavePacks and TraceBox receipts.</p></div><div class="inspect-block"><h4>Truth boundary</h4><p class="inspect-def">${esc(META.boundary)}</p></div><div class="inspect-block"><h4>Legacy source</h4><p class="inspect-def">JM3232 Navigator v0.1.1 remains the live donor body and public legacy route. It is preserved, not erased.</p></div>`,`<button class="small-btn" data-action="open-legacy">Open Navigator v0.1.1</button><button class="primary" data-action="close-modal">Understood</button>`)}
function openBodyModal(seed=''){modal('Add a sovereign body',`<div class="form-grid"><div class="field"><label>Body name</label><input id="bodyName" value="${esc(seed)}"></div><div class="field"><label>Body type</label><input id="bodyType" placeholder="Theory, game, app, file…"></div><div class="field"><label>Project string</label><input id="bodyProject" placeholder="JMStudios, FTR, Language Estate…"></div><div class="field"><label>Lifecycle state</label><select id="bodyStatus"><option>REGISTERED / MOUNT PENDING</option><option>ACTIVE WORKING BODY</option><option>LIVE PUBLIC</option><option>FROZEN CURRENT</option><option>DONOR BODY</option><option>PARKED</option></select></div><div class="field full"><label>Current role</label><textarea id="bodyRole" placeholder="What does this body contribute?"></textarea></div><div class="field full"><label>Route / URL (optional)</label><input id="bodyRoute" placeholder="https://… or leave unmounted"></div></div>`,`<button class="small-btn" data-action="close-modal">Cancel</button><button class="primary" data-action="save-body-form">Stringle body</button>`)}
function openRoomModal(seed=''){modal('Create a room',`<div class="form-grid"><div class="field full"><label>Room name</label><input id="roomName" value="${esc(seed)}"></div><div class="field full"><label>Purpose</label><textarea id="roomDesc" placeholder="What does this room hold without flattening?"></textarea></div></div>`,`<button class="small-btn" data-action="close-modal">Cancel</button><button class="primary" data-action="save-room-form">Create room</button>`)}
function chooseRoomForItem(id,kind){modal('Add to room',`<div class="list-view">${store.rooms.map(r=>`<button class="list-row" style="color:inherit;text-align:left;cursor:pointer" data-action="room-confirm-item" data-room="${esc(r.id)}" data-id="${esc(id)}" data-kind="${esc(kind)}"><div><h3>${esc(r.name)}</h3><p>${r.items.length} items</p></div></button>`).join('')}</div>`,`<button class="small-btn" data-action="create-room">Create new room</button>`)}
async function openRoom(id){const r=store.rooms.find(x=>x.id===id);if(!r)return;const items=r.items.map(key=>store.bodies.find(x=>x.id===key)||LEXICON.find(x=>x.id===key)||state.localDocs.find(x=>x.id===key)||BUILTIN_DOCS.find(x=>x.id===key)).filter(Boolean);modal(r.name,`<p class="inspect-def">${esc(r.description||'')}</p><div class="list-view">${items.map(x=>x.canonical_name?renderEstateListRow(x):x.role?renderBodyRow(x):renderLocalRow(x)).join('')||'<div class="empty">This room is empty.</div>'}</div>`)}
function applyTheme(){document.body.classList.toggle('light',store.settings.theme==='light')}
function download(name,data,type='application/json'){const blob=data instanceof Blob?data:new Blob([data],{type});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},500)}
async function exportSavePack(){const docs=await dbAll();const pack={format:'JM3232_UNIFIED_BROWSER_SAVEPACK',version:'1.1',exportedAt:new Date().toISOString(),browser:{tabs:store.tabs,activeTab:store.activeTab,bookmarks:store.bookmarks,history:store.history,receipts:store.receipts,rooms:store.rooms,bodies:store.bodies,settings:store.settings,libraryRows:store.libraryRows,libraryMeta:store.libraryMeta,wordDeck:store.wordDeck,wordTrace:store.wordTrace,legacyNavigatorV011:store.legacyNavigatorV011},localDocs:docs};download('JM3232_UNIFIED_BROWSER_SAVEPACK_'+new Date().toISOString().slice(0,10)+'.json',JSON.stringify(pack,null,2));trace('EXPORT_SAVEPACK',{target:pack.exportedAt,source:'local browser profile'});toast('SavePack exported.')}
async function importSavePack(file){try{const pack=JSON.parse(await file.text());if(pack.format==='JM3232_UNIFIED_BROWSER_SAVEPACK'){const b=pack.browser||{};['tabs','bookmarks','history','receipts','rooms','bodies','libraryRows','wordDeck','wordTrace'].forEach(k=>{if(Array.isArray(b[k]))store[k]=b[k]});if(b.activeTab)store.activeTab=b.activeTab;if(b.settings)store.settings={...store.settings,...b.settings};if(b.libraryMeta)store.libraryMeta=b.libraryMeta;if(b.legacyNavigatorV011)store.legacyNavigatorV011=b.legacyNavigatorV011;for(const d of pack.localDocs||[])await dbPut(d);saveStore();state.localDocs=await dbAll();trace('IMPORT_SAVEPACK',{target:file.name,source:'chosen file'});toast('SavePack imported.');render();return}if(pack.schema==='JM.NavigatorSavePack/1.0'){mergeLegacyV011IntoStore(pack.navigator||{},pack.receipts||[],pack.stringline||null,'legacy JM.NavigatorSavePack/1.0');saveStore();trace('IMPORT_LEGACY_NAVIGATOR_SAVEPACK',{target:file.name,source:'JM3232 Navigator v0.1.1'});toast('Legacy Navigator SavePack recovered into this browser.');render();return}throw new Error('Wrong format')}catch(e){toast('Could not import that SavePack.','bad')}}
async function importLocalFiles(files){let added=0;for(const file of files){try{let text=await file.text();const max=2_000_000;const clipped=text.length>max;text=text.slice(0,max);await dbPut({id:'local-'+uid(),name:file.name,type:file.type||file.name.split('.').pop()||'local file',size:file.size,lastModified:file.lastModified,importedAt:new Date().toISOString(),text,clipped,path:'Chosen from this device'});added++;trace('IMPORT_LOCAL_BODY',{target:file.name,source:'user-selected file',size:file.size,clipped})}catch(e){}}state.localDocs=await dbAll();state.localResults=await localSearch(state.query||'');toast(added+' local bod'+(added===1?'y':'ies')+' imported.');setMode('local')}
function bindMapNodes(){}
async function readerWeb(id){const r=state.webResults.find(x=>x.id===id);if(!r||r.kind!=='wikipedia')return;const area=$('#readerArea');if(area)area.innerHTML='<div class="inspect-block"><h4>Reader</h4><p class="inspect-def">Loading readable summary…</p></div>';try{const title=r.title.replace(/ /g,'_');const d=await fetch('https://en.wikipedia.org/api/rest_v1/page/summary/'+encodeURIComponent(title)).then(x=>x.json());if(area)area.innerHTML=`<div class="inspect-block"><h4>Reader</h4><p class="inspect-def">${esc(d.extract||'No summary returned.')}</p></div>`;trace('READER_FETCH',{target:r.url,source:'Wikipedia REST summary'})}catch(e){if(area)area.innerHTML='<div class="inspect-block"><h4>Reader</h4><p class="inspect-def">Reader summary could not be fetched. The public page can still open through your device browser.</p></div>'}}
function itemBy(kind,id){if(kind==='estate')return LEXICON.find(x=>x.id===id);if(kind==='body')return store.bodies.find(x=>x.id===id);if(kind==='web')return state.webResults.find(x=>x.id===id);if(kind==='local')return state.localDocs.find(x=>x.id===id)||BUILTIN_DOCS.find(x=>x.id===id);if(kind==='library')return allLibraryRows().find(x=>x.id===id)}
function bookmarkFrom(kind,x){if(kind==='estate')return{key:'estate:'+x.id,kind,title:x.canonical_name,description:x.definitions?.plain,id:x.id};if(kind==='body')return{key:'body:'+x.id,kind,title:x.name,description:x.role,url:x.route,id:x.id};if(kind==='web')return{key:'web:'+x.url,kind,title:x.title,description:x.description,url:x.url,id:x.id};if(kind==='library')return libraryBookmark(x);return{key:'local:'+x.id,kind:'local',title:x.name,description:clip(x.text||'',180),id:x.id}}
function compareFrom(kind,x){const b=bookmarkFrom(kind,x);return{...b,description:b.description||'',key:b.key}}
function select(kind,id){const x=itemBy(kind,id);if(!x)return;state.selected=x;state.selectedKind=kind;state.inspectorRequested=true;renderInspector();$('#inspector').scrollTop=0}
function saveKind(kind,id){const x=itemBy(kind,id);if(x)addBookmark(bookmarkFrom(kind,x));renderSidebar()}
function compareKind(kind,id){const x=itemBy(kind,id);if(x)addCompare(compareFrom(kind,x))}
function traceKind(kind,id){const x=itemBy(kind,id);if(!x)return;const b=bookmarkFrom(kind,x);trace('TRACE_ITEM',{target:b.title,source:kind,id});toast('TraceBox receipt stored.')}
function openBookmark(i){const b=store.bookmarks[i];if(!b)return;if(b.url)openExternal(b.url,'bookmark');else if(b.kind==='estate')select('estate',b.id);else if(b.kind==='body')select('body',b.id);else if(b.kind==='local')select('local',b.id);else if(b.kind==='library')select('library',b.id)}
function handleAction(btn,e){
  const a=btn.dataset.action,id=btn.dataset.id,kind=btn.dataset.kind;
  switch(a){
    case 'unified-open-lexicon': state.wordQuery=state.query; state.wordTab='lexicon'; setMode('words'); break;
    case 'unified-open-root': state.rootInput=state.query; state.rootResult=rootAnalyse(state.query); state.wordTab='rootwords'; setMode('words'); break;
    case 'unified-open-pow': state.wordQuery=state.query; state.wordTab='pow'; setMode('words'); break;
    case 'unified-open-workbench': state.wordInput=state.query; state.wordRoutes=makeWordRoutes(state.query); state.wordTab='workbench'; wordTracePush({kind:'WHOLE_CONTACT_TO_WORKBENCH',state:'CONTACT',text:state.query,before:'unified search',after:state.wordRoutes.length+' candidates'}); setMode('words'); break;
    case 'root-analyse': state.rootInput=($('#rootInput')?.value||'').trim(); state.rootResult=rootAnalyse(state.rootInput); wordTracePush({kind:'ROOTWORDS',state:'ROOT',text:state.rootInput,before:state.rootInput,after:(state.rootResult.hits.length+' roots · '+state.rootResult.prefixHits.length+' prefix contacts')}); render(); break;
    case 'root-load': state.rootInput=btn.dataset.root||''; state.rootResult=rootAnalyse(state.rootInput); state.wordTab='rootwords'; render(); break;
    case 'root-example': state.rootInput=btn.dataset.example||'REMAKE'; state.rootResult=rootAnalyse(state.rootInput); state.wordTab='rootwords'; render(); break;
    case 'root-to-workbench': state.wordInput=($('#rootInput')?.value||state.rootInput||'').trim(); state.wordRoutes=makeWordRoutes(state.wordInput); state.wordTab='workbench'; wordTracePush({kind:'ROOT_TO_WORKBENCH',state:'CONTACT',text:state.wordInput,before:'root lens',after:state.wordRoutes.length+' POW candidates'}); render(); break;
    case 'root-to-lexicon': state.wordQuery=btn.dataset.query||state.rootInput||''; state.query=state.wordQuery; state.wordTab='lexicon'; render(); break;
    case 'word-tab': state.wordTab=btn.dataset.tab||'lexicon'; render(); break;
    case 'word-search': state.wordQuery=($('#wordQueryInput')?.value||'').trim(); state.query=state.wordQuery; render(); break;
    case 'word-open': state.wordSelected=id; render(); break;
    case 'word-use': {const x=WORD_LEXICON.find(w=>w.id===id);if(x){state.wordInput=x.name;state.wordTab='workbench';state.wordRoutes=makeWordRoutes(state.wordInput);wordTracePush({kind:'LEXICON_TO_WORKBENCH',state:'CONTACT',name:x.name,before:x.name,after:'workbench route opened'});render()}break;}
    case 'word-trace': {const x=WORD_LEXICON.find(w=>w.id===id);if(x)wordTracePush({kind:'LEXICON',state:'TRACE',name:x.name,before:x.provenance,after:x.authority});toast('Word Estate trace stored.');render();break;}
    case 'pow-use': {const x=POW_ESTATE.find(p=>String(p.n)===String(btn.dataset.n));if(x){state.wordInput=x.pow;state.wordTab='workbench';state.wordRoutes=makeWordRoutes(state.wordInput);wordTracePush({kind:'POW_TO_WORKBENCH',state:'CONTACT',text:x.pow,before:x.source,after:'workbench route opened'});render()}break;}
    case 'pow-trace': {const x=POW_ESTATE.find(p=>String(p.n)===String(btn.dataset.n));if(x)wordTracePush({kind:'POW',state:'TRACE',text:x.pow,before:x.source,after:x.prov});toast('POW trace stored.');render();break;}
    case 'pow-route': state.wordInput=($('#powWorkbenchInput')?.value||'').trim(); state.wordRoutes=makeWordRoutes(state.wordInput); wordTracePush({kind:'WORKBENCH',state:'ROUTE',text:state.wordInput,before:state.wordInput,after:state.wordRoutes.length+' candidates'}); render(); break;
    case 'pow-example': state.wordInput='Gossip with front lead (e)nergy = … Egos sip'; state.wordRoutes=makeWordRoutes(state.wordInput); state.wordTab='workbench'; render(); break;
    case 'pow-clear': state.wordInput=''; state.wordRoutes=[]; render(); break;
    case 'pow-route-state': {const r=state.wordRoutes.find(x=>x.id===id);if(r){r.state=(btn.dataset.state||'HOLD').toLowerCase();const rec={id:uid(),state:btn.dataset.state||'HOLD',type:r.type,text:r.text,source:state.wordInput,signal:r.signal};if(rec.state!=='REJECT')store.wordDeck.unshift(rec);store.wordDeck=store.wordDeck.slice(0,120);wordTracePush({kind:'WORKBENCH_DECISION',state:rec.state,type:r.type,text:r.text,before:state.wordInput,after:rec.state});saveStore();render()}break;}
    case 'pow-branch': {const r=state.wordRoutes.find(x=>x.id===id);if(r){const before=state.wordInput;state.wordInput=r.text;state.wordRoutes=makeWordRoutes(r.text);wordTracePush({kind:'WORKBENCH_BRANCH',state:'BRANCH',type:r.type,text:r.text,before,after:r.text});render()}break;}
    case 'mode': setMode(btn.dataset.mode); break;
    case 'home': state.query=''; setMode('unified'); break;
    case 'focus-search': $('#searchInput').focus(); break;
    case 'quick-search':
    case 'map-search': { const q=btn.dataset.query; $('#searchInput').value=q; runSearch(q,'unified'); break; }
    case 'new-tab': { const t={id:uid(),title:'New tab',query:'',mode:'unified',history:[]}; store.tabs.push(t); store.activeTab=t.id; state.query=''; state.mode='unified'; saveStore(); render(); break; }
    case 'activate-tab': { store.activeTab=id; const t=activeTab(); state.query=t.query||''; state.mode=t.mode||'unified'; $('#searchInput').value=state.query; saveStore(); state.estateResults=state.query?estateSearch(state.query):[]; localSearch(state.query).then(r=>{state.localResults=r;render()}); render(); break; }
    case 'close-tab': { if(e)e.stopPropagation(); if(store.tabs.length<=1)break; const idx=store.tabs.findIndex(x=>x.id===id); store.tabs.splice(idx,1); if(store.activeTab===id)store.activeTab=store.tabs[Math.max(0,idx-1)].id; saveStore(); render(); break; }
    case 'back': { const prev=state.viewStack.pop(); if(prev){state.mode=prev;render()}else window.history.back(); break; }
    case 'voice': startVoice(); break;
    case 'toggle-theme': store.settings.theme=store.settings.theme==='dark'?'light':'dark'; saveStore(); render(); break;
    case 'open-about': openAbout(); break;
    case 'close-inspector': state.inspectorRequested=false; $('#inspector').classList.remove('open'); break;
    case 'close-modal': closeModal(); break;
    case 'add-body': openBodyModal(); break;
    case 'save-body-form': { const name=$('#bodyName').value.trim(); if(!name){toast('Body name is required.','bad');break} const b={id:'body-'+uid(),name,type:$('#bodyType').value.trim()||'Estate body',project:$('#bodyProject').value.trim()||'Unassigned',status:$('#bodyStatus').value,role:$('#bodyRole').value.trim()||'Role not yet stated.',route:$('#bodyRoute').value.trim(),authority:'USER-REGISTERED BODY'}; store.bodies.unshift(b); saveStore(); trace('REGISTER_BODY',{target:name,source:'Stringline registry',status:b.status}); closeModal(); setMode('estate'); toast('Body Stringled without flattening.'); break; }
    case 'import-local': $('#localFileInput').click(); break;
    case 'delete-local': dbDelete(id).then(async()=>{state.localDocs=await dbAll();state.localResults=await localSearch(state.query||'');trace('REMOVE_LOCAL_COPY',{target:id,source:'IndexedDB'});render();toast('Local browser copy removed; original file untouched.')}); break;
    case 'clear-local': if(confirm('Clear all imported browser copies? Original files will not be touched.'))dbClear().then(async()=>{state.localDocs=[];state.localResults=[];trace('CLEAR_LOCAL_COPIES',{source:'IndexedDB'});render();toast('Imported browser copies cleared.')}); break;
    case 'export-savepack': exportSavePack(); break;
    case 'export-ledger-json': exportLibraryLedger('json'); break;
    case 'export-ledger-csv': exportLibraryLedger('csv'); break;
    case 'import-ledger': $('#ledgerInput').click(); break;
    case 'library-house': state.libraryHouse=btn.dataset.house||'ALL'; setMode('library',false); break;
    case 'clear-imported-ledger': if(confirm('Clear imported Library ledger rows? The embedded authoritative seed remains.')){store.libraryRows=[];store.libraryMeta={importedAt:null,source:'embedded authoritative seed'};saveStore();trace('CLEAR_IMPORTED_LIBRARY_ROWS',{source:'Estate Library Mesh'});render();} break;
    case 'owner-device-selftest': runOwnerDeviceSelfTest(); break;
    case 'clear-selftest': store.ownerDeviceSelfTest=null; saveStore(); render(); break;
    case 'import-savepack': $('#savepackInput').click(); break;
    case 'inspect-estate': select('estate',id); break;
    case 'inspect-body': select('body',id); break;
    case 'inspect-web': select('web',id); break;
    case 'inspect-local': select('local',id); break;
    case 'inspect-library': select('library',id); break;
    case 'save-estate': saveKind('estate',id); break;
    case 'save-body': saveKind('body',id); break;
    case 'save-web': saveKind('web',id); break;
    case 'save-local': saveKind('local',id); break;
    case 'save-library': saveKind('library',id); break;
    case 'compare-estate': compareKind('estate',id); break;
    case 'compare-web': compareKind('web',id); break;
    case 'compare-local': compareKind('local',id); break;
    case 'trace-item': traceKind(kind,id); break;
    case 'open-body': { const b=itemBy('body',id); if(b?.route)openExternal(b.route,b.name); break; }
    case 'open-web': { const r=itemBy('web',id); if(r)openExternal(r.url,r.source); break; }
    case 'open-url': openExternal(btn.dataset.url,'explicit route'); break;
    case 'open-legacy': openExternal('https://jmisjustme-estate.pages.dev/navigator/','JM3232 Navigator v0.1.1 legacy body'); break;
    case 'reader-web': readerWeb(id); break;
    case 'compare-open': $('#compareDrawer').dataset.open='true'; renderCompare(); break;
    case 'compare-close': $('#compareDrawer').dataset.open='false'; renderCompare(); break;
    case 'compare-clear': store.compare=[]; saveStore(); renderCompare(); break;
    case 'compare-remove': store.compare.splice(Number(btn.dataset.index),1); saveStore(); renderCompare(); break;
    case 'create-room': openRoomModal(); break;
    case 'save-room-form': { const name=$('#roomName').value.trim(); if(!name){toast('Room name is required.','bad');break} store.rooms.push({id:'room-'+uid(),name,description:$('#roomDesc').value.trim(),items:[]}); saveStore(); trace('CREATE_ROOM',{target:name,source:'Rooms'}); closeModal(); setMode('rooms'); break; }
    case 'open-room': openRoom(id); break;
    case 'delete-room': if(confirm('Delete this room? Bodies remain in their source locations.')){store.rooms=store.rooms.filter(x=>x.id!==id);saveStore();trace('DELETE_ROOM',{target:id});render()} break;
    case 'room-add-item': chooseRoomForItem(id,kind); break;
    case 'room-confirm-item': { const r=store.rooms.find(x=>x.id===btn.dataset.room); if(r&&!r.items.includes(id)){r.items.push(id);saveStore();trace('ROOM_ADD_ITEM',{target:id,source:r.name});toast('Added to '+r.name)} closeModal(); break; }
    case 'open-bookmark': openBookmark(Number(btn.dataset.index)); break;
    case 'remove-bookmark': store.bookmarks.splice(Number(btn.dataset.index),1); saveStore(); render(); break;
    case 'history-reopen': { const target=btn.dataset.target; const u=validURL(target); if(u)openExternal(u,'history'); else{$('#searchInput').value=target;runSearch(target,'unified')} break; }
    case 'inspect-receipt': { const r=store.receipts.find(x=>x.id===id); if(r){state.selected=r;state.selectedKind='receipt';renderInspector()} break; }
    case 'export-receipts': download('JM3232_TRACEBOX_RECEIPTS.json',JSON.stringify({version:'1.1',receipts:store.receipts},null,2)); break;
    case 'clear-receipts': if(confirm('Clear local receipts? Export first if they matter.')){store.receipts=[];saveStore();render()} break;
    case 'clear-browser-data': if(confirm('Reset JM3232 local browser state? Imported local browser copies will also be cleared.')){localStorage.removeItem(STORE_KEY);dbClear().then(()=>location.reload())} break;
  }
}

function startVoice(){const SR=window.SpeechRecognition||window.webkitSpeechRecognition;if(!SR){toast('Speech recognition is not available in this browser. Type route remains active.','bad');return}if(state.voice){state.voice.stop();state.voice=null;return}const r=new SR();r.lang='en-GB';r.interimResults=true;r.continuous=false;r.onresult=e=>{let text='';for(let i=e.resultIndex;i<e.results.length;i++)text+=e.results[i][0].transcript;$('#searchInput').value=text;if(e.results[e.results.length-1].isFinal)runSearch(text,'unified')};r.onerror=()=>toast('Speech route stopped. Typed search remains available.','bad');r.onend=()=>state.voice=null;state.voice=r;r.start();toast('Listening… Speak the source, not the spelling.')}
function applySettings(){store.settings.engine=$('#setEngine')?.value||store.settings.engine;store.settings.theme=$('#setTheme')?.value||store.settings.theme;store.settings.remote=($('#setRemote')?.value||String(store.settings.remote))==='true';store.settings.receiptDetail=$('#setReceipt')?.value||store.settings.receiptDetail;saveStore();applyTheme()}
document.addEventListener('click',e=>{const btn=e.target.closest('[data-action]');if(btn)handleAction(btn,e)});
document.addEventListener('change',e=>{if(['setEngine','setTheme','setRemote','setReceipt'].includes(e.target.id)){applySettings();render()}if(e.target.id==='libraryHouseFilter'){state.libraryHouse=e.target.value;renderMain();}if(e.target.id==='wordGateFilter'){const gate=e.target.value;document.querySelectorAll('#wordRows .word-entry').forEach(el=>{const txt=el.textContent||'';el.style.display=!gate||txt.includes(gate)?'':'none'})}});
$('#searchForm').addEventListener('submit',e=>{e.preventDefault();runSearch($('#searchInput').value,$('#scopeSelect').value)});
$('#localFileInput').addEventListener('change',e=>{importLocalFiles([...e.target.files]);e.target.value=''})
$('#savepackInput').addEventListener('change',e=>{if(e.target.files[0])importSavePack(e.target.files[0]);e.target.value=''})
$('#ledgerInput').addEventListener('change',e=>{if(e.target.files[0])importLibraryLedger(e.target.files[0]);e.target.value=''})
window.addEventListener('keydown',e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='l'){e.preventDefault();$('#searchInput').focus();$('#searchInput').select()}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='t'){e.preventDefault();handleAction({dataset:{action:'new-tab'}},e)}if(e.key==='Escape'){closeModal();$('#inspector').classList.remove('open');$('#compareDrawer').dataset.open='false';renderCompare()}})
window.addEventListener('resize',()=>{if(window.innerWidth>=1181)$('#inspector').classList.remove('open')})
if('serviceWorker'in navigator&&location.protocol.startsWith('http'))navigator.serviceWorker.register('./service-worker.js').catch(()=>{});
(async()=>{state.localDocs=await dbAll();let resumed=resumeOwnerDeviceSelfTest();const carrier=readAutoContactCarrier();render();trace('APP_OPEN',{target:'JM3232 Unified Browser v1.4.0 Whole Contact Field',source:'OPEN_FIRST',boundary:META.boundary,hostClass:hostContinuityClass()});saveStore();if(resumed){setTimeout(()=>showOwnerDeviceSelfTestResult(resumed),180)}else if(carrier&&carrier.phase==='PASS'){store.ownerDeviceSelfTest=carrier;setTimeout(()=>showOwnerDeviceSelfTestResult(carrier),180)}else if(META.auto_owner_device_selftest&&!store.ownerDeviceSelfTest&&!carrier){setTimeout(()=>runOwnerDeviceSelfTest(),650)}})();
</script>
</body>
</html>