# FREEZE LOCK ANCHOR — RouteOS JM-Native Kernel v1.5A

## Crown candidate

**PrimitiveRoute — MAX DING pending frozen-head revalidation**

## Locks

1. The v1.5A source, generated unit, generation record, compiler, singular-seam integrator, adversarial suite and exact-name machine enforcement are the authoritative PrimitiveRoute body.
2. The fixed I/O-wait port, primitive signatures, bytewise semantics and six retained office names may not change under the v1.5A name.
3. The inherited handwritten primitive block must remain absent and all five residue signatures remain zero.
4. The PrimitiveRoute identity must appear exactly once in QEMU.
5. All six callable PrimitiveRoute boundaries must survive as exact ELF symbols; suffix or substring matches are invalid.
6. The frozen v1.4A ProtectionRoute and all earlier offices remain independently authoritative and intact.
7. Failed routes remain provenance only.
8. PR `#74` remains draft and unmerged unless merging is chosen separately.
9. Final DING requires the freeze head and permanent anchor to complete the full workflow matrix.

## Intended permanent anchor

`anchor/routeos-kernel-jm-native-v1-5a-primitiveroute-ding-pass`
