# RouteOS Kernel JM-Native v1.6A — DispatchRoute

Status: **CONSTRUCTION**

Parent will be the frozen v1.5A PrimitiveRoute crown.

This lane removes the standalone handwritten interrupt dispatcher and generates the timer, controlled syscall, fault and unhandled-vector routing office while preserving the inherited scheduler, gate and recovery bodies.

Claim boundary: the fixed vectors and legacy PIC EOI path used by this proof kernel; not arbitrary vector policies, nested interrupt safety, SMP dispatch or all exception families.
