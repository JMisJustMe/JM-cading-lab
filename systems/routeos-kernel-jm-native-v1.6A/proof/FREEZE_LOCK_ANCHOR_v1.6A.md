# FREEZE LOCK ANCHOR — RouteOS JM-Native Kernel v1.6A

**DispatchRoute — MAX DING pending frozen-head revalidation**

1. The v1.6A source, generated unit, generation record, compiler, integrator, tests and exact-name enforcement are authoritative.
2. Timer vector 32, syscall vector 128, invalid-opcode vector 6 and legacy PIC EOI values may not change under v1.6A.
3. The old handwritten dispatcher body remains absent.
4. DispatchRoute identity appears exactly once in QEMU.
5. All three exact ELF boundaries survive.
6. The corrected v1.5A anchor and all earlier offices remain intact.
7. PR #75 remains draft and unmerged.
8. Final DING requires frozen-head matrix revalidation.

Permanent anchor: `anchor/routeos-kernel-jm-native-v1-6a-dispatchroute-ding-pass`
