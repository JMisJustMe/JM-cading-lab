# FREEZE LOCK ANCHOR — RouteOS JM-Native Kernel v1.7A

**EntryRoute — MAX DING pending frozen-head revalidation**

1. The v1.7A source, split generated carriers, record, compiler, integrator, tests and exact-name enforcement are authoritative.
2. The fixed Multiboot magic/info argument order and IgnitionBody handoff may not change under v1.7A.
3. The handwritten C entry wrapper remains absent.
4. EntryRoute identity appears exactly once in QEMU.
5. Both exact ELF boundaries survive.
6. The v1.6A DispatchRoute anchor and every earlier office remain intact.
7. PR #76 remains draft and unmerged.
8. Final DING requires frozen-head matrix revalidation.

Permanent anchor: `anchor/routeos-kernel-jm-native-v1-7a-entryroute-ding-pass`
