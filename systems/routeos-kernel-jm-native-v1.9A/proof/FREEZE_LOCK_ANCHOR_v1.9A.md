# FREEZE LOCK ANCHOR — RouteOS JM-Native Kernel v1.9A

## Crown candidate

**OrchestrationRoute — MAX DING pending frozen-head revalidation**

## Locks

1. The v1.9A source, generated unit, generation record, compiler, singular-seam integrator, adversarial suite and exact-name machine enforcement are the authoritative OrchestrationRoute body.
2. The frozen activation order—SerialRoute, KernelContractRoute, PrimitiveRoute, EntryRoute, authority receipt, memory probe, DescriptorInstall, VectorRoute, user installation, InterruptController and first frame entry—may not change under the v1.9A name.
3. The inherited IgnitionBody implementation remains absent, while its exact frozen v0.7A identity remains explicitly emitted once.
4. OrchestrationRoute and every v1.5A–v1.8A identity must appear exactly once in QEMU.
5. `jm_generated_ignitionbody` must survive as an exact ELF symbol; substring or state-object matches are invalid.
6. Body 2's deliberate invalid opcode must be contained once, RecoveryBody must select the safe body once, and Body 1 must continue afterward.
7. The v1.8A KernelContractRoute anchor and every earlier body remain independently authoritative and intact.
8. Failed routes remain provenance only.
9. PR `#78` remains draft and unmerged unless merging is chosen separately.
10. Final DING requires the freeze head and permanent anchor to complete the full twenty-workflow matrix.

## Intended permanent anchor

`anchor/routeos-kernel-jm-native-v1-9a-orchestrationroute-ding-pass`
