# v2.2A transport correction

The first v2.2A machine lanes stopped before kernel construction because an oversized text carrier was not preserved byte-for-byte by the connected file-write route. No kernel, compiler, linker, ISO or QEMU claim failed.

The correction route moves the exact local source archive through a file-aware binary endpoint, then verifies its SHA-256 before expansion. The machine gate remains unchanged.
