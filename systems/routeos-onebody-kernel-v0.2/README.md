# RouteOS OneBody → Kernel v0.2

This is the next bounded gate after **ROUTEOS KERNEL HARD-BODY DING: PASS**.

## Route under proof

`Cading/Kading source → recovered JM Android Forge v1.1 Cading parser → OneBody IR → RouteOS Native User ABI → generated x86-64 CPL3 body → int 0x80 PermissionGate → RouteOS state change → Trace → Ding`

## Boundary

- User Body 1 is generated from `.jm.cading` source.
- User Body 2 remains the hand-authored fault/recovery reference body.
- The kernel floor is inherited from the proven v0.1A machine-proof commit.
- CadenVM is preserved as compiler/runtime lineage; this gate lowers directly into the proven native user ABI.
- No compiler-to-kernel Ding is claimed until the generated body boots and runs under independent GRUB/QEMU proof.
