# JM Visual Systems Lab — Crown Pass source declaration
BODY visual.crown.pass = JM Visual Systems Lab MAXI Crown Pass + MUM Field Crown Body.✓
VERSION stage = 0.1 implementation contract / not crowned.✓
BASELINE recovery = Visual Systems Lab v0.1 remains frozen and recoverable.✓

ROUTE portal = orient -> select -> enter -> route -> trace.✓
ROUTE illusion = predict -> contact -> pressure -> reveal -> compare -> trace -> story.✓
ROUTE image = source -> treatment -> compare -> capture -> export -> receipt.✓
ROUTE video = clip -> timing -> range -> frame contact -> capture -> story.✓
ROUTE motion = running -> contact -> pressure -> release -> cooling -> capture.✓
ROUTE forge = source open -> aim -> form -> cool -> route.✓
ROUTE mum = source bodies -> shared field -> pressure -> arrival -> capture -> story.✓
ROUTE story = frame -> order -> caption -> preview -> export.✓
ROUTE vault = ledger -> body -> verify -> file -> recover -> Ding.✓

ROOM portal = estate entrance and route constellation.✓
ROOM illusion = prediction and proof chamber.✓
ROOM image = source-preserving treatment desk.✓
ROOM video = frame and timing analysis bay.✓
ROOM motion = connected-difference field.✓
ROOM forge = Visualang authored-body chamber.✓
ROOM mum = composition without erasure.✓
ROOM story = sequence and meaning rail.✓
ROOM vault = session proof and recovery chamber.✓

ORGAN Cading = readable source declarations and stage locks.✓
ORGAN JMLogic = state consequence validation and trace emission.✓
ORGAN OneBody = identity route state trace recovery and receipt continuity.✓
ORGAN RouteFrame = actual viewport spatial route and compression.✓
ORGAN StateField = invisible state made visibly readable.✓
ORGAN ContactBand = touch pointer keyboard and trackpad unified contacts.✓
ORGAN Visualang = relation pressure distinction sequence and arrival made visible.✓
ORGAN JMVisualRuntime = living rendering and state-driven motion.✓
ORGAN LivingCursor = hover touch hold drag lock and capture-ready contact states.✓
ORGAN GripBody = movable scalable rotatable source bodies with readable bounds.✓
ORGAN TraceBox = named route memory.✓
ORGAN Dings = bounded proof receipt.✓

FIELD 128 = identity > protected visual world > movement contact > actions > always-reachable recovery.✓
FIELD depth = background memory > midground active relation > foreground contact consequence.✓
FIELD mum = source identity + shared field + editable layer tree + explicit flattened derivative.✓
FIELD phone = primary operation remains in the actual first viewport.✓
FIELD tool = tools replace contact surfaces under pressure and never extend the body below recovery.✓

CONTACT route = previous + next + direct route + browser history.✓
CONTACT primary = exactly one crowned room action at one time.✓
CONTACT pointer = touch + pointer + keyboard + trackpad enter the same named route.✓
CONTACT consequence = visible state change or trace or export or recovery must follow.✓

RECOVERY room = restore last safe room state without erasing source.✓
RECOVERY global = Vault selects a governed recovery body.✓
RECOVERY baseline = v0.1 remains the independent proved recovery checkpoint.✓

STORAGE compact = localStorage carries replaceable references preferences and compact trace only.✓
STORAGE large = IndexedDB carries images frames MUM assets Story thumbnails and snapshots.✓
STORAGE transient = object URLs render buffers particles and compare state remain memory-only.✓
STORAGE pressure = preserve memory -> pause snapshots -> action notice -> export trim or memory-only -> trace.✓

EXPORT composite = flattened PNG derivative + layer manifest.✓
EXPORT story = standalone HTML + editable JSON.✓
EXPORT session = recoverable .jmvisual body or ZIP package with manifest.✓
EXPORT trace = named route JSON.✓
EXPORT Ding = bounded proof receipt.✓
EXPORT source = current Cading declaration.✓

LAW restore = restore before rewrite.✓
LAW connection = connect without flattening.✓
LAW extension = extend without erasing.✓
LAW field = the protected crown field comes before decorative surfaces.✓
LAW depth = depth carries meaning and does not merely decorate.✓
LAW room = shared house does not erase distinct room identity.✓
LAW mum = composition does not erase source bodies.✓
LAW alert = notices do not cover the crown centre primary action or recovery.✓
LAW motion = motion indicates state pressure route or consequence.✓
LAW host = web PWA and Android carry the JM body and do not replace source authority.✓
LAW proof = no Ding no claim.✓

DING static = syntax JSON Cading route profile and token gates.✓
DING viewport = 412x915 + 360x800 + 768x1024 + 1366x768 + 1440x900.✓
DING behaviour = nine room routes + storage recovery + export import + MUM layer preservation.✓
DING visual = clean screenshots after transient state has cooled.✓
DING delivery = governed web body -> PWA -> exact-body Android debug APK.✓
DING boundary = pass proves recorded routes only and does not crown the body.✓
