# JM Estate Visual Systems Lab — source-side route seed
BODY visual.systems.lab = JM Estate Visual Systems Lab.✓
VERSION stage = 0.1.0 working body not crowned.✓

ROUTE illusion = source -> signal -> contact -> pressure -> compare -> trace -> Ding.✓
ROUTE image = source -> treatment -> difference -> compare -> export -> receipt.✓
ROUTE video = clip -> timing -> frame -> capture -> story -> receipt.✓
ROUTE motion = still -> difference -> preserved relation -> movement -> trace.✓
ROUTE prompt = aim -> separate -> compose -> govern -> output.✓
ROUTE story = frame -> order -> meaning -> export -> receipt.✓

FIELD 128 = identity > protected visual world > movement contact > actions > always-reachable recovery.✓
FIELD device = touch + keyboard + trackpad + configurable contact.✓
FIELD visualang = JMLogic made visible through state, relation, pressure and consequence.✓

LAW source = source remains recoverable before treatment.✓
LAW contact = touching the field causes readable state and consequence.✓
LAW bodies = shared organs do not erase room identity.✓
LAW proof = no Ding, no claim.✓
LAW delivery = OneBody source -> build -> receipt -> package -> OPEN_FIRST.✓
LAW host = web and Android hosts serve the JM body; they do not govern source authority.✓
