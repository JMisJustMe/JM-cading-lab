# MASTER WEBSITE · v1.3.0 SR

Promoted authority: **JMISJUSTME Living Website v1.3.0 SR**.

This GitHub seat contains:

- governing authority and exact package hashes;
- the proper Android carrier project;
- the API-36 APK/AAB production workflow;
- an explicit binary-transfer boundary.

## Android route

The carrier does not pretend a shallow launcher is the full 35 MB website. It uses Android's document picker to import the exact promoted OneBody, copies it into private app storage, verifies the required Master Website identity markers, and then runs it locally/offline in a WebView. The stored body persists across app restarts.

Package: `com.jmisjustme.livingestate`  
Version: `1.3.0` (`130`)

The final embedded-body APK remains a later packaging refinement after an exact binary transfer route is available to GitHub. The current carrier is a genuine executable and contactable Android body, not a visual mock-up.
